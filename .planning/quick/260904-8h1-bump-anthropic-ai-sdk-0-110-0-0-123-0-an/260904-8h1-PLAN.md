---
quick_id: 260904-8h1
phase: quick-260904-8h1
plan: "01"
description: Bump @anthropic-ai/sdk 0.110.0 → 0.123.0 and add claude-opus-5 to MODEL_CATALOG.anthropic
date: 2026-09-04
mode: quick
type: execute
wave: 1
autonomous: true
depends_on: []
source: .planning/STATE.md (Open Todos → "Post-v1 (2026-09-04, from the Task 4 checkpoint discussion)")
files_modified:
  - "package.json"
  - "pnpm-lock.yaml"
  - "src/lib/llm/types.ts"
  - ".planning/STATE.md"
requirements:
  - POSTV1-SDK-BUMP        # @anthropic-ai/sdk exact pin 0.110.0 -> 0.123.0 + refreshed lockfile
  - POSTV1-MODEL-CATALOG   # claude-opus-5 appended to MODEL_CATALOG.anthropic
  - POSTV1-GATES           # biome + tsc + unit + integration all green; STATE todo closed

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "package.json pins @anthropic-ai/sdk at exactly 0.123.0 (no caret, no tilde) and node_modules resolves that same version"
    - "pnpm-lock.yaml is consistent with package.json — a second `pnpm install` is a no-op and leaves the lockfile unchanged"
    - "MODEL_CATALOG.anthropic offers four IDs ending with claude-opus-5, appended last so the cheapest-first ordering the file's comment claims is still literally true"
    - "The Settings → AI provider dropdown still auto-resets to claude-haiku-4-5 when the provider is switched to anthropic (anthropic[0] is unchanged by the append)"
    - "All four gates pass on the bumped tree: biome check, tsc --noEmit, unit tests, Testcontainers integration tests"
    - "The STATE.md Post-v1 todo for this bump is marked done in place (struck through with a DONE annotation), not deleted"
  artifacts:
    - path: "package.json"
      provides: "The exact dependency pin for the Anthropic SDK"
      contains: '"@anthropic-ai/sdk": "0.123.0"'
    - path: "pnpm-lock.yaml"
      provides: "Reproducible resolution of the bumped SDK for `pnpm install --frozen-lockfile` (Docker build)"
      contains: "@anthropic-ai/sdk"
    - path: "src/lib/llm/types.ts"
      provides: "The curated model dropdown catalog (D-01) including the new opus generation"
      contains: "claude-opus-5"
  key_links:
    - from: "src/lib/llm/types.ts (MODEL_CATALOG.anthropic)"
      to: "src/app/(app)/settings/llm-provider-form.tsx"
      via: "MODEL_CATALOG[provider] dropdown + catalog[0] auto-reset on provider switch"
      pattern: "MODEL_CATALOG\\[provider\\]"
    - from: "package.json pin"
      to: "src/lib/llm/providers/anthropic.ts"
      via: "client.messages.parse + zodOutputFormat — the only SDK call surface besides models.list"
      pattern: "messages\\.parse"
---

<objective>
Bump the Anthropic SDK to the current latest (0.123.0) and add the `claude-opus-5` model ID to the
curated dropdown catalog. Two mechanical edits, then prove nothing broke.

Purpose: closes the Post-v1 todo the maintainer opened at the Phase 7 Task 4 checkpoint 2026-09-04
("approved, bump SDK-nya nanti setelah phase 7 ditutup"). AIDA's LLM layer stays model-agnostic and
bring-your-own (CLAUDE.md non-negotiable) — this only refreshes one vendor adapter's client library
and one entry in a curated, free-text-escapable dropdown. No provider gains special treatment.

Output: `package.json` + `pnpm-lock.yaml` on 0.123.0, one appended catalog entry in
`src/lib/llm/types.ts`, four green gates, and the STATE.md todo closed in place.

Scope guard (verified before planning, do not re-research):
- No 1.x of `@anthropic-ai/sdk` exists on npm as of 2026-09-04; 0.123.0 (2026-09-01) is latest.
- Changelog 0.111–0.123 does not touch `messages.parse`, `helpers/zod`'s `zodOutputFormat`,
  `models.list`, or the client `timeout`/`maxRetries` options — i.e. every surface AIDA actually
  uses (`src/lib/llm/providers/anthropic.ts`, `src/lib/llm/test-connection.ts`). The only breaking
  changes land in the beta Files/Skills namespaces, which AIDA does not import.
- 0.122.0 documents TypeScript 5.0 as the floor; this repo is well past it.
- `openai` and `ollama` are explicitly NOT bumped. `website/` is a separate pnpm workspace with its
  own lockfile and is NOT touched. No docs/README/website file mentions the SDK version or any
  Anthropic model ID (grep-confirmed), so there is no doc to update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@package.json
@src/lib/llm/types.ts
@src/lib/llm/providers/anthropic.ts
@src/lib/llm/test-connection.ts
</context>

<environment_notes>
Read before running anything — these are the two known Windows/Git-Bash traps in this repo.

1. **Node version for pnpm.** Volta pins Node 22.23.1, but in non-interactive shells `pnpm` can
   resolve to a Node-20 standalone and fail with an engines/undici error. If that happens, either
   prefix PATH with `/c/Users/MANAGER-IT/AppData/Local/Volta/tools/image/node/22.23.1` and retry,
   or call the command as `volta run --node 22.23.1 pnpm …` (the exact version string is required —
   `volta run --node 22` fails offline).
2. **Gate binaries are invoked through `node`, not `pnpm exec`.** The RTK shell hook can intercept
   `pnpm exec`. Every gate in this plan calls the binary directly:
   `node node_modules/@biomejs/biome/bin/biome`, `node node_modules/typescript/bin/tsc`,
   `node node_modules/vitest/vitest.mjs` (all three paths verified to exist).
3. **Line endings.** `.gitattributes` sets `* text=auto eol=lf`; the Windows working tree is CRLF.
   That is normal and expected — never mass-reformat or renormalize while doing this task.
</environment_notes>

<supply_chain_note>
`@anthropic-ai/sdk` is not a new dependency: it has been in the tree since 04-01 under the official
`@anthropic-ai` npm scope, published by Anthropic, repository `anthropics/anthropic-sdk-typescript`.
This is a version bump of an already-trusted first-party package, not a new-package install, so no
`[ASSUMED]`/`[SUS]` human legitimacy checkpoint applies. Task 1 still asserts provenance
(name + version + repository field) on the installed tree rather than trusting the resolver blindly.
</supply_chain_note>

<tasks>

<task type="auto">
  <name>Task 1: Pin @anthropic-ai/sdk 0.123.0 and refresh the lockfile</name>
  <files>package.json, pnpm-lock.yaml</files>
  <precondition>Repo working tree is clean (`git status --porcelain` empty) before starting — the diff guards in this plan assume no unrelated pending edits.</precondition>
  <action>
    In `package.json`, change the `dependencies` entry `"@anthropic-ai/sdk"` from `0.110.0` to
    `0.123.0`. Keep it an exact pin — no caret, no tilde — matching how the other runtime-critical
    deps in this file are pinned. Change nothing else in the file; in particular leave `openai` and
    `ollama` on their current versions.

    Then run `pnpm install` from the repo root to refresh `pnpm-lock.yaml` and `node_modules`. If it
    fails with an engines/undici error, retry via `volta run --node 22.23.1 pnpm install` per the
    environment notes. Do not pass `--frozen-lockfile` (the point of this step is to update the
    lockfile) and do not run install inside `website/`, which is a separate workspace with its own
    lockfile.

    If `pnpm install` reports peer-dependency warnings, read them but do not "fix" them by editing
    other package versions — an unrelated version change here is out of scope and would break the
    diff guard below.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'),i=require('./node_modules/@anthropic-ai/sdk/package.json');if(p.dependencies['@anthropic-ai/sdk']!=='0.123.0')throw new Error('package.json pin wrong: '+p.dependencies['@anthropic-ai/sdk']);if(i.name!=='@anthropic-ai/sdk'||i.version!=='0.123.0')throw new Error('installed tree wrong: '+i.name+'@'+i.version);if(!/anthropics\/anthropic-sdk-typescript/.test(JSON.stringify(i.repository||'')))throw new Error('provenance check failed');console.log('pin+provenance ok')"</automated>
    <automated>grep -q "@anthropic-ai/sdk@0.123.0" pnpm-lock.yaml &amp;&amp; echo "lockfile resolves 0.123.0"</automated>
    <automated>test "$(git diff --name-only | sort | tr '\n' ' ')" = "package.json pnpm-lock.yaml " &amp;&amp; git diff --stat</automated>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
  </verify>
  <done>
    `package.json` pins exactly `0.123.0`; `node_modules/@anthropic-ai/sdk` is that version and
    carries the official Anthropic repository field; `pnpm-lock.yaml` resolves it; `git diff
    --name-only` lists exactly `package.json` and `pnpm-lock.yaml` and nothing else; `tsc --noEmit`
    is clean, proving `messages.parse`, `zodOutputFormat` and `models.list` still typecheck against
    the adapter and the connection probe.
  </done>
  <commit>chore(quick-260904-8h1): bump @anthropic-ai/sdk 0.110.0 -> 0.123.0</commit>
</task>

<task type="auto">
  <name>Task 2: Append claude-opus-5 to MODEL_CATALOG.anthropic</name>
  <files>src/lib/llm/types.ts</files>
  <action>
    In `src/lib/llm/types.ts`, extend the `anthropic` array of `MODEL_CATALOG` with one additional
    string ID, `claude-opus-5`, placed last — after `claude-opus-4-8`. Position matters: the comment
    directly above the catalog states the list is biased toward the cheapest/fastest tier, and the
    settings form uses `catalog[0]` as the auto-reset default when the provider is switched. Keeping
    the append at the end leaves `claude-haiku-4-5` as the anthropic default and keeps that comment
    literally true, so the comment needs no edit.

    Do not touch the `openai` or `ollama` arrays, the `LlmProviderName` union, or any other export in
    the file. Do not remove `claude-opus-4-8` — this is an addition, not a replacement. The resulting
    line stays under Biome's 100-column limit, so no reflow is needed.
  </action>
  <verify>
    <automated>grep -qF 'anthropic: ["claude-haiku-4-5", "claude-sonnet-5", "claude-opus-4-8", "claude-opus-5"],' src/lib/llm/types.ts &amp;&amp; echo "catalog line exact + ordering preserved"</automated>
    <automated>node node_modules/@biomejs/biome/bin/biome check src/lib/llm/types.ts</automated>
    <automated>test "$(git diff --name-only | sort | tr '\n' ' ')" = "src/lib/llm/types.ts " &amp;&amp; git diff --stat</automated>
  </verify>
  <done>
    `MODEL_CATALOG.anthropic` is exactly the four-element list with `claude-opus-5` last;
    `claude-haiku-4-5` remains index 0 so the settings-form auto-reset default is unchanged; Biome is
    clean on the file; `git diff --name-only` lists only `src/lib/llm/types.ts`.
  </done>
  <commit>feat(quick-260904-8h1): add claude-opus-5 to the anthropic model catalog</commit>
</task>

<task type="auto">
  <name>Task 3: Run the four gates and close the STATE.md Post-v1 todo</name>
  <files>.planning/STATE.md</files>
  <precondition>Docker daemon is running — `pnpm test:integration` is Testcontainers-backed (pgvector) and will fail to start otherwise. Verify with `docker info` before the integration gate; halt and report if the daemon is down rather than skipping the gate.</precondition>
  <action>
    First run all four gates, in this order, from the repo root, and record the pass/fail plus the
    test counts for the SUMMARY: Biome (`node node_modules/@biomejs/biome/bin/biome check .`),
    TypeScript (`node node_modules/typescript/bin/tsc --noEmit`), unit tests
    (`node node_modules/vitest/vitest.mjs run tests/unit`), and integration tests
    (`volta run --node 22.23.1 pnpm test:integration` — roughly 30 tests, 3–6 minutes). The full e2e
    suite is deliberately not part of this task's gate set.

    If a gate fails, stop and report rather than patching around it: a failure here is real signal
    about the SDK bump, and the correct response is diagnosis, not a workaround. A stale generated
    `.next/` directory has historically produced phantom `tsc` failures in this repo (see the Phase 7
    gate-run note in STATE.md) — if and only if the TypeScript errors point at generated files under
    `.next/`, delete that gitignored directory and re-run the gate.

    Then edit the single Open Todos bullet in `.planning/STATE.md` under the
    "Post-v1 (2026-09-04, from the Task 4 checkpoint discussion — not a blocker, not part of Phase 7)"
    heading — the one about bumping the Anthropic SDK. Mark it done the way this file already marks
    every other closed todo: wrap the existing bullet text in `~~strikethrough~~` markers and append
    a trailing annotation naming this quick task, the date, and what shipped (the exact pin, the
    lockfile refresh, the catalog entry, and the fact that all four gates were green with their
    counts). Preserve the original wording inside the strikethrough — do not delete the line, do not
    reword it, and do not touch any other bullet, the heading, or the frontmatter. The frontmatter
    refresh and the Quick Tasks Completed table row are the orchestrator's `state.update` job, not
    this task's.
  </action>
  <verify>
    <automated>node node_modules/@biomejs/biome/bin/biome check .</automated>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit</automated>
    <automated>volta run --node 22.23.1 pnpm test:integration</automated>
    <automated>grep -c '~~Bump `@anthropic-ai/sdk`' .planning/STATE.md | grep -qx 1 &amp;&amp; grep -q 'quick-260904-8h1' .planning/STATE.md &amp;&amp; echo "STATE todo struck through + annotated"</automated>
    <automated>test "$(git diff --numstat -- .planning/STATE.md | cut -f1,2 | tr '\t' ' ')" = "1 1" &amp;&amp; echo "exactly one line edited in place, nothing deleted"</automated>
  </verify>
  <done>
    All four gates exit 0 (Biome clean across the repo, `tsc --noEmit` clean, unit suite green,
    integration suite green against Testcontainers pgvector); the STATE.md Post-v1 bullet for this
    bump is struck through in place and annotated with `quick-260904-8h1`, and the STATE.md diff is
    exactly one line changed — one insertion, one deletion, nothing removed from the file.
  </done>
  <commit>docs(quick-260904-8h1): close the Post-v1 SDK-bump todo in STATE.md</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dependency tree | A third-party artifact enters the build and ships inside the Docker image |
| AIDA worker → Anthropic API | Ticket text (untrusted input) leaves the host through this SDK's HTTP client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8h1-SC | Tampering | `pnpm install` of `@anthropic-ai/sdk@0.123.0` | high | mitigate | Existing first-party dep under the official `@anthropic-ai` scope, not a new package; Task 1 asserts installed name + version + `anthropics/anthropic-sdk-typescript` repository provenance, and the `git diff --name-only` guard proves no other dependency was silently altered by the resolver |
| T-8h1-01 | Information disclosure | `src/lib/llm/providers/anthropic.ts` egress | medium | accept | Unchanged by this task — the adapter code is not edited, and the existing egress-isolation test suite plus the human-in-the-loop approval gate on AI sends still govern what leaves the host |
| T-8h1-02 | Elevation of privilege | Structured-output call surface (`messages.parse`) | low | mitigate | D-16's structural guarantee holds: this task adds no function/tool-calling field, and `tsc --noEmit` in Tasks 1 and 3 proves the no-tool-calling `output_config` shape still typechecks against 0.123.0 |
</threat_model>

<verification>
- `node node_modules/@biomejs/biome/bin/biome check .` exits 0
- `node node_modules/typescript/bin/tsc --noEmit` exits 0
- `node node_modules/vitest/vitest.mjs run tests/unit` exits 0
- `volta run --node 22.23.1 pnpm test:integration` exits 0
- `git log --oneline -3` shows three commits all scoped `(quick-260904-8h1)`
- `git status --porcelain` is empty at the end (everything committed, no stray files)
- Final cumulative diff touches exactly four files: `package.json`, `pnpm-lock.yaml`,
  `src/lib/llm/types.ts`, `.planning/STATE.md`
</verification>

<success_criteria>
- `@anthropic-ai/sdk` is pinned at exactly `0.123.0` in `package.json`, resolved in
  `pnpm-lock.yaml`, and installed in `node_modules`
- `MODEL_CATALOG.anthropic` is `["claude-haiku-4-5", "claude-sonnet-5", "claude-opus-4-8",
  "claude-opus-5"]` — appended last, cheapest-first ordering comment still accurate
- Four gates green; no e2e run required
- The STATE.md Post-v1 todo is closed in place with a `quick-260904-8h1` annotation
- No changes to `openai`, `ollama`, `website/`, docs, README, or any adapter source
</success_criteria>

<output>
Create `.planning/quick/260904-8h1-bump-anthropic-ai-sdk-0-110-0-0-123-0-an/260904-8h1-SUMMARY.md` when done,
recording: the exact versions before/after, the gate results with test counts, anything surprising in
the `pnpm install` output (peer warnings, transitive bumps visible in the lockfile diff), and the
STATE.md line as it now reads.
</output>
