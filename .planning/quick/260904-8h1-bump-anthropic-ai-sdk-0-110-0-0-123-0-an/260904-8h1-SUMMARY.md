---
phase: quick-260904-8h1
plan: "01"
subsystem: ai
tags: [anthropic-sdk, llm, dependency-bump, model-catalog]

# Dependency graph
requires:
  - phase: 07-launch-readiness
    provides: Phase 7 close-out and the Task 4 checkpoint maintainer decision to defer this bump until after Phase 7 closed
provides:
  - "@anthropic-ai/sdk pinned at exactly 0.123.0 (was 0.110.0), lockfile refreshed"
  - "claude-opus-5 appended to MODEL_CATALOG.anthropic (settings dropdown)"
  - "Post-v1 STATE.md todo for this bump closed in place"
affects: [llm-provider-form, anthropic-adapter, settings]

# Actuals (#2632)
actuals:
  tokens: 1100
  tasks: 3
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - package.json
    - pnpm-lock.yaml
    - src/lib/llm/types.ts
    - .planning/STATE.md

key-decisions:
  - "Exact pin (no caret/tilde) kept for @anthropic-ai/sdk, matching existing runtime-critical dep convention"
  - "claude-opus-5 appended LAST in MODEL_CATALOG.anthropic so claude-haiku-4-5 stays index 0 (settings-form auto-reset default unchanged, cheapest-first comment stays true)"
  - "STATE.md edit left UNCOMMITTED per orchestrator instruction — the orchestrator's final step commits PLAN/SUMMARY/STATE docs artifacts together"

patterns-established: []

requirements-completed:
  - POSTV1-SDK-BUMP
  - POSTV1-MODEL-CATALOG
  - POSTV1-GATES

coverage:
  - id: D1
    description: "package.json pins @anthropic-ai/sdk at exactly 0.123.0, node_modules resolves the same version, pnpm-lock.yaml is consistent (no-op on re-install)"
    requirement: "POSTV1-SDK-BUMP"
    verification:
      - kind: other
        ref: "node -e provenance+pin assertion script (Task 1 verify)"
        status: pass
      - kind: other
        ref: "grep @anthropic-ai/sdk@0.123.0 pnpm-lock.yaml"
        status: pass
    human_judgment: false
  - id: D2
    description: "MODEL_CATALOG.anthropic offers four IDs ending with claude-opus-5, appended last"
    requirement: "POSTV1-MODEL-CATALOG"
    verification:
      - kind: other
        ref: "grep exact catalog line in src/lib/llm/types.ts (Task 2 verify)"
        status: pass
      - kind: other
        ref: "node node_modules/@biomejs/biome/bin/biome check src/lib/llm/types.ts"
        status: pass
    human_judgment: false
  - id: D3
    description: "All four gates (Biome, tsc, unit, integration) pass on the bumped tree; STATE.md Post-v1 todo closed in place"
    requirement: "POSTV1-GATES"
    verification:
      - kind: other
        ref: "node node_modules/@biomejs/biome/bin/biome check . (283 files, 0 errors)"
        status: pass
      - kind: other
        ref: "node node_modules/typescript/bin/tsc --noEmit (clean)"
        status: pass
      - kind: unit
        ref: "node node_modules/vitest/vitest.mjs run tests/unit (18 files, 90 tests)"
        status: pass
      - kind: integration
        ref: "volta run --node 22.23.1 pnpm test:integration (12 files, 30 tests, Testcontainers pgvector)"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-09-04
status: complete
---

# Quick Task 260904-8h1: Bump @anthropic-ai/sdk 0.110.0 -> 0.123.0 Summary

**@anthropic-ai/sdk pinned to 0.123.0, claude-opus-5 added to the curated Anthropic model dropdown, all four quality gates green.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-09-03T23:14:00Z (approx.)
- **Completed:** 2026-09-03T23:28:42Z
- **Tasks:** 3 (3 completed)
- **Files modified:** 4 (`package.json`, `pnpm-lock.yaml`, `src/lib/llm/types.ts`, `.planning/STATE.md`)

## Accomplishments
- `@anthropic-ai/sdk` bumped from `0.110.0` to `0.123.0` (exact pin, no caret/tilde), lockfile refreshed via `pnpm install`, provenance verified (`anthropics/anthropic-sdk-typescript` repository field on the installed package)
- `claude-opus-5` appended as the fourth, last entry of `MODEL_CATALOG.anthropic` in `src/lib/llm/types.ts` — `claude-haiku-4-5` remains index 0, so the settings-form auto-reset default and the "cheapest-first" catalog comment are both unchanged
- All four quality gates run and passed on the bumped tree: Biome (283 files clean), `tsc --noEmit` (clean), unit tests (18 files / 90 tests), Testcontainers integration tests (12 files / 30 tests, 576.77s)
- Post-v1 STATE.md todo for this bump struck through in place and annotated with `quick-260904-8h1`, exact gate counts included

## Before / After Versions

| Item | Before | After |
|---|---|---|
| `@anthropic-ai/sdk` (package.json + installed + lockfile) | `0.110.0` | `0.123.0` |
| `MODEL_CATALOG.anthropic` | `["claude-haiku-4-5", "claude-sonnet-5", "claude-opus-4-8"]` | `["claude-haiku-4-5", "claude-sonnet-5", "claude-opus-4-8", "claude-opus-5"]` |

## Task Commits

Each task was committed atomically:

1. **Task 1: Pin @anthropic-ai/sdk 0.123.0 and refresh the lockfile** - `5ddc2f7` (chore)
2. **Task 2: Append claude-opus-5 to MODEL_CATALOG.anthropic** - `acbe670` (feat)
3. **Task 3: Run the four gates and close the STATE.md Post-v1 todo** - NOT committed (see below)

**Task 3 commit note:** per the orchestrator's explicit constraint, Task 3's only file change (`.planning/STATE.md`) was left **uncommitted** — the orchestrator commits PLAN/SUMMARY/STATE docs artifacts together in its own final step. Task 3 had no other code changes, so no separate commit was created for it (the plan's own `<commit>` line for Task 3 was superseded by this instruction).

## `pnpm install` Output — Surprises

- `pnpm install` completed cleanly on the first attempt (Node 22.23.1 via explicit Volta PATH prefix — no engines/undici error hit).
- `WARN  2 deprecated subdependencies found: glob@10.5.0, prebuild-install@7.1.3` — pre-existing, unrelated to this bump (not new deprecations introduced by `@anthropic-ai/sdk`).
- `WARN  Issues with peer dependencies found` — printed with no accompanying detail lines in the terminal output; nothing pnpm identified as needing a fix, and no other package version changed as a result.
- **Lockfile diff, verified by manually diffing the raw file content across commits (git's own `git diff` is suppressed for this file by `.gitattributes`' `pnpm-lock.yaml text eol=lf -diff` rule):** exactly 3 hunks, all `@anthropic-ai/sdk` version-string changes (`specifier`, `version`, the two resolution-block headers). Zero transitive dependency changes. This directly confirms the plan's diff guard held — no unrelated dependency was silently altered by the resolver.

## Files Created/Modified
- `package.json` - `@anthropic-ai/sdk` dependency pin `0.110.0` -> `0.123.0`
- `pnpm-lock.yaml` - refreshed resolution for the bumped SDK only (3 hunks, no transitive changes)
- `src/lib/llm/types.ts` - `MODEL_CATALOG.anthropic` gained `claude-opus-5` as its 4th, last entry
- `.planning/STATE.md` - Post-v1 SDK-bump todo struck through and annotated (left uncommitted, per orchestrator instruction)

## Decisions Made
- Kept the exact-pin convention (no caret/tilde) for `@anthropic-ai/sdk`, matching other runtime-critical deps in `package.json`.
- Appended `claude-opus-5` at the END of the anthropic catalog array (not sorted alphabetically or by recency) to preserve the existing cheapest-first ordering comment and the settings form's `catalog[0]` auto-reset default (`claude-haiku-4-5`).
- Did not touch `openai`, `ollama`, or `website/` — out of scope per the plan's scope guard.

## Deviations from Plan

None — plan executed exactly as written, including the orchestrator-level override on Task 3's commit behavior (which the plan's own `<commit>` line anticipated would be handled differently; the orchestrator instructions took precedence and are documented above, not treated as a deviation from correctness).

One tooling note (not a deviation from the plan's content, but worth recording): the shell's `rtk` hook intercepts bare `grep` and rewrites its output format, which broke the literal `grep -c ... | grep -qx 1` verify command as written (rtk's rewritten output is not a bare integer). Worked around by invoking `\grep` (bypassing the shell alias/hook) for STATE.md verification; the underlying assertion (exactly one strikethrough match, `quick-260904-8h1` present) passed once measured correctly.

## Gate Results (Task 3)

| Gate | Command | Result | Detail |
|---|---|---|---|
| 1. Biome | `node node_modules/@biomejs/biome/bin/biome check .` | PASS | 283 files checked, 0 errors, exit 0 |
| 2. TypeScript | `node node_modules/typescript/bin/tsc --noEmit` | PASS | clean, exit 0 |
| 3. Unit | `node node_modules/vitest/vitest.mjs run tests/unit` | PASS | 18 files, 90 tests, 7.50s, exit 0 |
| 4. Integration | `volta run --node 22.23.1 pnpm test:integration` | PASS | 12 files, 30 tests, 576.77s (Testcontainers pgvector), exit 0 |

e2e suite deliberately not run (out of scope per plan).

## STATE.md Line As It Now Reads

```
- ~~Bump `@anthropic-ai/sdk` 0.110.0 → 0.123.0 (no 1.x exists on npm as of 2026-09-04; changelog 0.111–0.123 touches nothing AIDA uses; add `claude-opus-5` to `MODEL_CATALOG` in `src/lib/llm/types.ts` while there). Maintainer decision 2026-09-04: after Phase 7 closes, as a `/gsd-quick` task with typecheck + unit + LLM integration tests as the gate.~~ — DONE in quick-260904-8h1 (2026-09-04): package.json/pnpm-lock.yaml pinned to exactly 0.123.0, `claude-opus-5` appended last to `MODEL_CATALOG.anthropic`; all four gates green — Biome 283 files clean, `tsc --noEmit` clean, unit 90/90 (18 files), integration 30/30 (12 files, Testcontainers pgvector).
```

## Issues Encountered
None beyond the `rtk` grep-hook workaround noted above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Quick task complete; `@anthropic-ai/sdk` and the model catalog are current as of 2026-09-04.
- STATE.md edit is staged but uncommitted, pending the orchestrator's final docs-artifact commit (this SUMMARY.md included).
- No blockers for future work.

---
*Phase: quick-260904-8h1*
*Completed: 2026-09-04*

## Self-Check: PASSED

All created/modified files found on disk (`package.json`, `pnpm-lock.yaml`, `src/lib/llm/types.ts`, `.planning/STATE.md`, this SUMMARY.md); both task commits (`5ddc2f7`, `acbe670`) found in `git log --oneline --all`.
