---
phase: 01-foundation
plan: 8
type: execute
wave: 6
depends_on: [7]
files_modified: []
autonomous: false
requirements: [AIDA-10, AIDA-11, AIDA-21]
must_haves:
  truths:
    - "A human has run the full self-host flow via docker compose up and confirmed the onboarding + shell render correctly"
    - "The first-run /setup wizard creates an admin and the workspace, then self-disables"
    - "Login works, the app shell is screenshot-worthy in both light and dark mode, and sign out works"
    - "Server-side auth is enforced (signed-out users cannot reach (app) routes)"
  artifacts: []
  key_links: []
---

<objective>
Final Phase-1 acceptance gate: a human verifies the running, self-hosted stack end-to-end — first-run setup, login, the screenshot-worthy app shell (light + dark), the AI toggle, and server-side auth enforcement. This confirms the "full shell, empty rooms" deliverable (D-25) is README-hero-ready and that AIDA-10 / AIDA-11 / AIDA-21 behave correctly for a real user, not just in tests.

Purpose: Code verification and integration tests prove the parts; this gate proves the whole product experience and captures the visual quality that drives stars (repo health).
Output: Human sign-off (or a concrete defect list to route into a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/ROADMAP.md

<note>
This plan is a single human-verification checkpoint. Before it runs, Plan 07 already brought the stack up and confirmed /api/health automatically. Here a human exercises the actual UI/flow. Keep the stack running from Plan 07, or bring it up fresh (docker compose up -d) for the walkthrough.
</note>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human walkthrough of the self-hosted onboarding + app shell</name>
  <files>(verification only — no source files modified)</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (the visual contract being verified)
    - .planning/phases/01-foundation/01-07-SUMMARY.md (how Plan 07 brought the stack up + the exact run commands)
  </read_first>
  <action>
    This is a human-verification checkpoint — do NOT auto-complete it. Ensure the stack is running (reuse Plan 07's `docker compose up -d`, or bring it up fresh per the prereq in <how-to-verify>), then present the numbered walkthrough below to the user and PAUSE for their reply. On "approved", record sign-off in the SUMMARY; on a defect list, record it verbatim so it can be routed into `/gsd:plan-phase 1 --gaps`. The concrete steps the human performs are enumerated in <how-to-verify>.
  </action>
  <what-built>
    The complete Phase-1 foundation, self-hosted via docker compose: PostgreSQL/pgvector + Next.js app + pg-boss worker + Caddy. First-run setup wizard, credentials login, auth-gated app shell (sidebar + top bar + dark mode + user menu), Tickets/KB empty states, and a tenant-scoped AI toggle in Settings. Server-side auth (middleware + layout guard) and workspace isolation (scopedDb, proven by the integration test in Plan 03) are in place.
  </what-built>
  <how-to-verify>
    Prereq: from a clean checkout, `cp .env.example .env`, set `BETTER_AUTH_SECRET` (openssl rand -base64 32), keep `DOMAIN=localhost`, leave `ADMIN_*` blank. Run `docker compose up -d` and wait ~90s (db -> migrate -> app/worker/caddy). If it is already up from Plan 07, reuse it.

    1. First-run setup (D-07): open http://localhost — you should be redirected to /setup (fresh instance, zero users). Fill Workspace name (watch the URL slug auto-derive), Your name, Email, Password, Confirm password; click "Create workspace". Expect a redirect to /login with the toast "Workspace created. Sign in to continue."
    2. Setup self-disable: manually navigate to http://localhost/setup again — you should be redirected to /login (the wizard is permanently disabled now). Confirm there is NO public "Create account" / register link anywhere (D-23).
    3. Login (AIDA-10): try a WRONG password first — expect the inline error "Invalid email or password..." (not a toast). Then sign in correctly -> you land on /tickets.
    4. App shell (D-22/D-25): confirm the 240px sidebar (AIDA wordmark; Tickets / Knowledge Base / Settings with icons; active item highlighted) and the 56px top bar (page title left; theme toggle + user avatar right). Click between Tickets and Knowledge Base — confirm the empty states ("Your inbox is empty", "No articles yet") and that the active nav highlight + page title update.
    5. Dark mode: click the theme toggle (Sun/Moon) — the whole UI switches light<->dark cleanly and persists on reload. Confirm BOTH modes look polished (README hero).
    6. Settings AI toggle (D-18): open Settings -> "AI Features"; the "Enable AI" switch is OFF by default. Toggle ON, reload — it stays ON (persisted). Toggle back OFF.
    7. Server-side auth (AIDA-10): user menu (avatar, top-right) -> "Sign out" -> back at /login. Now manually navigate to http://localhost/tickets while signed out — you must be redirected to /login (server-side enforcement, not just hidden UI).
    8. Worker liveness (AIDA-21): open http://localhost/api/health — confirm 200 JSON with status ok, db connected, and a recent worker.lastRunAt.
    9. (Optional) capture light + dark screenshots of the shell + empty states for the future README hero.

    Reply "approved" if all steps pass. Otherwise list each failing step (number) with observed vs expected.
  </how-to-verify>
  <verify>
    <automated>MANUAL — human-verify checkpoint. Automated proxy already passed in Plan 07: docker compose up + curl http://localhost/api/health returns status ok with a recent worker lastRunAt.</automated>
  </verify>
  <resume-signal>Type "approved", or describe the issues (which numbered step, observed vs expected).</resume-signal>
  <done>Human has exercised steps 1-8 and either approved or produced a precise defect list; outcome recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- Human confirms steps 1-8 pass (setup -> login -> shell -> dark mode -> AI toggle -> sign out/guard -> health).
- Any defects are captured precisely enough to drive a `/gsd:plan-phase 1 --gaps` closure plan.
</verification>

<success_criteria>
A human has validated the full self-hosted Phase-1 experience: first-run onboarding, credentials auth with server-side enforcement, a polished light/dark app shell with empty-room stubs, a persisted tenant-scoped AI toggle, and a live worker heartbeat — AIDA-10, AIDA-11 (proven by Plan 03's test), and AIDA-21 confirmed for a real user.
</success_criteria>

<output>
Record the verification outcome in `.planning/phases/01-foundation/01-08-SUMMARY.md`: approved or the defect list. If approved, Phase 1 is complete and ready for `/gsd:verify-work`; if defects, route them into `/gsd:plan-phase 1 --gaps`.
</output>
