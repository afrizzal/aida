---
phase: 01-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - biome.json
  - .nvmrc
  - .gitignore
  - .env.example
  - components.json
  - postcss.config.mjs
  - vitest.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/theme-provider.tsx
  - src/components/ui/
  - src/lib/utils.ts
  - tests/unit/smoke.test.ts
autonomous: true
requirements: [AIDA-21]
must_haves:
  truths:
    - "pnpm install completes and the project type-checks with tsc --noEmit (exit 0)"
    - "Biome lint/format passes on all source files (exit 0)"
    - "shadcn/ui is initialized (components.json exists) with all 12 Phase-1 components present under src/components/ui"
    - "Dark mode is wired once via next-themes ThemeProvider in the root layout"
    - "vitest runs the unit suite green (smoke test passes)"
    - ".env.example documents every Phase-1 environment variable"
  artifacts:
    - path: "package.json"
      provides: "All Phase-1 dependencies (verified versions) + npm scripts"
      contains: "\"next\": \"16.2.9\""
    - path: ".nvmrc"
      provides: "Node 22 pin"
      contains: "22"
    - path: "next.config.ts"
      provides: "standalone output for Docker"
      contains: "output: \"standalone\""
    - path: "components.json"
      provides: "shadcn config (new-york / zinc / css-variables)"
      contains: "new-york"
    - path: "src/app/layout.tsx"
      provides: "Root layout: Inter font + ThemeProvider + sonner Toaster"
      min_lines: 25
    - path: "src/app/globals.css"
      provides: "Tailwind v4 + shadcn CSS-variable design tokens (zinc), --font-inter"
      contains: "@theme inline"
    - path: ".env.example"
      provides: "Documented config for all Phase-1 env vars"
      contains: "DATABASE_URL="
    - path: "vitest.config.ts"
      provides: "Unit test config with @ alias"
      contains: "vitest/config"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next-themes ThemeProvider"
      via: "<ThemeProvider attribute=\"class\" defaultTheme=\"system\" enableSystem>"
      pattern: "ThemeProvider"
    - from: "src/app/layout.tsx"
      to: "Inter font"
      via: "next/font/google, inter.variable on <html>"
      pattern: "Inter"
    - from: "tsconfig.json"
      to: "src/*"
      via: "path alias @/*"
      pattern: "\"@/\\*\""
---

<objective>
Bootstrap the AIDA application shell from greenfield: a Next.js 16 (App Router) + TypeScript project with the full locked toolchain (pnpm, Node 22, Biome, Tailwind v4, shadcn/ui new-york/zinc, next-themes dark mode, Vitest), every Phase-1 dependency installed at the researched versions, and a documented `.env.example`. This is the bedrock every other Phase-1 plan builds on.

Purpose: Make the repo a buildable, lintable, testable Next.js app with the design system wired once (D-22, D-25) — "full shell" foundation before any feature code.
Output: A project that `pnpm install && pnpm typecheck && pnpm lint && pnpm test && pnpm build` all pass on, with shadcn components and ThemeProvider ready for the auth + app-shell UI plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<notes>
- Repo is NOT empty: LICENSE, README.md, CLAUDE.md, docs/, .planning/, .gitignore, .git already exist. `create-next-app` refuses non-empty dirs — scaffold MANUALLY (write package.json + configs by hand). Do NOT overwrite or delete LICENSE / README.md / CLAUDE.md / docs/ / .planning/.
- Verified versions are LOCKED in RESEARCH.md "Standard Stack". Use them exactly.
- Tailwind v4 is CSS-first: NO tailwind.config.js/ts. All tokens live in src/app/globals.css.
- Two font weights only (400, 600) per UI-SPEC — no font-medium/bold.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js + TypeScript project, dependencies, and tooling configs</name>
  <read_first>
    - .gitignore (current state — you will APPEND, not replace)
    - README.md (confirm it exists; do NOT modify)
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack table + Project Structure + Biome config)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-13 runtime, D-19 testing)
  </read_first>
  <action>
    Manually scaffold the project (the directory is non-empty, so create-next-app is not usable).

    1. Create `.nvmrc` containing exactly: `22`

    2. Create `package.json` with `"name": "aida"`, `"private": true`, `"type": "module"`, `"packageManager": "pnpm@10.34.4"`, `"engines": { "node": ">=22" }`, and these scripts:
       - "dev": "next dev"
       - "build": "next build"
       - "start": "next start"
       - "worker": "tsx src/lib/worker/index.ts"
       - "lint": "biome check ."
       - "format": "biome format --write ."
       - "typecheck": "tsc --noEmit"
       - "test": "vitest run tests/unit"
       - "test:integration": "vitest run --config vitest.integration.config.ts"
       - "test:all": "pnpm test && pnpm test:integration"
       - "db:migrate": "prisma migrate dev"
       - "db:deploy": "prisma migrate deploy"
       - "db:generate": "prisma generate"
       - "db:studio": "prisma studio"

    3. Install dependencies at the EXACT versions from RESEARCH.md (use pnpm; let pnpm write pnpm-lock.yaml):
       - prod: `pnpm add next@16.2.9 react react-dom better-auth@1.6.22 prisma@7.8.0 @prisma/client@7.8.0 @prisma/adapter-pg@7.8.0 pg@8.22.0 pg-boss@12.23.0 zod@4.4.3 tailwindcss@4.3.1 next-themes@0.4.6`
       - dev (use --save-exact for biome): `pnpm add -D @biomejs/biome@2.5.1 --save-exact` then `pnpm add -D vitest@4.1.9 @vitest/coverage-v8 testcontainers@12.0.3 @testcontainers/postgresql@12.0.3 vitest-environment-prisma-postgres@2.0.0 tsx@4.22.4 esbuild @types/node @types/pg @types/react @types/react-dom typescript @better-auth/cli@1.4.21 @tailwindcss/postcss postcss`
       (esbuild is added now for the Plan 07 worker bundle. If any pinned version is yanked, install the nearest patch and note it in the SUMMARY.)

    4. Create `tsconfig.json` (strict) with: `"strict": true`, `"target": "ES2022"`, `"module": "esnext"`, `"moduleResolution": "bundler"`, `"jsx": "preserve"`, `"noEmit": true`, `"esModuleInterop": true`, `"skipLibCheck": true`, `"resolveJsonModule": true`, `"isolatedModules": true`, the Next.js plugin, `"paths": { "@/*": ["./src/*"] }`, `"baseUrl": "."`, include `["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"]`, exclude `["node_modules", "src/generated"]`.

    5. Create `next.config.ts`:
       ```ts
       import type { NextConfig } from "next";
       const nextConfig: NextConfig = {
         output: "standalone",
       };
       export default nextConfig;
       ```

    6. Create `biome.json` exactly as in RESEARCH.md "Biome Configuration" (schema, organizeImports on, formatter indentWidth 2 / lineWidth 100, linter recommended), and set files to ignore `node_modules`, `.next`, `src/generated`, `prisma/generated`.

    7. APPEND to the existing `.gitignore` (do not rewrite the file): add lines `/src/generated/` and `/dist/` (dist may already be present — keep one copy).

    8. Create `.env.example` documenting EVERY Phase-1 env var with comments and safe placeholders:
       ```
       # --- App ---
       BETTER_AUTH_URL=http://localhost:3000
       NEXT_PUBLIC_APP_URL=http://localhost:3000
       # Generate with: openssl rand -base64 32
       BETTER_AUTH_SECRET=replace-me-with-a-32-byte-random-secret

       # --- Database (Postgres 16 + pgvector) ---
       DATABASE_URL=postgresql://aida:aida@localhost:5432/aida
       POSTGRES_USER=aida
       POSTGRES_PASSWORD=aida
       POSTGRES_DB=aida

       # --- Reverse proxy (Caddy) ---
       # localhost for local dev; a real domain enables Let's Encrypt auto-HTTPS in prod
       DOMAIN=localhost

       # --- First-run admin bootstrap (OPTIONAL, headless/CI installs) ---
       # If set on a fresh instance, the app auto-creates the first org + admin and skips the /setup wizard.
       # Leave blank to use the interactive /setup wizard instead. NEVER ship default creds.
       ADMIN_EMAIL=
       ADMIN_PASSWORD=
       ADMIN_NAME=
       ```
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm exec tsc --noEmit && pnpm exec biome check .</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists and contains `"next": "16.2.9"`, `"better-auth": "1.6.22"`, `"prisma": "7.8.0"`, `"pg-boss": "12.23.0"`
    - `package.json` scripts include `"worker"`, `"db:migrate"`, `"test:integration"`
    - `.nvmrc` contains `22`
    - `next.config.ts` contains `output: "standalone"`
    - `tsconfig.json` contains `"@/*"` and `"strict": true`
    - `biome.json` contains `src/generated` in its ignore list
    - `.gitignore` contains `/src/generated/`
    - `.env.example` contains `DATABASE_URL=`, `BETTER_AUTH_SECRET=`, `DOMAIN=`, `ADMIN_EMAIL=`
    - `pnpm exec tsc --noEmit` exits 0; `pnpm exec biome check .` exits 0
    - LICENSE, README.md, CLAUDE.md still present and unmodified (git status shows them unchanged)
  </acceptance_criteria>
  <done>Project installs, type-checks, and lints clean; all locked dependencies present at researched versions; .env.example documents all config.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Tailwind v4 + shadcn/ui (new-york/zinc) + Inter font + dark-mode ThemeProvider + root layout</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Design System, Spacing, Typography, Color, Component Inventory, Implementation Notes 1-10)
    - src/app/layout.tsx (if Task 1 created a stub) and tsconfig.json (for @ alias)
    - .planning/phases/01-foundation/01-RESEARCH.md (shadcn Tailwind v4 setup)
  </read_first>
  <action>
    1. Create `postcss.config.mjs` with the Tailwind v4 PostCSS plugin:
       ```js
       export default { plugins: { "@tailwindcss/postcss": {} } };
       ```

    2. Initialize shadcn/ui (writes components.json + globals.css token block + src/lib/utils.ts):
       `npx shadcn@latest init --style new-york --base-color zinc --css-variables --yes`
       Confirm `components.json` contains `"style": "new-york"` and base color zinc. If init writes a tailwind.config file, DELETE it (Tailwind v4 is CSS-first — no config file, per UI-SPEC note 2).

    3. Install ALL 12 Phase-1 components from the official registry (UI-SPEC Component Inventory):
       `npx shadcn@latest add button input label card form switch separator dropdown-menu avatar sonner tooltip badge --yes`
       Confirm each exists under `src/components/ui/`.

    4. Add the Inter font in `src/app/layout.tsx` via `next/font/google`:
       - `import { Inter } from "next/font/google"; const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });`
       - Apply `className={inter.variable}` to `<html lang="en" suppressHydrationWarning>`.
       - In `globals.css`, set the base font: `body { font-family: var(--font-inter), system-ui, sans-serif; }` and ensure the `@theme inline` block maps `--font-sans` to `var(--font-inter)`.

    5. Create `src/components/theme-provider.tsx` — a `"use client"` wrapper re-exporting next-themes' `ThemeProvider` (pass-through props).

    6. In `src/app/layout.tsx`, wrap `{children}` with `<ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>` and render the sonner `<Toaster />` (from `@/components/ui/sonner`) inside `<body>`. Set `export const metadata = { title: "AIDA", description: "Open-source, AI-native, self-hostable helpdesk" }`.

    7. Replace `src/app/page.tsx` (root `/`) with a Server Component that does `import { redirect } from "next/navigation"; export default function Home() { redirect("/login"); }`. (The /login route is created in Plan 05; this redirect 404s until then — that is expected and validated end-to-end in Plan 07.)
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm exec biome check src/components src/app</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists and contains `new-york`
    - All 12 components exist: `ls src/components/ui` shows button, input, label, card, form, switch, separator, dropdown-menu, avatar, sonner, tooltip, badge (.tsx files)
    - NO `tailwind.config.js` or `tailwind.config.ts` exists at repo root
    - `src/app/globals.css` contains `@theme inline`
    - `src/app/layout.tsx` contains `Inter`, `ThemeProvider`, and `Toaster`
    - `src/app/layout.tsx` contains `attribute="class"` and `defaultTheme="system"`
    - `src/components/theme-provider.tsx` contains `"use client"` and `next-themes`
    - `src/app/page.tsx` contains `redirect("/login")`
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>shadcn initialized with all 12 components; Inter font + dark-mode ThemeProvider + sonner Toaster wired in the root layout; design tokens live in globals.css; no Tailwind config file.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest unit config + smoke test + full green-bar verification</name>
  <read_first>
    - tsconfig.json (for the @ alias to mirror into vitest)
    - .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture — vitest.config alias pattern)
    - package.json (confirm test scripts from Task 1)
  </read_first>
  <action>
    1. Create `vitest.config.ts` (UNIT tests only — integration config is created separately in Plan 03):
       ```ts
       import { defineConfig } from "vitest/config";
       import path from "node:path";
       export default defineConfig({
         test: {
           include: ["tests/unit/**/*.test.ts"],
           environment: "node",
         },
         resolve: { alias: { "@": path.resolve(__dirname, "src") } },
       });
       ```

    2. Create `tests/unit/smoke.test.ts` asserting the toolchain runs:
       ```ts
       import { describe, it, expect } from "vitest";
       import { cn } from "@/lib/utils";
       describe("smoke", () => {
         it("runs vitest and resolves the @ alias", () => {
           expect(cn("a", "b")).toContain("a");
         });
       });
       ```

    3. Run the full green-bar gate and fix anything that fails: `pnpm typecheck`, `pnpm lint`, `pnpm test`, and a production build `pnpm build` (next build must succeed with standalone output). Confirm `.next/standalone/server.js` is produced by the build.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm exec biome check . && pnpm exec vitest run tests/unit</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists and contains `vitest/config` and the `@` alias
    - `tests/unit/smoke.test.ts` exists and imports from `@/lib/utils`
    - `pnpm exec vitest run tests/unit` exits 0 with the smoke test passing
    - `pnpm build` completes successfully and `.next/standalone/server.js` exists
    - `pnpm exec tsc --noEmit` and `pnpm exec biome check .` both exit 0
  </acceptance_criteria>
  <done>The full toolchain (typecheck + lint + unit test + production build with standalone output) is green; the project is a verified, buildable Next.js app ready for feature plans.</done>
</task>

</tasks>

<verification>
- `pnpm install --frozen-lockfile` succeeds from the committed lockfile.
- `pnpm typecheck && pnpm lint && pnpm test && pnpm build` all pass.
- `components.json` present; 12 shadcn components under src/components/ui; no tailwind config file.
- Root layout has Inter + ThemeProvider + Toaster; globals.css has zinc CSS-variable tokens.
- `.env.example` documents all Phase-1 env vars; LICENSE/README untouched.
</verification>

<success_criteria>
A greenfield Next.js 16 + TypeScript app that builds with standalone output, lints/type-checks clean, runs a green unit suite, and ships the shadcn/ui design system + dark mode wired once — the foundation for all subsequent Phase-1 plans. (Partial AIDA-21: documented `.env.example` + buildable self-hostable app shell.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting: the exact installed versions (note any deviations from pinned versions), the shadcn preset confirmed, key file locations (layout, globals.css token block), and the `@/*` alias + `@/generated/*` convention for downstream plans.
</output>
