---
phase: 02-core-ticketing
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lib/tickets/status-token.ts
  - src/lib/tickets/sla.ts
  - src/lib/contacts/find-or-create-contact.ts
  - src/lib/tickets/create-ticket.ts
  - tests/integration/create-ticket.test.ts
autonomous: true
requirements: [AIDA-01, AIDA-03, AIDA-06]
must_haves:
  truths:
    - "Creating a ticket assigns a concurrency-safe, per-workspace sequential number with no duplicates under concurrent load"
    - "Ticket creation auto-links (or creates) a Contact by normalized (lowercased) email"
    - "Ticket creation stamps SLA first-response + resolution due timestamps derived from priority"
    - "The public status token is a dedicated high-entropy secret, not the ticket id"
  artifacts:
    - path: "src/lib/tickets/create-ticket.ts"
      provides: "createTicket() transaction: counter upsert + contact link + Ticket + initial Message"
      exports: ["createTicket"]
      min_lines: 40
    - path: "src/lib/tickets/sla.ts"
      provides: "DEFAULT_SLA_TARGETS + getSlaTargets(db, priority) + computeDueTimestamps()"
      exports: ["getSlaTargets", "DEFAULT_SLA_TARGETS"]
    - path: "src/lib/contacts/find-or-create-contact.ts"
      provides: "findOrCreateContact(db, {email,name,...}) normalized-email dedup"
      exports: ["findOrCreateContact"]
    - path: "src/lib/tickets/status-token.ts"
      provides: "generateStatusToken() via crypto.randomBytes"
      exports: ["generateStatusToken"]
  key_links:
    - from: "createTicket"
      to: "TicketCounter"
      via: "upsert increment inside $transaction"
      pattern: "ticketCounter\\.upsert"
    - from: "createTicket"
      to: "Contact"
      via: "findOrCreateContact by normalized email"
      pattern: "findOrCreateContact"
    - from: "createTicket initial Message"
      to: "renderMarkdown"
      via: "bodyHtml stored sanitized"
      pattern: "renderMarkdown"
---

<objective>
Build the ticket-creation domain core: a concurrency-safe per-workspace ticket number (TicketCounter upsert inside the create transaction), normalized-email contact find-or-create, SLA target lookup + due-timestamp computation, and a dedicated high-entropy public status token. This is the single `createTicket()` entrypoint reused by both the agent "New Ticket" flow and the public web form.

Purpose: AIDA-01 (create ticket + lifecycle), AIDA-03 (contact linkage), AIDA-06 (SLA timers). All ticket intake paths funnel through here so numbering/contact/SLA rules are enforced in exactly one place.
Output: createTicket + helpers + an integration test proving no duplicate numbers under concurrency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-ticketing/02-RESEARCH.md
@.planning/phases/02-core-ticketing/02-CONTEXT.md
@src/lib/scoped-db.ts
@src/lib/db.ts
@tests/integration/workspace-isolation.test.ts

<interfaces>
scopedDb signature (from src/lib/scoped-db.ts): `scopedDb(orgId: string)` returns a Prisma client that auto-injects organizationId for DOMAIN_MODELS. Plan 01 Task 3 proved (or flagged fallback for) tenant injection inside `$transaction`.
renderMarkdown (from plan 02, src/lib/markdown/render.ts): `renderMarkdown(md: string): string` — use for the initial Message bodyHtml.
Ticket required fields at create (from schema, plan 01): number, statusToken, subject, priority, contactId, firstResponseTargetMinutes, resolutionTargetMinutes, firstResponseDueAt, resolutionDueAt. Message required: ticketId, direction, visibility, bodyMarkdown, bodyHtml (+ authorContactId for inbound / authorUserId for agent).
TicketPriority enum: LOW NORMAL HIGH URGENT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: status-token + SLA helpers</name>
  <files>src/lib/tickets/status-token.ts, src/lib/tickets/sla.ts</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Open Question 1 — dedicated token; Secondary Topics — SLA data model + seeded defaults; Open Question 3 — proportional at-risk)
    - src/lib/scoped-db.ts (ScopedDb type for helper signatures)
  </read_first>
  <action>
Create `src/lib/tickets/status-token.ts`:
```ts
import { randomBytes } from "node:crypto";
export function generateStatusToken(): string {
  return randomBytes(24).toString("base64url");
}
```
Create `src/lib/tickets/sla.ts` with:
- `export const DEFAULT_SLA_TARGETS: Record<TicketPriority, { firstResponseMinutes: number; resolutionMinutes: number }>` using final numbers: URGENT 60/480, HIGH 240/1440, NORMAL 480/2880, LOW 1440/4320 (minutes; = 1h/8h, 4h/24h, 8h/48h, 24h/72h). Import `TicketPriority` from `@/generated/prisma/client`.
- `export async function getSlaTargets(db, priority: TicketPriority): Promise<{ firstResponseMinutes: number; resolutionMinutes: number }>` — reads `db.slaPolicy.findFirst({ where: { priority } })` (scopedDb auto-scopes org); if found, return its `firstResponseTargetMinutes`/`resolutionTargetMinutes`; else fall back to `DEFAULT_SLA_TARGETS[priority]`. `db` is the return type of `scopedDb` — type the param as `ReturnType<typeof scopedDb>` (import scopedDb type).
- `export function computeDueTimestamps(from: Date, firstResponseMinutes: number, resolutionMinutes: number): { firstResponseDueAt: Date; resolutionDueAt: Date }` — `new Date(from.getTime() + minutes * 60_000)` for each.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/tickets/status-token.ts` exports `generateStatusToken` and uses `randomBytes(24)` with `base64url`
    - `src/lib/tickets/sla.ts` exports `DEFAULT_SLA_TARGETS`, `getSlaTargets`, `computeDueTimestamps`
    - `DEFAULT_SLA_TARGETS` contains the value `60` (URGENT first response) and `4320` (LOW resolution)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Token + SLA helpers compile; defaults match the 1h/8h..24h/72h matrix; at-risk is proportional (computed later in the worker job, plan 05).</done>
</task>

<task type="auto">
  <name>Task 2: findOrCreateContact + createTicket transaction</name>
  <files>src/lib/contacts/find-or-create-contact.ts, src/lib/tickets/create-ticket.ts</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Topic 2 — full createTicket transaction pattern + scopedDb-in-tx gotcha)
    - .planning/phases/02-core-ticketing/02-01-SUMMARY.md (whether the tx smoke test passed or requires the explicit-orgId fallback)
    - src/lib/markdown/render.ts (renderMarkdown for the initial message)
  </read_first>
  <action>
Create `src/lib/contacts/find-or-create-contact.ts`:
- `export async function findOrCreateContact(db, input: { email: string; name?: string | null; phone?: string | null; company?: string | null }): Promise<{ id: string }>`.
- Normalize: `const email = input.email.trim().toLowerCase();`
- `const existing = await db.contact.findFirst({ where: { email } });` (scopedDb scopes org). If existing: optionally fill missing name/phone/company via `db.contact.update` only for currently-null fields; return existing. Else `db.contact.create({ data: { email, name, phone, company } })`.

Create `src/lib/tickets/create-ticket.ts` with `createTicket(orgId, input)` where input = `{ subject: string; priority: TicketPriority; body: string; contact: { email; name?; phone?; company? }; direction: "INBOUND" | "OUTBOUND"; authorUserId?: string }`.
Flow (per RESEARCH.md Topic 2), using `const db = scopedDb(orgId)` then `db.$transaction(async (tx) => { ... })`:
1. `findOrCreateContact(tx, input.contact)` → contactId. (If plan 01 SUMMARY flagged the tx fallback, pass orgId explicitly into `tx.contact.*` data/where.)
2. `const counter = await tx.ticketCounter.upsert({ where: { organizationId: orgId }, create: { lastNumber: 1 }, update: { lastNumber: { increment: 1 } } });`
3. `getSlaTargets(tx, input.priority)` → targets; `computeDueTimestamps(now, ...)`.
4. `tx.ticket.create({ data: { number: counter.lastNumber, statusToken: generateStatusToken(), subject, priority, contactId, firstResponseTargetMinutes, resolutionTargetMinutes, firstResponseDueAt, resolutionDueAt } })`.
5. `tx.message.create({ data: { ticketId: ticket.id, direction: input.direction, visibility: "PUBLIC", authorContactId: input.direction === "INBOUND" ? contactId : null, authorUserId: input.authorUserId ?? null, bodyMarkdown: input.body, bodyHtml: renderMarkdown(input.body) } })`.
Return `{ id, number, statusToken }`. Add a code comment: "single-org v1: callers resolve orgId; public intake uses organization.findFirstOrThrow (plan 11)."
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/contacts/find-or-create-contact.ts` exports `findOrCreateContact` and contains `.toLowerCase()`
    - `src/lib/tickets/create-ticket.ts` exports `createTicket` and contains `ticketCounter.upsert`, `findOrCreateContact`, and `renderMarkdown`
    - `create-ticket.ts` wraps counter + ticket + message writes in a single `$transaction`
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>createTicket links a contact by normalized email, atomically increments the per-org counter, stamps SLA due times, and stores a sanitized initial message — all in one transaction.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test — ticket-number race + creation wiring</name>
  <files>tests/integration/create-ticket.test.ts</files>
  <read_first>
    - tests/integration/workspace-isolation.test.ts (Testcontainers harness + makeOrgData)
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Validation Architecture — ticket-number race seam)
    - src/lib/tickets/create-ticket.ts (the function under test)
  </read_first>
  <action>
Create `tests/integration/create-ticket.test.ts` mirroring the Testcontainers style of `workspace-isolation.test.ts`. Seed one org via bare `prisma.organization.create`. Tests:
- "assigns sequential numbers": call `createTicket(org.id, {...})` twice sequentially; assert numbers are 1 then 2.
- "no duplicate numbers under concurrency": `await Promise.all(Array.from({length: 20}, () => createTicket(org.id, { subject, priority: "NORMAL", body: "hi", contact: { email: \`u${i}@x.com\` }, direction: "INBOUND" })))`; collect the 20 `number` values; assert `new Set(numbers).size === 20` (no duplicates) and no thrown unique-constraint error.
- "links contact by normalized email": create two tickets with emails `A@X.com` and `a@x.com`; assert both tickets share the same `contactId` (one Contact created).
  </action>
  <verify>
    <automated>pnpm test:integration -- create-ticket</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/create-ticket.test.ts` contains `Promise.all` and asserts `new Set(` size equals the concurrent count
    - The test asserts `A@X.com` and `a@x.com` resolve to the same contactId
    - `pnpm test:integration -- create-ticket` exits 0
  </acceptance_criteria>
  <done>Concurrent creation yields unique gap-tolerant numbers; case-different emails dedupe to one contact.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` clean.
- `pnpm test:integration -- create-ticket` green (sequential numbering, 20-way concurrency uniqueness, email dedup).
</verification>

<success_criteria>
There is exactly one code path that creates tickets; it is race-safe on numbering, dedupes contacts by lowercased email, computes SLA due timestamps from priority, and mints a dedicated status token — verified by integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ticketing/02-03-SUMMARY.md`.
</output>
