---
phase: 02-core-ticketing
plan: 09
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04", "02-06", "02-07", "02-08"]
files_modified:
  - src/app/(app)/tickets/[id]/page.tsx
  - src/components/tickets/ticket-meta-header.tsx
  - src/components/tickets/thread-message.tsx
  - src/components/tickets/thread-system-event.tsx
  - src/components/tickets/composer.tsx
  - src/components/tickets/composer-toggle.tsx
  - src/app/(app)/tickets/[id]/actions.ts
  - src/app/api/tickets/[id]/messages/route.ts
  - src/app/api/attachments/[id]/route.ts
  - src/app/(app)/tickets/new-ticket-dialog.tsx
  - src/app/(app)/tickets/new-ticket-action.ts
autonomous: true
requirements: [AIDA-01, AIDA-04, AIDA-05, AIDA-06, AIDA-07]
must_haves:
  truths:
    - "An agent can move a ticket to any status; the change persists and the SLA flags clear on resolve"
    - "An agent can assign a ticket to a workspace member"
    - "An agent can post a public reply OR an internal note; the two are visually unmistakable (amber+lock note)"
    - "Replies/notes are Markdown rendered to sanitized HTML and can carry file attachments (10MB, MIME-allowlisted)"
    - "The thread renders chronologically (inbound + outbound + notes) with per-message attachments"
    - "Attachments download only through an authenticated, workspace-scoped route"
    - "An agent can create a new ticket from the inbox"
    - "Changing a ticket's priority recomputes its SLA due dates and clears stale at-risk/breach flags (a downgrade no longer shows a false 'Overdue')"
  artifacts:
    - path: "src/app/api/tickets/[id]/messages/route.ts"
      provides: "POST multipart: create Message (public|internal) + attachments; set firstRespondedAt + clear SLA flags"
      contains: "formData"
    - path: "src/components/tickets/thread-message.tsx"
      provides: "ThreadMessage — inbound / public-reply / internal-note variants"
      exports: ["ThreadMessage"]
    - path: "src/app/(app)/tickets/[id]/actions.ts"
      provides: "changeStatus, assignTicket, addTag, removeTag, setCustomFieldValue"
      exports: ["changeStatus", "assignTicket"]
    - path: "src/app/api/attachments/[id]/route.ts"
      provides: "GET authenticated workspace-scoped attachment serving"
      contains: "getScopedDb"
  key_links:
    - from: "messages route"
      to: "renderMarkdown + localFileStorage + file-type"
      via: "sanitized bodyHtml + validated stored attachments"
      pattern: "renderMarkdown"
    - from: "changeStatus (RESOLVED/CLOSED)"
      to: "isAtRisk/isBreached cleared + resolvedAt set"
      via: "same-write flag clearing (Pitfall 5)"
      pattern: "isBreached: false|isAtRisk: false"
    - from: "changePriority (downgrade)"
      to: "recomputed due timestamps + isAtRisk/isBreached cleared"
      via: "same-write flag reset so a downgrade drops a now-stale at-risk/breach (Pitfall 5)"
      pattern: "isBreached: false|isAtRisk: false"
    - from: "ThreadMessage internal-note variant"
      to: "--warning amber + Lock"
      via: "bg-warning/10 border-l-warning + Lock icon"
      pattern: "Lock"
---

<objective>
Build the ticket reading pane: the meta header (status/priority/assignee dropdowns + SLA chip + tags + custom-field values), the chronological thread (`ThreadMessage` inbound/public/internal variants + system-event rows), the composer with the public-reply/internal-note toggle, the multipart message Route Handler (Markdown→sanitized HTML + attachments), the authenticated attachment-serving route, the ticket mutation Server Actions (status/assign/tag/custom-field), and the "New Ticket" flow.

Purpose: AIDA-01 (lifecycle transitions), AIDA-04 (assign + public/note visual distinction), AIDA-05 (tags apply + custom values), AIDA-06 (SLA flag clearing on response/resolve — Pitfall 5), AIDA-07 (thread + attachments). This is the core agent workflow.
Output: reading pane page + components + actions + 2 route handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-SYSTEM.md
@.planning/phases/02-core-ticketing/02-UI-SPEC.md
@.planning/phases/02-core-ticketing/02-CONTEXT.md
@.planning/phases/02-core-ticketing/02-RESEARCH.md
@src/lib/session.ts
@src/lib/markdown/render.ts
@src/lib/attachments/local-file-storage.ts
@src/lib/attachments/constants.ts
@src/lib/tickets/create-ticket.ts

<interfaces>
getScopedDb (src/lib/session.ts): `const { db, orgId, session } = await getScopedDb();` — session.user.id is the acting agent.
renderMarkdown (plan 02): `renderMarkdown(md): string` — the ONLY sanitizer; store its output as Message.bodyHtml, and it is the only value ever passed to `dangerouslySetInnerHTML`.
localFileStorage (plan 04): `localFileStorage.save({orgId, key, data: Buffer})`, `.read({orgId, key})`; `buildStorageKey(originalFilename)`; constants `MAX_BYTES`, `ALLOWED_MIME`.
createTicket (plan 03): `createTicket(orgId, { subject, priority, body, contact:{email,name?}, direction:"OUTBOUND", authorUserId })` → {id, number, statusToken}.
Chips (plan 06): StatusChip, PriorityChip, SlaDueChip, TagChip, AttachmentChip, AssigneeAvatar. CustomFieldInput (plan 07): `@/components/tickets/custom-field-input`.
Schema: Message { direction, visibility PUBLIC|INTERNAL, authorUserId?, authorContactId?, bodyMarkdown, bodyHtml, attachments[] }. Ticket { status, priority, assigneeId, firstRespondedAt, resolvedAt, isAtRisk, isBreached, firstResponseDueAt, resolutionDueAt }.
File-bearing mutations MUST be Route Handlers (Server Actions cap at 1MB) — RESEARCH.md Topic 4. Plain mutations (status/assign/tag) stay Server Actions. Route Handlers need `export const runtime = "nodejs";`. Next 16 dynamic params are async: `{ params }: { params: Promise<{ id: string }> }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reading pane page + meta header + ThreadMessage variants</name>
  <files>src/app/(app)/tickets/[id]/page.tsx, src/components/tickets/ticket-meta-header.tsx, src/components/tickets/thread-message.tsx, src/components/tickets/thread-system-event.tsx</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Screen Contracts §1 reading pane + §2 ThreadMessage/system-event exact classes)
    - src/components/tickets/custom-field-input.tsx (plan 07 — reuse for editable custom values)
    - src/components/empty-state.tsx (for invalid-ticket state)
  </read_first>
  <action>
Create `src/app/(app)/tickets/[id]/page.tsx` (async Server Component; renders inside plan-08's 2-pane layout, on the right, alongside `<TicketListPanel/>` on the left — import and render TicketListPanel too so the list stays visible): `const { db, orgId, session } = await getScopedDb(); const { id } = await params;` fetch the ticket with `db.ticket.findFirst({ where: { id }, include: { contact, assignee, tags: { include: { tag } }, messages: { include: { authorUser, authorContact, attachments }, orderBy: { createdAt: "asc" } }, customFieldValues: { include: { definition } } } })`. If not found → EmptyState. Also load workspace members (for assignee dropdown) via bare `prisma.member.findMany({ where: { organizationId: orgId }, include: { user: true } })` and custom-field definitions.
Create `src/components/tickets/ticket-meta-header.tsx` (Client): sticky `h-14 px-6 border-b bg-background/80 backdrop-blur-sm`: `#{number}` + subject (`text-[18px] font-semibold truncate`); right cluster: Status `DropdownMenu` (shows StatusChip; items = 5 statuses → calls `changeStatus`), Priority `DropdownMenu` (PriorityChip; 4 levels → `changeStatus`? no — a `changePriority` — add it to actions), Assignee `DropdownMenu` (AssigneeAvatar+name; members list + "Unassign" → `assignTicket`), `SlaDueChip`. Secondary meta row (`px-6 py-2 border-b border-border/70 flex flex-wrap gap-2`): TagChips + a `TagAutocomplete` "+ Add tag" (Popover+Command; calls addTag/removeTag) + inline custom-field values via `CustomFieldInput` (calls setCustomFieldValue on change).
Create `src/components/tickets/thread-message.tsx` — `ThreadMessage({ message, contactName, agentName })` with 3 variants per UI-SPEC §2:
- INBOUND (authorContactId set): `bg-card border border-border rounded-lg p-3`, contact name + relative time.
- OUTBOUND + PUBLIC: `bg-primary/5 border border-primary/15 rounded-lg p-3`, agent name + time.
- INTERNAL (visibility INTERNAL): `bg-warning/10 border border-warning/30 border-l-2 border-l-warning rounded-lg p-3`, `<Lock className="size-4 text-warning"/>` + `Internal Note` label (`text-[12px] font-medium uppercase tracking-wide text-warning`) + agent name + time.
Body: render `message.bodyHtml` via `dangerouslySetInnerHTML` inside a wrapper `text-[14px] leading-relaxed text-foreground` (bodyHtml is already sanitized by renderMarkdown — never re-render bodyMarkdown raw). Attachments: `AttachmentChip` row, each linking to `/api/attachments/${att.id}`.
Create `src/components/tickets/thread-system-event.tsx` — `ThreadSystemEvent({ text })`: `text-[12px] text-muted-foreground text-center py-1` (used for auto-reopen copy `Ticket reopened — new reply from {contact}.`).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `[id]/page.tsx` fetches messages `orderBy: { createdAt: "asc" }` and renders `<TicketListPanel` (left) + reading pane (right)
    - `thread-message.tsx` internal-note variant contains `bg-warning/10`, `border-l-warning`, `Lock`, and the `Internal Note` label
    - `thread-message.tsx` passes ONLY `message.bodyHtml` (not bodyMarkdown) to `dangerouslySetInnerHTML`
    - `ticket-meta-header.tsx` renders Status/Priority/Assignee dropdowns and a `SlaDueChip`
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Reading pane shows ticket meta with editable status/priority/assignee/tags/custom-fields and a chronological thread where notes are unmistakably amber+lock; attachments link to the authenticated route.</done>
</task>

<task type="auto">
  <name>Task 2: Composer + message Route Handler (multipart, attachments) + authenticated serve route</name>
  <files>src/components/tickets/composer.tsx, src/components/tickets/composer-toggle.tsx, src/app/api/tickets/[id]/messages/route.ts, src/app/api/attachments/[id]/route.ts</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (§2 Composer + ComposerToggle exact classes + CTAs `Send Reply`/`Save Internal Note`)
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Topic 4 — upload/serve Route Handlers, file-type sniffing, Pitfall 5 flag-clearing)
    - src/lib/attachments/local-file-storage.ts, constants.ts, src/lib/markdown/render.ts
  </read_first>
  <action>
Create `src/components/tickets/composer-toggle.tsx` (Client): bespoke segmented control (`inline-flex border border-border rounded-lg overflow-hidden`), two Buttons: "Public Reply" active `bg-primary/10 text-primary`; "Internal Note" active `bg-warning/10 text-warning` (NEVER primary/indigo, even hover). Emits mode `public|internal`.
Create `src/components/tickets/composer.tsx` (Client): ComposerToggle + `Textarea` (Markdown source, `min-h-[96px]`; when internal mode wrap in `bg-warning/5 border border-warning/30 rounded-lg`), a toolbar (`Paperclip` ghost icon button → native file input, client-side 10MB/allowlist check; attached files shown as removable `AttachmentChip`s), and the primary action: public → `Button` default label `Send Reply`; internal → warning-tint `Button` (`bg-warning/10 text-warning border border-warning/30 hover:bg-warning/20`) + `Lock` icon, label `Save Internal Note`. On submit, POST `multipart/form-data` to `/api/tickets/${ticketId}/messages` with fields `mode`, `body`, and `file` entries; on success `router.refresh()`; on failure show a `sonner` toast (`Couldn't send your reply. Try again.` / `Couldn't save your note. Try again.`) and preserve content.
Create `src/app/api/tickets/[id]/messages/route.ts` — `export const runtime = "nodejs";` POST handler:
1. `const { db, orgId, session } = await getScopedDb();` (401 if unauthenticated — getScopedDb redirects; in a route handler catch + return 401).
2. `const { id: ticketId } = await params;` verify `db.ticket.findFirst({ where: { id: ticketId } })` exists (org-scoped).
3. `const form = await request.formData();` read `mode` (`public|internal`), `body` (string), and all `file` entries.
4. For each file: check `file.size <= MAX_BYTES`; `const buf = Buffer.from(await file.arrayBuffer());` `const sniff = await (await import("file-type")).fileTypeFromBuffer(buf);` reject (415) if `!sniff || !ALLOWED_MIME.has(sniff.mime)`. Compute `key = buildStorageKey(file.name)`; `await localFileStorage.save({ orgId, key, data: buf })`.
5. In a `db.$transaction`: create the Message `{ ticketId, direction: "OUTBOUND", visibility: mode==="internal"?"INTERNAL":"PUBLIC", authorUserId: session.user.id, bodyMarkdown: body, bodyHtml: renderMarkdown(body) }` then create Attachment rows `{ messageId, storageKey: key, originalFilename: file.name, mimeType: sniff.mime, sizeBytes: file.size }`.
6. Pitfall 5 flag-clearing: if `mode==="public"` and the ticket's `firstRespondedAt` is null → update ticket `firstRespondedAt: now, isAtRisk: false, isBreached: false` (the 5-min job re-flags resolution risk if needed).
7. Return `Response.json({ ok: true })`.
Create `src/app/api/attachments/[id]/route.ts` — `export const runtime = "nodejs";` GET: `const { db, orgId } = await getScopedDb();` `const { id } = await params;` `const att = await db.attachment.findFirst({ where: { id } });` 404 if missing; `const buf = await localFileStorage.read({ orgId, key: att.storageKey });` return `new Response(buf, { headers: { "Content-Type": att.mimeType, "Content-Disposition": \`attachment; filename="${encodeURIComponent(att.originalFilename)}"\`, "Cache-Control": "private, no-store" } })`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `messages/route.ts` has `export const runtime = "nodejs"`, reads `request.formData()`, validates size against `MAX_BYTES` and MIME via `file-type` against `ALLOWED_MIME`, and stores via `localFileStorage.save`
    - `messages/route.ts` sets `bodyHtml: renderMarkdown(body)` and, for a first public reply, sets `firstRespondedAt` + `isAtRisk: false` + `isBreached: false`
    - `composer-toggle.tsx` internal mode never uses a `primary`/indigo class; internal CTA is `Save Internal Note` with a `Lock` icon
    - `attachments/[id]/route.ts` uses `getScopedDb()` (workspace-scoped) and returns a `Content-Disposition` attachment header
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Agents post public replies or amber internal notes with attachments; content is sanitized; first public reply clears at-risk/breach; attachments serve only through the authenticated scoped route.</done>
</task>

<task type="auto">
  <name>Task 3: Ticket mutation Server Actions + New Ticket flow</name>
  <files>src/app/(app)/tickets/[id]/actions.ts, src/app/(app)/tickets/new-ticket-dialog.tsx, src/app/(app)/tickets/new-ticket-action.ts</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Pitfall 5 — clear flags on resolve)
    - src/app/(app)/settings/actions.ts (scopedDb server-action idiom + revalidatePath)
    - src/lib/tickets/create-ticket.ts (createTicket signature)
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Copywriting Contract — `New Ticket`)
  </read_first>
  <action>
Create `src/app/(app)/tickets/[id]/actions.ts` ("use server"), each using `getScopedDb()` + `revalidatePath(\`/tickets/${ticketId}\`)`:
- `changeStatus(ticketId, status: TicketStatus)`: if status in (RESOLVED, CLOSED) → `db.ticket.update({ where: { id: ticketId }, data: { status, resolvedAt: new Date(), isAtRisk: false, isBreached: false } })`; else → `data: { status, resolvedAt: null }` (reopening resumes resolution SLA).
- `changePriority(ticketId, priority: TicketPriority)`: update priority; recompute the new priority's SLA via `getSlaTargets`+`computeDueTimestamps` from createdAt and update `firstResponseTargetMinutes`/`resolutionTargetMinutes` + `firstResponseDueAt`/`resolutionDueAt`. CRITICAL (Pitfall 5): because a priority change moves the due timestamps — a downgrade pushes them into the future — and the SLA-flag job is strictly one-directional (only ever sets flags `true`, never clears), this SAME update MUST also reset `isAtRisk: false, isBreached: false`. Otherwise a ticket flagged at-risk/breached under the old (higher) priority keeps showing "At risk"/"Overdue" indefinitely after a downgrade even though its new due date is safely in the future. Clearing here lets the 5-min job re-flag only if the ticket is still genuinely at-risk/breached under the new targets — identical to the `changeStatus` resolve and first-public-reply writes.
- `assignTicket(ticketId, assigneeId: string | null)`: update assigneeId.
- `addTag(ticketId, name)`: find-or-create Tag by name (scopedDb) then `db.ticket.update({ where:{id:ticketId}, data:{ tags:{ create:{ tagId } } } })` (nested write scopes via parent; TicketTag not in DOMAIN_MODELS). `removeTag(ticketId, tagId)`: `data: { tags: { delete: { ticketId_tagId: { ticketId, tagId } } } }`.
- `setCustomFieldValue(ticketId, definitionId, value)`: upsert the CustomFieldValue for (ticketId, definitionId) writing the correct typed column (valueText/valueNumber/valueBoolean/valueDate) based on the definition type.
Create `src/app/(app)/tickets/new-ticket-dialog.tsx` (Client): a `Dialog` "New Ticket" button (secondary CTA in the inbox) with fields Subject, Contact email + optional name, Priority (DropdownMenu), Message (Textarea) → calls the action below; on success `router.push(\`/tickets/${id}\`)`.
Create `src/app/(app)/tickets/new-ticket-action.ts` ("use server"): `createTicketAction(input)` → `const { orgId, session } = await getScopedDb(); const t = await createTicket(orgId, { ...input, direction: "OUTBOUND", authorUserId: session.user.id }); revalidatePath("/tickets"); return { id: t.id };`. Wire the `New Ticket` button into the inbox (list panel header or reading-pane header).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `[id]/actions.ts` exports `changeStatus`, `changePriority`, `assignTicket`, `addTag`, `removeTag`, `setCustomFieldValue`
    - `changeStatus` sets `isAtRisk: false` and `isBreached: false` and `resolvedAt` when moving to RESOLVED/CLOSED
    - `changePriority` recomputes the due timestamps from the new priority AND resets `isAtRisk: false, isBreached: false` in the same update (Pitfall 5 — the one-directional job never clears flags, so a downgrade must clear them or the chip shows a stale "At risk"/"Overdue")
    - `addTag` uses a nested `tags: { create: { tagId } }` write (not a direct ticketTag create)
    - `new-ticket-action.ts` calls `createTicket` with `direction: "OUTBOUND"` and the acting `authorUserId`
    - `pnpm exec tsc --noEmit` exits 0 and `pnpm run build` completes
  </acceptance_criteria>
  <done>Agents change status (with SLA flag clearing on resolve), reassign, tag, edit custom-field values, and create new tickets — all persisting and revalidating.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` clean; `pnpm run build` succeeds.
- Message posting is a nodejs Route Handler (multipart, file-type validated); status change clears SLA flags on resolve; attachments serve only via authenticated scoped route.
- Internal notes are amber+lock, never indigo.
</verification>

<success_criteria>
The full agent ticket workflow works: create → assign → move through the lifecycle → public reply vs internal note (visually distinct) → attachments, with SLA flags clearing on first response/resolve and every render path sanitized.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ticketing/02-09-SUMMARY.md`.
</output>
