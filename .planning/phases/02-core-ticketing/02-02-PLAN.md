---
phase: 02-core-ticketing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/components/ui/badge.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/command.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/skeleton.tsx
  - src/lib/markdown/render.ts
  - src/lib/markdown/render.test.ts
autonomous: true
requirements: [AIDA-04, AIDA-07]
must_haves:
  truths:
    - "Markdown from an untrusted user is rendered to sanitized HTML with script/onclick/javascript: stripped"
    - "The --warning (amber) and --success (emerald) tokens exist and Badge exposes warning/success variants"
    - "The 5 new shadcn primitives (textarea, popover, command, checkbox, skeleton) are installed"
  artifacts:
    - path: "src/lib/markdown/render.ts"
      provides: "renderMarkdown(md): string — the single sanitized Markdown->HTML pipeline"
      exports: ["renderMarkdown"]
    - path: "src/app/globals.css"
      provides: "--warning and --success tokens (light + dark) + @theme inline registration"
      contains: "--warning"
    - path: "src/components/ui/badge.tsx"
      provides: "warning + success Badge variants (tint-only, no -foreground companion)"
      contains: "warning"
  key_links:
    - from: "renderMarkdown"
      to: "rehype-sanitize defaultSchema"
      via: "unified pipeline with rehype-sanitize"
      pattern: "rehypeSanitize"
    - from: "globals.css @theme inline"
      to: "--warning / --success"
      via: "--color-warning / --color-success mapping"
      pattern: "--color-warning"
---

<objective>
Install the Phase-2 npm dependencies and shadcn primitives, add the two new semantic design tokens (`--warning`, `--success`) exactly per the UI-SPEC single-token convention, extend `Badge` with `warning`/`success` variants, and build the single shared `renderMarkdown()` sanitization pipeline (TDD — XSS is the load-bearing behavior).

Purpose: These are cross-cutting primitives every Wave-2/3 plan consumes (chips need the tokens; composer/thread need renderMarkdown; autocomplete needs popover+command). Building them once, correctly sanitized, prevents an XSS foothold on untrusted ticket text.
Output: New packages installed, 5 shadcn components added, tokens + Badge variants live, tested renderMarkdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-SYSTEM.md
@CLAUDE.md
@.planning/phases/02-core-ticketing/02-RESEARCH.md
@.planning/phases/02-core-ticketing/02-UI-SPEC.md
@src/components/ui/badge.tsx
@src/app/globals.css

<interfaces>
From src/components/ui/badge.tsx — the existing `destructive` variant is the EXACT template to mirror (tint-only, no -foreground):
```ts
destructive:
  "bg-destructive/10 text-destructive focus-visible:ring-destructive/20 dark:bg-destructive/20 dark:focus-visible:ring-destructive/40 [a]:hover:bg-destructive/20",
```
globals.css structure: `@theme inline { ... --color-destructive: var(--destructive); ... }`, then `:root { ... --destructive: oklch(0.577 0.245 27.325); ... }`, then `.dark { ... }`. Add the new tokens in all three places, mirroring `--destructive`.
Package manager: pnpm 10.34.4, Node 22 (Volta). shadcn CLI already initialized (components.json present).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install markdown/file-type packages and add 5 shadcn primitives</name>
  <files>package.json, src/components/ui/textarea.tsx, src/components/ui/popover.tsx, src/components/ui/command.tsx, src/components/ui/checkbox.tsx, src/components/ui/skeleton.tsx</files>
  <read_first>
    - package.json (confirm current deps; pnpm + type: module)
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Standard Stack table — exact versions)
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Component Inventory — the 5 new shadcn components)
  </read_first>
  <action>
Run: `pnpm add unified remark-parse remark-gfm remark-rehype rehype-sanitize rehype-stringify file-type`
(Research pins: unified@11.0.5, remark-parse@11.0.0, remark-gfm@4.0.1, remark-rehype@11.1.2, rehype-sanitize@6.0.0, rehype-stringify@10.0.1, file-type@22.0.1 — accept the latest matching minors pnpm resolves; hast-util-sanitize comes transitively as a peer of rehype-sanitize.)
Then add the shadcn primitives from the official registry only: `pnpm dlx shadcn@latest add textarea popover command checkbox skeleton` (this also pulls `cmdk` for command). Do NOT add `select`, `tabs`, `table`, `alert`, or `calendar` — UI-SPEC deliberately excludes them.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `unified`, `remark-parse`, `remark-gfm`, `remark-rehype`, `rehype-sanitize`, `rehype-stringify`, `file-type`
    - Files exist: `src/components/ui/textarea.tsx`, `src/components/ui/popover.tsx`, `src/components/ui/command.tsx`, `src/components/ui/checkbox.tsx`, `src/components/ui/skeleton.tsx`
    - `package.json` does NOT add a `select.tsx`/`tabs.tsx` dependency footprint beyond the 5 listed
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>7 markdown/file-type packages + 5 shadcn primitives installed; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add --warning / --success tokens and Badge variants</name>
  <files>src/app/globals.css, src/components/ui/badge.tsx</files>
  <read_first>
    - src/app/globals.css (@theme inline block, :root block, .dark block — mirror --destructive placement)
    - src/components/ui/badge.tsx (destructive variant to mirror)
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (New Design Tokens to Add — exact oklch values + variant strings)
  </read_first>
  <action>
In `src/app/globals.css`:
- In the `@theme inline` block, add alongside `--color-destructive`: `--color-warning: var(--warning);` and `--color-success: var(--success);`
- In `:root`, add alongside `--destructive`: `--warning: oklch(0.6 0.16 70);` and `--success: oklch(0.58 0.14 155);`
- In `.dark`, add alongside `--destructive`: `--warning: oklch(0.75 0.16 75);` and `--success: oklch(0.72 0.15 155);`

In `src/components/ui/badge.tsx`, add two variants to `badgeVariants` mirroring `destructive` exactly (tint-only, NO `-foreground` companion):
```ts
warning: "bg-warning/10 text-warning focus-visible:ring-warning/20 dark:bg-warning/20 dark:focus-visible:ring-warning/40 [a]:hover:bg-warning/20",
success: "bg-success/10 text-success focus-visible:ring-success/20 dark:bg-success/20 dark:focus-visible:ring-success/40 [a]:hover:bg-success/20",
```
Do NOT declare `--warning-foreground` or `--success-foreground` anywhere.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/app/globals.css','utf8'); if(!/--warning/.test(c)||!/--success/.test(c)||/--warning-foreground/.test(c)){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains `--color-warning: var(--warning);` and `--color-success: var(--success);`
    - `src/app/globals.css` contains `--warning: oklch(` in BOTH `:root` and `.dark` (two occurrences of `--warning:` minimum for light+dark, likewise `--success:`)
    - `src/app/globals.css` does NOT contain `--warning-foreground` or `--success-foreground`
    - `src/components/ui/badge.tsx` contains `warning:` and `success:` variant keys with `bg-warning/10 text-warning` and `bg-success/10 text-success`
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both semantic tokens live in light+dark, mapped in @theme inline, Badge exposes warning/success tint variants, no -foreground token added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build and test renderMarkdown() sanitized pipeline</name>
  <files>src/lib/markdown/render.ts, tests/unit/markdown-render.test.ts</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Topic 3 — exact render function + schema customization)
    - CLAUDE.md (untrusted-input rule) and docs/SECURITY.md (prompt-injection / untrusted text)
  </read_first>
  <behavior>
    - Test 1: `renderMarkdown("**bold**")` contains `<strong>bold</strong>`.
    - Test 2: `renderMarkdown("<script>alert(1)</script>")` does NOT contain `<script>`.
    - Test 3: `renderMarkdown("[x](javascript:alert(1))")` output does NOT contain `javascript:`.
    - Test 4: `renderMarkdown("<img src=x onerror=alert(1)>")` does NOT contain `onerror`.
    - Test 5: `renderMarkdown("[link](https://a.com)")` contains `rel="nofollow noopener noreferrer"` and `target="_blank"`.
    - Test 6 (GFM): `renderMarkdown("~~gone~~")` contains `<del>`.
  </behavior>
  <action>
Create `src/lib/markdown/render.ts` exactly per RESEARCH.md Topic 3: `unified().use(remarkParse).use(remarkGfm).use(remarkRehype).use(rehypeSanitize, schema).use(rehypeStringify)`, where `schema` spreads `defaultSchema` (imported from `rehype-sanitize`) and extends `attributes.a` with `["target","_blank"]` and `["rel","nofollow noopener noreferrer"]` (import `type { Schema } from "hast-util-sanitize"`). Export `renderMarkdown(markdown: string): string` using `String(processor.processSync(markdown))`.
Create `src/lib/markdown/render.test.ts` (unit test, lives under tests? — place next to source and ensure vitest.config `include` covers it OR put it in tests/unit). Since vitest.config includes only `tests/unit/**`, create the test at `tests/unit/markdown-render.test.ts` importing from `@/lib/markdown/render`. Write the 6 behavior assertions above FIRST (they must fail before render.ts is complete), then implement.
  </action>
  <verify>
    <automated>pnpm test -- markdown-render</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/markdown/render.ts` exists and exports `renderMarkdown`
    - `src/lib/markdown/render.ts` contains `rehypeSanitize` and `defaultSchema`
    - `tests/unit/markdown-render.test.ts` contains an assertion that a `<script>` input does not appear in output
    - `pnpm test -- markdown-render` exits 0 (all 6 tests green)
  </acceptance_criteria>
  <done>renderMarkdown sanitizes untrusted markdown (script/onclick/javascript: stripped), supports GFM, adds safe link rels; all 6 tests green.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` clean.
- `pnpm test -- markdown-render` green (6 XSS/GFM assertions).
- globals.css has warning/success in :root + .dark + @theme inline; no -foreground companions.
- Badge exposes warning + success variants.
</verification>

<success_criteria>
Untrusted markdown is centrally sanitized before any thread render; the amber/emerald semantic tokens and Badge variants exist for every chip; the 5 required shadcn primitives are present with no extra dependency creep.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ticketing/02-02-SUMMARY.md`.
</output>
