---
phase: 02-core-ticketing
plan: 07
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lib/authz.ts
  - src/app/(app)/settings/layout.tsx
  - src/app/(app)/settings/settings-nav.tsx
  - src/app/(app)/settings/sla/page.tsx
  - src/app/(app)/settings/sla/sla-form.tsx
  - src/app/(app)/settings/sla/actions.ts
  - src/app/(app)/settings/tags/page.tsx
  - src/app/(app)/settings/tags/tag-manager.tsx
  - src/app/(app)/settings/tags/actions.ts
  - src/app/(app)/settings/custom-fields/page.tsx
  - src/app/(app)/settings/custom-fields/custom-field-manager.tsx
  - src/app/(app)/settings/custom-fields/actions.ts
  - src/components/tickets/custom-field-input.tsx
  - src/components/ui/dialog.tsx
autonomous: true
requirements: [AIDA-05, AIDA-06, AIDA-12]
must_haves:
  truths:
    - "An admin can set per-priority SLA first-response and resolution targets; they persist and drive future tickets"
    - "An admin can rename and delete tags globally (delete removes from all tickets, confirmed via dialog)"
    - "An admin can define custom fields of 5 types (Text/Select/Number/Checkbox/Date)"
    - "Non-admins cannot mutate settings (server-side authorization, not just hidden UI)"
  artifacts:
    - path: "src/app/(app)/settings/sla/actions.ts"
      provides: "saveSlaTargets server action (admin-gated upsert per priority)"
      exports: ["saveSlaTargets"]
    - path: "src/app/(app)/settings/custom-fields/actions.ts"
      provides: "create/update/delete custom field definition actions"
    - path: "src/components/tickets/custom-field-input.tsx"
      provides: "CustomFieldInput type-dispatching component (consumed by reading pane, plan 09)"
      exports: ["CustomFieldInput"]
    - path: "src/lib/authz.ts"
      provides: "requireOrgAdmin() server-side role gate"
      exports: ["requireOrgAdmin", "getOrgRole"]
  key_links:
    - from: "settings actions"
      to: "requireOrgAdmin"
      via: "every mutating settings action calls it first"
      pattern: "requireOrgAdmin"
    - from: "settings/layout.tsx"
      to: "SettingsNav pill tabs"
      via: "AI Features | SLA Policies | Tags | Custom Fields"
      pattern: "Custom Fields"
---

<objective>
Extend `/settings` with three admin surfaces (AIDA-12 partial): per-priority SLA policies, tag management (rename/delete), and custom-field definitions (5 types) — behind a server-side admin authorization gate — plus the type-dispatching `CustomFieldInput` component consumed later by the ticket reading pane. Add a pill-tab settings sub-nav.

Purpose: AIDA-05 (custom fields + tags), AIDA-06 (SLA targets are admin-configured), AIDA-12 (settings area). SECURITY.md requires server-side authz on every mutating route — enforced via `requireOrgAdmin()`.
Output: settings sub-nav + 3 admin sections + actions + authz helper + CustomFieldInput.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-SYSTEM.md
@.planning/phases/02-core-ticketing/02-UI-SPEC.md
@.planning/phases/02-core-ticketing/02-CONTEXT.md
@src/app/(app)/settings/page.tsx
@src/app/(app)/settings/actions.ts
@src/lib/session.ts
@src/lib/db.ts
@src/components/ui/badge.tsx

<interfaces>
Existing settings pattern (settings/page.tsx): `const { db } = await getScopedDb();` then reads/renders a Client child; mutations are Server Actions in `actions.ts` using `getScopedDb()` + `revalidatePath("/settings")`. The scopedDb findFirst+conditional-create/update idiom (see actions.ts) avoids the compound-key upsert bug.
Better Auth roles: `member.role` in {owner, admin, member}. `session.user.id` + `session.session.activeOrganizationId`.
Schema (plan 01): SlaPolicy @@unique([organizationId, priority]); Tag @@unique([organizationId, name]); CustomFieldDefinition { label, type: CustomFieldType, options Json?, position }. Enum CustomFieldType: TEXT SELECT NUMBER CHECKBOX DATE. TicketPriority: LOW NORMAL HIGH URGENT.
DEFAULT_SLA_TARGETS (plan 03, src/lib/tickets/sla.ts) — reuse for seeded placeholder values: URGENT 60/480, HIGH 240/1440, NORMAL 480/2880, LOW 1440/4320 minutes.
shadcn: DropdownMenu (+DropdownMenuRadioGroup) reused for single-selects (no Select primitive). Checkbox available (plan 02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: authz helper + settings sub-nav layout + SLA policies</name>
  <files>src/lib/authz.ts, src/components/ui/dialog.tsx, src/app/(app)/settings/settings-nav.tsx, src/app/(app)/settings/layout.tsx, src/app/(app)/settings/sla/page.tsx, src/app/(app)/settings/sla/sla-form.tsx, src/app/(app)/settings/sla/actions.ts</files>
  <read_first>
    - src/app/(app)/settings/page.tsx and actions.ts (existing settings + scopedDb mutation idiom)
    - src/lib/session.ts (getScopedDb, requireSession)
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Settings Surfaces §4 — SLA Policies)
    - src/components/sidebar.tsx (active pill styling reference: bg-primary/10 text-primary)
  </read_first>
  <action>
Create `src/lib/authz.ts` with:
```ts
import { prisma } from "@/lib/db";
import { requireSession } from "@/lib/session";
export async function getOrgRole(): Promise<string | null> {
  const s = await requireSession();
  const orgId = s.session.activeOrganizationId;
  if (!orgId) return null;
  const m = await prisma.member.findFirst({ where: { userId: s.user.id, organizationId: orgId } });
  return m?.role ?? null;
}
export async function requireOrgAdmin(): Promise<string> {
  const role = await getOrgRole();
  if (role !== "owner" && role !== "admin") throw new Error("Forbidden: admin role required");
  return role;
}
```
Add the shadcn dialog primitive: `pnpm dlx shadcn@latest add dialog` (creates `src/components/ui/dialog.tsx` — needed by tags/custom-fields delete + add flows; the UI-SPEC copy references Dialog confirmations).

Create `src/app/(app)/settings/settings-nav.tsx` (Client, `usePathname`): pill-tab `Link`s for `AI Features`(/settings) | `SLA Policies`(/settings/sla) | `Tags`(/settings/tags) | `Custom Fields`(/settings/custom-fields); active = `bg-primary/10 text-primary`, inactive = `text-muted-foreground hover:bg-muted`, `h-8 px-3 rounded-full text-[13px] font-medium`. Match /settings active only on exact path (not startsWith) so sub-routes don't also light up "AI Features".
Create `src/app/(app)/settings/layout.tsx`: renders `<SettingsNav/>` above `{children}` in a `space-y-6` container.

Create `src/app/(app)/settings/sla/page.tsx` (Server Component): `const { db } = await getScopedDb();` read `db.slaPolicy.findMany()`; build a map priority→policy; pass to `<SlaForm/>` with seeded defaults from DEFAULT_SLA_TARGETS where a policy is missing. Render 4 rows (URGENT, HIGH, NORMAL, LOW) each: `<PriorityChip/>` + two number inputs (First Response hours, Resolution hours) — store minutes but display hours (divide by 60 for display, multiply on save). One form-level `Save SLA Targets` button.
Create `src/app/(app)/settings/sla/sla-form.tsx` (Client) using react-hook-form + the existing `Form`/`Input` components.
Create `src/app/(app)/settings/sla/actions.ts`: `saveSlaTargets(input: { priority; firstResponseHours; resolutionHours }[])` — `await requireOrgAdmin();` then `const { db, orgId } = await getScopedDb();` for each priority do findFirst-by-priority + conditional update/create (minutes = hours*60), `revalidatePath("/settings/sla")`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/authz.ts` exports `requireOrgAdmin` and `getOrgRole`; requireOrgAdmin throws unless role is owner/admin
    - `src/app/(app)/settings/settings-nav.tsx` contains all four labels including `Custom Fields`
    - `src/components/ui/dialog.tsx` exists
    - `src/app/(app)/settings/sla/actions.ts` calls `requireOrgAdmin()` before any write and contains `Save SLA Targets` copy in the form
    - SLA form renders 4 priority rows and converts hours↔minutes (contains `* 60` or `/ 60`)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Settings sub-nav live; admins set per-priority SLA targets (persisted as minutes); saves are admin-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Tag management (rename / delete with confirmation)</name>
  <files>src/app/(app)/settings/tags/page.tsx, src/app/(app)/settings/tags/tag-manager.tsx, src/app/(app)/settings/tags/actions.ts</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Settings Surfaces §4 — Tags; Copywriting Contract — delete tag dialog)
    - src/app/(app)/settings/actions.ts (scopedDb mutation idiom)
    - src/components/ui/dialog.tsx (added in Task 1)
  </read_first>
  <action>
Create `src/app/(app)/settings/tags/page.tsx` (Server Component): read `db.tag.findMany({ orderBy: { name: "asc" } })` and, for each, a ticket count via `db.ticketTag`? TicketTag is not in DOMAIN_MODELS (scoped via parent). Compute counts with `db.ticket.count` is not per-tag; instead use bare grouping: `prisma.ticketTag.groupBy({ by: ["tagId"], _count: true, where: { tag: { organizationId: orgId } } })` (bare prisma — TicketTag has no organizationId; filter via relation). Pass tags + counts to `<TagManager/>`. Empty state copy: `No tags yet — tags created from the ticket composer will appear here for management.`
Create `src/app/(app)/settings/tags/tag-manager.tsx` (Client): flat `divide-y divide-border` rows: `<TagChip/>` + count (`text-[12px] text-muted-foreground`) + inline rename `Input` (appears on click of a Pencil `Button variant="ghost" size="icon-sm"`) + delete `Button variant="ghost" size="icon-sm"` (`Trash2`, `aria-label="Delete tag {name}"`). Delete opens a `Dialog`: `Delete tag "{tag}"? This removes it from all tickets. This can't be undone.` confirm button `Delete tag` (destructive).
Create `src/app/(app)/settings/tags/actions.ts`: `renameTag(id, name)` and `deleteTag(id)` — each `await requireOrgAdmin();` then `getScopedDb()` and `db.tag.update`/`db.tag.delete` (scopedDb scopes org; delete cascades TicketTag via schema onDelete: Cascade). `revalidatePath("/settings/tags")`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/settings/tags/actions.ts` exports `renameTag` and `deleteTag`, both calling `requireOrgAdmin()` first
    - `tag-manager.tsx` contains the exact delete-confirm copy `This removes it from all tickets. This can't be undone.` and `aria-label="Delete tag`
    - Tag page renders the empty-state copy `No tags yet` when there are no tags
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Admins rename/delete tags globally with a destructive confirmation dialog; deletes cascade off all tickets.</done>
</task>

<task type="auto">
  <name>Task 3: Custom field definitions + CustomFieldInput component</name>
  <files>src/app/(app)/settings/custom-fields/page.tsx, src/app/(app)/settings/custom-fields/custom-field-manager.tsx, src/app/(app)/settings/custom-fields/actions.ts, src/components/tickets/custom-field-input.tsx</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Settings Surfaces §4 — Custom Fields; Custom-field types; Date = native input; Copywriting Contract — Create Field, delete field dialog)
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Secondary — CustomFieldDefinition/Value EAV)
    - src/components/ui/dropdown-menu.tsx, src/components/ui/checkbox.tsx, src/components/ui/input.tsx
  </read_first>
  <action>
Create `src/app/(app)/settings/custom-fields/page.tsx` (Server): read `db.customFieldDefinition.findMany({ orderBy: { position: "asc" } })`; render flat list rows: label + `<Badge variant="outline">{type}</Badge>` + edit (`Pencil` icon-sm, `aria-label="Edit field {label}"`) + delete (`Trash2` icon-sm, `aria-label="Delete field {label}"`). "Add Field" `Button` opens a `Dialog` form. Empty copy: `No custom fields yet. Add one to start capturing extra ticket details.`
Create `src/app/(app)/settings/custom-fields/custom-field-manager.tsx` (Client): the Add/Edit `Dialog` form — Label `Input`, Type single-select via `DropdownMenu`+`DropdownMenuRadioGroup` over the 5 types, Options repeatable `Input` list shown ONLY when type = SELECT (stored as `options: string[]` JSON), primary action `Create Field`. Delete opens `Dialog`: `Delete field "{label}"? Existing values on tickets will be permanently removed. This can't be undone.` confirm `Delete field` (destructive).
Create `src/app/(app)/settings/custom-fields/actions.ts`: `createCustomField`, `updateCustomField`, `deleteCustomField` — each `requireOrgAdmin()` + `getScopedDb()`; SELECT validates `options` non-empty; `revalidatePath("/settings/custom-fields")`.
Create `src/components/tickets/custom-field-input.tsx` — `CustomFieldInput({ definition, value, onChange }: { definition: { id; label; type; options?: string[] }; value: {...}; onChange: (v) => void })` dispatching by `type`:
- TEXT → `<Input/>`; NUMBER → `<Input type="number"/>`; CHECKBOX → `<Checkbox/>`; DATE → `<Input type="date"/>` (native, no calendar dep per UI-SPEC Assumption 5); SELECT → `<DropdownMenu>`+`DropdownMenuRadioGroup` over `definition.options`.
This component is EXPORTED for reuse in the reading pane (plan 09) — do not couple it to any page here.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/settings/custom-fields/actions.ts` exports `createCustomField`, `updateCustomField`, `deleteCustomField`, all calling `requireOrgAdmin()` first
    - `custom-field-manager.tsx` contains `Create Field` and the delete copy `Existing values on tickets will be permanently removed.`
    - `src/components/tickets/custom-field-input.tsx` exports `CustomFieldInput` and handles all 5 types (contains `type="date"` and `type="number"` and `Checkbox`)
    - `custom-field-input.tsx` does NOT import a `calendar` component
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Admins define/edit/delete the 5 custom-field types; CustomFieldInput dispatches by type and is reusable in the ticket reading pane.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` clean.
- Every settings mutating action calls `requireOrgAdmin()` (grep: 3 action files each contain `requireOrgAdmin`).
- Settings sub-nav shows 4 sections; SLA saved as minutes; tag/field deletes confirmed via Dialog.
</verification>

<success_criteria>
The admin can configure SLA targets, manage tags, and define custom fields; all mutations are server-side admin-gated; CustomFieldInput is ready for the ticket detail (plan 09) — satisfying AIDA-05, AIDA-06 (config), AIDA-12 (partial).
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ticketing/02-07-SUMMARY.md`.
</output>
