---
phase: 02-core-ticketing
plan: 11
type: execute
wave: 3
depends_on: ["02-03", "02-04", "02-05"]
files_modified:
  - src/app/(public)/layout.tsx
  - src/components/public/public-page-shell.tsx
  - src/components/public/honeypot-field.tsx
  - src/app/(public)/request/page.tsx
  - src/app/(public)/request/request-form.tsx
  - src/app/api/public/intake/route.ts
  - src/middleware.ts
  - docker-compose.yml
  - Caddyfile
  - .env.example
autonomous: true
requirements: [AIDA-08]
must_haves:
  truths:
    - "An unauthenticated public web form creates a ticket (contact auto-linked) and returns a status link"
    - "The public form is protected by a hidden honeypot (silent success) and per-IP rate limiting — no third-party CAPTCHA / no external calls"
    - "Public intake and status routes are exempt from the auth middleware guard"
    - "Attachments uploaded on the form are validated (10MB + MIME allowlist) server-side and stored on the uploads volume"
  artifacts:
    - path: "src/app/api/public/intake/route.ts"
      provides: "POST public intake: honeypot + rate-limit + createTicket + attachments"
      contains: "findFirstOrThrow"
    - path: "src/app/(public)/request/page.tsx"
      provides: "Public intake form page + success state"
      contains: "PublicPageShell"
    - path: "src/middleware.ts"
      provides: "PUBLIC_PREFIXES extended with /request, /status, /api/public"
      contains: "/api/public"
  key_links:
    - from: "intake route"
      to: "createTicket + checkRateLimit + localFileStorage"
      via: "single multipart POST"
      pattern: "createTicket"
    - from: "docker-compose.yml app service"
      to: "uploads_data:/data/uploads"
      via: "named volume"
      pattern: "uploads_data"
---

<objective>
Build the public web intake channel (AIDA-08): a `(public)` route group with a branded `PublicPageShell`, the `/request` form (Name/Email/Subject/Message/Attachments), and the `POST /api/public/intake` Route Handler that enforces a honeypot (silent success) + per-IP rate limiting, resolves the single-org, calls `createTicket`, stores validated attachments, and returns the tokenized status link. Wire the middleware exemptions, the `uploads_data` Docker volume, and the Caddy body-size ceiling.

Purpose: AIDA-08 — a public form that creates a ticket and returns a status link, fully self-contained (no CAPTCHA/no egress per privacy-first). This is the v1 stand-in for email intake.
Output: public shell + form + intake route + infra wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-SYSTEM.md
@.planning/phases/02-core-ticketing/02-UI-SPEC.md
@.planning/phases/02-core-ticketing/02-RESEARCH.md
@docs/SECURITY.md
@src/app/(auth)/layout.tsx
@src/components/sidebar.tsx
@src/middleware.ts
@src/lib/tickets/create-ticket.ts
@src/lib/rate-limit/check-rate-limit.ts
@src/lib/attachments/local-file-storage.ts
@src/lib/db.ts

<interfaces>
createTicket (plan 03): `createTicket(orgId, { subject, priority, body, contact:{email,name?}, direction:"INBOUND" })` → `{ id, number, statusToken }`. It creates the initial inbound Message internally.
checkRateLimit (plan 05): `checkRateLimit(scope: string, ip: string): Promise<boolean>` — false = rejected.
Attachments (plan 04): `localFileStorage.save({orgId,key,data})`, `buildStorageKey(name)`, constants `MAX_BYTES`, `ALLOWED_MIME`, `MAX_TOTAL_REQUEST_BYTES`. `file-type` for byte sniffing.
Single-org resolution (RESEARCH Open Q2): `const org = await prisma.organization.findFirstOrThrow();` (bare prisma).
middleware (src/middleware.ts): `PUBLIC_PREFIXES = ["/login","/setup","/api/auth","/api/health"]`.
(auth)/layout.tsx is the DECORATION reference (dotted grid + primary glow) — the (public) layout mirrors it but the brand-box markup reuses sidebar.tsx's brand box verbatim (gap-2.5, size-7, Sparkles, text-[15px]). Per DESIGN-SYSTEM §4.4 layout wraps, page renders Card only.
Route Handlers accepting files MUST be `export const runtime = "nodejs";`. Client IP: `request.headers.get("x-forwarded-for")?.split(",")[0] ?? "unknown"` (Caddy sets X-Forwarded-* per Caddyfile).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: (public) route group layout + PublicPageShell + HoneypotField + middleware exemptions</name>
  <files>src/app/(public)/layout.tsx, src/components/public/public-page-shell.tsx, src/components/public/honeypot-field.tsx, src/middleware.ts</files>
  <read_first>
    - src/app/(auth)/layout.tsx (decoration to mirror) and src/components/sidebar.tsx (brand-box markup to reuse verbatim)
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Screen Contract §5 — PublicPageShell markup; Honeypot spec)
    - src/middleware.ts (PUBLIC_PREFIXES)
  </read_first>
  <action>
Create `src/app/(public)/layout.tsx`: a full-screen decorative wrapper mirroring `(auth)/layout.tsx` (dotted-grid `radial-gradient(var(--border) 1px, transparent 1px)` 24×24 + `bg-primary/15 blur-[120px]` glow), NO `requireSession`, `bg-background`, centered content. It renders `{children}` only (pages render their own PublicPageShell+Card).
Create `src/components/public/public-page-shell.tsx` — `PublicPageShell({ children, maxWidth = 640 })`: the brand mark block reusing sidebar.tsx's brand box verbatim (`flex items-center justify-center gap-2.5`, `size-7 rounded-md bg-sidebar-primary`, `<Sparkles className="size-4 text-sidebar-primary-foreground"/>`, `text-[15px] font-semibold tracking-tight` "AIDA"), then a `<Card className="shadow-xl shadow-primary/5">` wrapper at `max-w-[{maxWidth}px]`. Accept a `maxWidth` prop so plan 12 can reuse it at 720px.
Create `src/components/public/honeypot-field.tsx` — `HoneypotField()`: a `<label>` wrapping a text `<input name="company_website" tabIndex={-1} autoComplete="off" />` styled `absolute left-[-9999px] top-auto h-px w-px overflow-hidden` (NOT `type="hidden"`), with equally-hidden label text.
Edit `src/middleware.ts`: extend `PUBLIC_PREFIXES` to `["/login","/setup","/api/auth","/api/health","/request","/status","/api/public"]`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/middleware.ts` PUBLIC_PREFIXES contains `/request`, `/status`, and `/api/public`
    - `public-page-shell.tsx` exports `PublicPageShell`, accepts a `maxWidth` prop, and reuses the `Sparkles` + `bg-sidebar-primary` brand box
    - `honeypot-field.tsx` input is named `company_website`, has `tabIndex={-1}`, and is NOT `type="hidden"` (uses `left-[-9999px]`)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Public route group renders on-brand without auth; honeypot is a visually-hidden trap; middleware exempts public paths.</done>
</task>

<task type="auto">
  <name>Task 2: /request intake form page + success state</name>
  <files>src/app/(public)/request/page.tsx, src/app/(public)/request/request-form.tsx</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Screen Contract §5 — form fields, success confirmation, rate-limited + honeypot states, copy)
    - src/components/ui/form.tsx, input.tsx, textarea.tsx (react-hook-form + zod)
    - src/components/public/public-page-shell.tsx, honeypot-field.tsx (Task 1)
  </read_first>
  <action>
Create `src/app/(public)/request/page.tsx`: `export const dynamic = "force-dynamic";` render `<PublicPageShell maxWidth={640}><RequestForm/></PublicPageShell>`.
Create `src/app/(public)/request/request-form.tsx` (Client, react-hook-form + zod): fields Name (`Input`), Email (`Input type="email"`, zod email), Subject (`Input`), Message (`Textarea min-h-[120px]`), Attachments (drag-or-click zone `border-2 border-dashed border-border rounded-lg p-4 text-center text-[13px] text-muted-foreground`, client-side 10MB/allowlist pre-check), plus `<HoneypotField/>`. No priority/category picker (D-19). Primary CTA `Submit Request`. On submit, POST `multipart/form-data` to `/api/public/intake`.
Success state (replaces the form on 200, not a redirect): heading `Request received` (`text-[18px] font-semibold`), body `We've sent a confirmation with a link to track your request.`, and a prominent `View status` `Button` linking to `/status/{token}` (token returned by the API). Field errors inline via `FormMessage` (`text-[12px] text-destructive`). Rate-limited (HTTP 429): form-level banner `You've submitted a few requests recently. Please wait a bit before trying again.` (`text-[13px] text-destructive`). Honeypot: API returns a normal success (no special client handling — silent success).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `request/page.tsx` has `export const dynamic = "force-dynamic"` and renders `PublicPageShell`
    - `request-form.tsx` renders Name/Email/Subject/Message/Attachments + `HoneypotField`, CTA copy `Submit Request`, and NO priority/category field
    - Success state shows `Request received` + a `View status` link to `/status/`
    - Rate-limited banner copy is exactly `You've submitted a few requests recently. Please wait a bit before trying again.`
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The public form collects intake fields, honeypot-protected, and on success shows the tracked status link.</done>
</task>

<task type="auto">
  <name>Task 3: POST /api/public/intake route + uploads volume + Caddy + env</name>
  <files>src/app/api/public/intake/route.ts, docker-compose.yml, Caddyfile, .env.example</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-RESEARCH.md (Topic 4 — public intake multipart + total-size cap; Secondary — honeypot silent-drop; Open Q2 findFirstOrThrow)
    - docker-compose.yml (app service + volumes block) and Caddyfile (site block)
    - .env.example (add UPLOADS_DIR + RATE_LIMIT_PEPPER)
  </read_first>
  <action>
Create `src/app/api/public/intake/route.ts` — `export const runtime = "nodejs";` POST:
1. `const contentLength = Number(request.headers.get("content-length") ?? 0);` if `> MAX_TOTAL_REQUEST_BYTES` → 413.
2. `const form = await request.formData();`
3. Honeypot: `if ((form.get("company_website") ?? "") !== "")` → return `Response.json({ ok: true, token: null }, { status: 200 })` and DO NOT create anything (silent success).
4. Rate-limit: `const ip = request.headers.get("x-forwarded-for")?.split(",")[0]?.trim() ?? "unknown"; if (!(await checkRateLimit("public-intake", ip))) return Response.json({ error: "rate_limited" }, { status: 429 });`
5. zod-validate name/email/subject/message. On failure → 400 with field errors.
6. `const org = await prisma.organization.findFirstOrThrow();` (bare prisma — single-org v1; add a code comment referencing RESEARCH Open Q2).
7. Validate + store each `file` entry: size ≤ MAX_BYTES, `file-type` sniff ∈ ALLOWED_MIME else 415; `key = buildStorageKey(file.name)`; `localFileStorage.save({ orgId: org.id, key, data: Buffer.from(await file.arrayBuffer()) })`. Enforce combined total ≤ MAX_TOTAL_REQUEST_BYTES.
8. `const t = await createTicket(org.id, { subject, priority: "NORMAL", body: message, contact: { email, name }, direction: "INBOUND" });` (NORMAL default — customers never set priority, D-05).
9. Link attachments to the ticket's initial inbound message: `const msg = await prisma.message.findFirst({ where: { ticketId: t.id }, orderBy: { createdAt: "asc" } });` then `prisma.attachment.create` for each stored file `{ organizationId: org.id, messageId: msg.id, storageKey, originalFilename: file.name, mimeType: sniff.mime, sizeBytes: file.size }`.
10. Return `Response.json({ ok: true, token: t.statusToken }, { status: 200 })`.
Edit `docker-compose.yml`: add `uploads_data:/data/uploads` to the `app` service `volumes:` (add a `volumes:` key to app) and declare `uploads_data:` under the top-level `volumes:` block (alongside postgres_data). Add `UPLOADS_DIR: /data/uploads` and `RATE_LIMIT_PEPPER: ${RATE_LIMIT_PEPPER:-}` to the `app` service environment. (Worker does NOT need the volume.)
Edit `Caddyfile`: inside the site block add `request_body { max_size 12MB }`.
Edit `.env.example`: add a section with `UPLOADS_DIR=/data/uploads` and `# Generate with: openssl rand -base64 32` + `RATE_LIMIT_PEPPER=replace-me-with-a-random-pepper`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `intake/route.ts` has `runtime = "nodejs"`, silently succeeds on non-empty `company_website`, calls `checkRateLimit("public-intake"`, resolves org via `findFirstOrThrow`, and calls `createTicket(... direction: "INBOUND")`
    - `intake/route.ts` validates each file against `MAX_BYTES` + `ALLOWED_MIME` via `file-type` and stores via `localFileStorage.save`
    - `docker-compose.yml` app service mounts `uploads_data:/data/uploads` and the top-level `volumes:` declares `uploads_data`
    - `Caddyfile` contains `request_body` with `max_size 12MB`
    - `.env.example` contains `UPLOADS_DIR=/data/uploads` and `RATE_LIMIT_PEPPER=`
    - `pnpm exec tsc --noEmit` exits 0 and `pnpm run build` completes
  </acceptance_criteria>
  <done>The public form creates a ticket + contact, stores validated attachments on the uploads volume, throttles abuse, silently drops bots, and returns the status token.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` clean; `pnpm run build` succeeds.
- intake route: honeypot silent-drop, per-IP rate-limit, single-org resolution, createTicket(INBOUND), server-side attachment validation.
- middleware exempts /request, /status, /api/public; uploads_data volume + Caddy 12MB cap wired.
</verification>

<success_criteria>
A stranger can submit the public form, a ticket + contact are created, attachments are validated and stored, spam is throttled without any third-party service, and the submitter receives a working status link — AIDA-08 (intake half) satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ticketing/02-11-SUMMARY.md`.
</output>
