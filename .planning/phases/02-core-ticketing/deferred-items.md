# Deferred Items — Phase 02 (core-ticketing)

Out-of-scope discoveries logged during plan execution. Not fixed per SCOPE BOUNDARY rule (pre-existing, not caused by the current plan's changes).

## From 02-05 (SLA-flag job, rate limiter, worker wiring)

- **CRLF vs LF formatter mismatch (repo-wide, environment artifact).** `biome check` flags every file in the working tree as needing LF reformatting (e.g. `src/lib/db.ts`, `src/lib/worker/jobs/heartbeat.ts` — both untouched by this plan — show the identical CRLF diff). Root cause: `core.autocrlf=true` in the local git config checks files out with CRLF on Windows while the repo stores LF. Not introduced by 02-05; affects the whole tree equally. Do not fix by mass-rewriting line endings (destructive, out of scope, and requires a git-config change this agent is not permitted to make). Revisit at a repo-hygiene pass (e.g. `.gitattributes` with `* text=auto eol=lf`).
- **Import order in `src/lib/worker/index.ts` (pre-existing, from Phase 1).** `assist/source/organizeImports` wants `import type { Job }` sorted before `import { PgBoss }`. Confirmed pre-existing by diffing against the commit prior to this plan's Task 3 edit — the violation was already present in the original heartbeat-only file. Left as-is; low-risk one-line fix, safe to pick up in a future lint-cleanup plan or the next time this file is touched substantively.
- **`gsd-tools requirements mark-complete AIDA-06 AIDA-08` returns `not_found` (tooling/format mismatch, project-wide, pre-existing).** `.planning/REQUIREMENTS.md` lists requirements as plain `- **AIDA-06** ...` bullets with a prose Traceability section — no `- [ ] **ID**` checkboxes and no `| ID | Phase | Status |` table exist for the tool's regex to match. This predates this plan (established in Phase 1's REQUIREMENTS.md) and is out of scope to restructure here. Also structurally correct to skip in this case: AIDA-06 (SLA visible indicator) and AIDA-08 (public web intake) are each only partially delivered by 02-05 (worker-side computation / rate limiting) — the UI chip (plan 06) and the public intake route (plan 08/11) still need to land before either requirement is genuinely "complete."
- **`gsd-tools state update-progress` / `add-decision` no-op on this project's `STATE.md`.** See the corresponding note added directly to `STATE.md`'s Key Decisions — logged there (not duplicated here) since it's operationally relevant to every future plan's state-update step, not just this one.

## From 02-10 (contacts list/detail/notes)

- **`gsd-tools requirements mark-complete AIDA-03` also returns `not_found`** — same pre-existing REQUIREMENTS.md format mismatch documented above for AIDA-06/AIDA-08 (plain `- **AIDA-NN**` bullets, no checkbox/table the tool's regex can match). AIDA-03 ("every ticket linked to a contact; opening a contact shows ticket history") is genuinely fully satisfied as of this plan (contact link/create was already done by 02-03's `createTicket`; this plan adds the browsable list/detail/history UI) — hand-verify and check it off manually in `.planning/REQUIREMENTS.md` at the next repo-hygiene pass, alongside restructuring the file for the tool's expected format.

## From 02-11 (public web intake channel)

- **Turbopack build warning: "Encountered unexpected file in NFT list" tracing `next.config.ts` through `src/lib/attachments/local-file-storage.ts`.** `pnpm run build` succeeds (compiles, typechecks, generates all routes) but emits a Node File Tracing warning once `src/app/api/public/intake/route.ts` (new in this plan) imports `local-file-storage.ts` (created in plan 04, pre-existing, untouched by 02-11). Root cause is `local-file-storage.ts`'s `path.join(ROOT, orgId)` where `ROOT = process.env.UPLOADS_DIR ?? "/data/uploads"` — Turbopack's static analysis can't fully prove the join is scoped and over-traces. Cosmetic (standalone output already worked correctly for the Docker image in Phase 1/plan 07); not fixed here per SCOPE BOUNDARY (the flagged file belongs to plan 04, not 02-11). Revisit with a `/*turbopackIgnore*/` comment or `path.join(process.cwd(), ...)` scoping if a future plan needs a clean Turbopack trace.
