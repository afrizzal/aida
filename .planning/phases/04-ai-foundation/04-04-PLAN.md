---
phase: 04-ai-foundation
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/(app)/settings/actions.ts
  - src/app/(app)/settings/page.tsx
  - src/app/(app)/settings/ai-toggle.tsx
  - src/app/(app)/settings/llm-provider-form.tsx
  - src/app/(app)/settings/llm-test-connection-button.tsx
autonomous: true
requirements: [AIDA-13, AIDA-12]
must_haves:
  truths:
    - "An admin can select a provider (OpenAI/Anthropic/Ollama), pick or type a model, and save the encrypted key"
    - "An admin can Test Connection per provider with a 10s timeout and inline success/failure"
    - "The Enable AI switch is disabled with a 'Configure a provider first' hint until a provider is saved"
    - "The Enable AI switch is NOT gated on the last Test Connection result"
    - "Toggling AI off leaves the helpdesk fully functional (no behavior change beyond skipping triage)"
  artifacts:
    - path: "src/app/(app)/settings/llm-provider-form.tsx"
      provides: "Provider dropdown + model dropdown+custom field + key/base-URL fields + Test Connection (D-01/D-03/D-04)"
      contains: "MODEL_CATALOG"
    - path: "src/app/(app)/settings/actions.ts"
      provides: "saveLlmSettings + testLlmConnection admin-gated Server Actions"
      contains: "requireOrgAdmin"
    - path: "src/app/(app)/settings/ai-toggle.tsx"
      provides: "providerConfigured-gated Switch with hint (D-21)"
      contains: "providerConfigured"
  key_links:
    - from: "src/app/(app)/settings/actions.ts"
      to: "src/lib/llm/settings.ts"
      via: "saveLlmSettings persists encrypted config"
      pattern: "saveLlmSettings"
    - from: "src/app/(app)/settings/actions.ts"
      to: "src/lib/llm/test-connection.ts"
      via: "testProviderConnection probe"
      pattern: "testProviderConnection"
    - from: "src/app/(app)/settings/page.tsx"
      to: "src/app/(app)/settings/ai-toggle.tsx"
      via: "providerConfigured prop from isProviderConfigured()"
      pattern: "providerConfigured"
---

<objective>
Extend the existing `/settings` "AI Features" page in place (no new nav tab) with provider configuration: a provider dropdown, a curated model dropdown plus a custom-model-ID free-text field (D-01), an encrypted API key field (OpenAI/Anthropic) or an Ollama base-URL field (D-03), a real per-provider Test Connection button (D-04), and the D-21 gating of the Enable AI switch (disabled until a provider is configured; NOT gated on the last test result).

Purpose: This is how an operator turns the abstract LLM port into a working AI. It mirrors Settings → Email (03-06) exactly so the UX is consistent and the security posture (admin-gated actions, blank-key-keeps-existing, never round-trip the plaintext key) is inherited, not reinvented.
Output: Extended `settings/actions.ts` + `settings/page.tsx` + `ai-toggle.tsx`, and two new client components (`llm-provider-form.tsx`, `llm-test-connection-button.tsx`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-ai-foundation/04-CONTEXT.md
@.planning/DESIGN-SYSTEM.md

<interfaces>
<!-- FROM 04-02 (already built): -->
```typescript
// src/lib/llm/settings.ts
export const LLM_SETTING_KEYS: { provider; model; apiKeyEnc; ollamaBaseUrl };
export interface LlmSettings { provider: "openai"|"anthropic"|"ollama"|""; model: string; apiKey: string; ollamaBaseUrl: string }
export async function getLlmSettings(db): Promise<LlmSettings>;
export async function saveLlmSettings(db, orgId, input: { provider?; model?; apiKey?; ollamaBaseUrl? }): Promise<void>; // blank apiKey = keep existing
export function isProviderConfigured(s: LlmSettings): boolean;
// src/lib/llm/types.ts
export const MODEL_CATALOG: Record<"openai"|"anthropic"|"ollama", readonly string[]>;
// src/lib/llm/test-connection.ts
export async function testProviderConnection(config: { provider; model; apiKey; ollamaBaseUrl }): Promise<void>; // throws on failure, 10s timeout
```
<!-- MIRROR EXACTLY (email settings 03-06): -->
<!--   src/app/(app)/settings/email/actions.ts — requireOrgAdmin() first in every action; test action falls back to the stored decrypted secret when the form field is blank; error string sliced to 200 chars; never echoes the secret. -->
<!--   src/app/(app)/settings/email/email-settings-form.tsx — react-hook-form + zodResolver + `import { z } from "zod/v4"` + shadcn Form/FormField/Input/Checkbox + toast; password field starts blank. -->
<!--   src/app/(app)/settings/email/test-connection-button.tsx — 4-state idle/testing/success/failure with aria-live region. -->
<!-- EXISTING (extend in place): -->
<!--   src/app/(app)/settings/actions.ts currently exports only setAiEnabled({ ok }). -->
<!--   src/app/(app)/settings/page.tsx currently: getScopedDb -> setting.findFirst({ key:"aiEnabled" }) -> <AiToggle defaultEnabled=... />. -->
<!--   src/app/(app)/settings/ai-toggle.tsx: optimistic Switch calling setAiEnabled. -->
<!--   src/lib/authz.ts: requireOrgAdmin() gate. -->
<!-- shadcn Select is available under @/components/ui/select (used elsewhere); if missing, add via `pnpm dlx shadcn@latest add select`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LLM provider Server Actions (save + test), admin-gated</name>
  <files>src/app/(app)/settings/actions.ts</files>
  <read_first>
    - src/app/(app)/settings/actions.ts (existing setAiEnabled — extend this file)
    - src/app/(app)/settings/email/actions.ts (saveEmailSettings/testImapConnection — the EXACT admin-gate + stored-secret-fallback + error-slice pattern to mirror)
    - src/lib/llm/settings.ts, src/lib/llm/test-connection.ts (04-02 signatures above)
    - src/lib/authz.ts (requireOrgAdmin)
  </read_first>
  <action>
    Extend `src/app/(app)/settings/actions.ts` (keep `setAiEnabled` unchanged) with:
    1. `export interface LlmSettingsInput { provider: "openai"|"anthropic"|"ollama"; model: string; apiKey?: string; ollamaBaseUrl?: string }`.
    2. `saveLlmSettings(input: LlmSettingsInput): Promise<{ ok: boolean }>` — `await requireOrgAdmin()` FIRST; `const { db, orgId } = await getScopedDb()`; call `persistLlmSettings(db, orgId, { provider, model, apiKey: input.apiKey, ollamaBaseUrl: input.ollamaBaseUrl })` (import the lib function aliased, e.g. `import { saveLlmSettings as persistLlmSettings } from "@/lib/llm/settings"`); `revalidatePath("/settings")`; return `{ ok: true }`, catch -> `{ ok: false }`. Blank `apiKey` is forwarded as-is (lib treats empty as "keep existing").
    3. `testLlmConnection(input: LlmSettingsInput): Promise<{ ok: boolean; error?: string }>` — `await requireOrgAdmin()`; `const { db } = await getScopedDb()`; resolve the key with the stored-secret fallback exactly like testImapConnection: `const apiKey = input.apiKey || (await getLlmSettings(db)).apiKey`; call `await testProviderConnection({ provider: input.provider, model: input.model, apiKey, ollamaBaseUrl: input.ollamaBaseUrl ?? "" })`; return `{ ok: true }`, catch `(e)` -> `{ ok: false, error: String((e as Error).message).slice(0, 200) }`. Never echo the key in the error.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/settings/actions.ts still exports setAiEnabled AND now exports saveLlmSettings and testLlmConnection
    - Both new actions call requireOrgAdmin() before any DB access
    - testLlmConnection falls back to the stored key via getLlmSettings when input.apiKey is blank
    - testLlmConnection slices its error to 200 chars and does not include the key
    - `pnpm exec tsc --noEmit` exits 0 and `pnpm lint` is clean on the file
  </acceptance_criteria>
  <done>Two admin-gated Server Actions persist encrypted provider config and probe connectivity, mirroring the email settings security contract exactly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Provider form + Test Connection button + AI-toggle gating (D-21), wired into the settings page</name>
  <files>src/app/(app)/settings/llm-provider-form.tsx, src/app/(app)/settings/llm-test-connection-button.tsx, src/app/(app)/settings/page.tsx, src/app/(app)/settings/ai-toggle.tsx</files>
  <read_first>
    - src/app/(app)/settings/email/email-settings-form.tsx (react-hook-form + zod/v4 + shadcn Form pattern to mirror)
    - src/app/(app)/settings/email/test-connection-button.tsx (4-state button + aria-live — mirror for LLM)
    - src/app/(app)/settings/page.tsx and src/app/(app)/settings/ai-toggle.tsx (existing — extend)
    - src/lib/llm/types.ts (MODEL_CATALOG) and src/lib/llm/settings.ts (getLlmSettings/isProviderConfigured)
    - .planning/DESIGN-SYSTEM.md (token-only classes, explicit text-[Npx] sizes, §9 checklist)
  </read_first>
  <action>
    1. Create `src/app/(app)/settings/llm-test-connection-button.tsx` — a client component mirroring `email/test-connection-button.tsx`'s 4-state idle/testing/success/failure + `role="status" aria-live="polite"` structure, but calling `testLlmConnection` from `./actions`. Props: `{ getValues: () => Promise<LlmSettingsInput | null> }`.

    2. Create `src/app/(app)/settings/llm-provider-form.tsx` — a client component (`"use client"`) mirroring `EmailSettingsForm` (react-hook-form + `zodResolver` + `import { z } from "zod/v4"` + shadcn `Form`/`FormField`/`Input`/`Select`). Fields:
       - `provider`: a shadcn `Select` with options OpenAI / Anthropic / Ollama.
       - `model`: a `Select` populated from `MODEL_CATALOG[provider]` PLUS a "Custom…" option; when Custom is chosen (or provider changes), show a free-text `Input` for the custom model ID (D-01). Persist whichever is active as the `model` string.
       - `apiKey`: a `type="password"` Input (placeholder `••••••••`, starts blank — blank = keep existing), shown ONLY when provider is openai or anthropic.
       - `ollamaBaseUrl`: a text Input (placeholder `http://localhost:11434`), shown ONLY when provider is ollama (D-03 — base URL only).
       - A `LlmTestConnectionButton` whose `getValues` validates the currently-relevant fields (`form.trigger([...])`) and returns the form values or null.
       - A single bottom Save submit calling `saveLlmSettings` from `./actions`, `toast.success`/`toast.error` on result (mirror EmailSettingsForm.onSubmit).
       Use token-only classes and explicit `text-[13px]`/`text-[12px]` sizes per DESIGN-SYSTEM.md (copy the label/description class strings from EmailSettingsForm).
       Props: `{ initial: { provider: ""|"openai"|"anthropic"|"ollama"; model: string; ollamaBaseUrl: string } }` (never receives the decrypted key).

    3. Update `src/app/(app)/settings/page.tsx`: load `const settings = await getLlmSettings(db)` alongside the existing aiEnabled read; compute `const providerConfigured = isProviderConfigured(settings)`. Render `<LlmProviderForm initial={{ provider: settings.provider, model: settings.model, ollamaBaseUrl: settings.ollamaBaseUrl }} />` and pass `providerConfigured` into `<AiToggle defaultEnabled={aiEnabled} providerConfigured={providerConfigured} />`. Keep the `<h1>AI Features</h1>` heading; place provider config above or below the toggle with the existing `space-y-6` rhythm. Ensure `export const dynamic = "force-dynamic"` is present (this page reads DB at request time).

    4. Update `src/app/(app)/settings/ai-toggle.tsx`: add a `providerConfigured: boolean` prop. When `!providerConfigured`, render the `Switch` as `disabled` and replace the helper text with "Configure a provider first" (D-21). Do NOT gate on any Test Connection result — persisted test results go stale immediately (D-21 rationale). The switch stays enabled whenever a provider is configured, regardless of whether a test was ever run or passed.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm run build && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/settings/llm-provider-form.tsx contains "MODEL_CATALOG" and a custom-model free-text Input path
    - The form shows apiKey only for openai/anthropic and ollamaBaseUrl only for ollama
    - src/app/(app)/settings/llm-test-connection-button.tsx contains `aria-live="polite"` and calls testLlmConnection
    - src/app/(app)/settings/ai-toggle.tsx accepts a `providerConfigured` prop and disables the Switch + shows "Configure a provider first" when false
    - src/app/(app)/settings/ai-toggle.tsx does NOT reference any test-connection result for gating
    - src/app/(app)/settings/page.tsx passes providerConfigured to AiToggle and renders LlmProviderForm
    - No hardcoded hex/oklch colors in the new components (token-only)
    - `pnpm exec tsc --noEmit`, `pnpm run build`, and `pnpm lint` all pass
  </acceptance_criteria>
  <done>The AI Features page lets an admin configure an encrypted provider with model selection + custom-ID fallback + Test Connection, and the Enable AI switch is correctly gated on provider-existence only (not on a stale test result).</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit`, `pnpm run build`, `pnpm lint` all clean.
- Manual reasoning check: with no provider saved, the AI switch is disabled with the hint; after saving a provider, the switch is enabled even before any Test Connection is run.
- DESIGN-SYSTEM.md §9 conformance (token-only, explicit text sizes, mirrors email settings layout).
</verification>

<success_criteria>
- Provider/model/key/base-URL configurable and stored encrypted (via 04-02's saveLlmSettings).
- Per-provider Test Connection (10s) with inline states.
- Enable AI gated on provider-configured only (D-21).
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-foundation/04-04-SUMMARY.md`. Run the DESIGN-SYSTEM.md §9 checklist and record the result.
</output>
