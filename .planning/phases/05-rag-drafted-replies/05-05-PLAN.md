---
phase: 05-rag-drafted-replies
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/app/(app)/settings/actions.ts
  - src/app/(app)/settings/embedding-provider-form.tsx
  - src/app/(app)/settings/embedding-test-connection-button.tsx
  - src/app/(app)/settings/reembed-all-button.tsx
  - src/app/(app)/settings/page.tsx
autonomous: true
requirements: [AIDA-15]

must_haves:
  truths:
    - "An admin can select an embedding provider (OpenAI or Ollama), model, and credential in Settings -> AI Features, independent of the chat provider"
    - "Test Connection validates the embedding provider/model (a not-pulled Ollama model or bad key surfaces a clear error)"
    - "Changing the embedding model exposes a 'Re-embed all KB articles' action that re-enqueues embedding for every article"
  artifacts:
    - path: "src/app/(app)/settings/embedding-provider-form.tsx"
      provides: "embedding provider config card"
      contains: "Embedding"
    - path: "src/app/(app)/settings/actions.ts"
      provides: "saveEmbeddingSettings / testEmbeddingConnection / reembedAllKb Server Actions (admin-gated)"
      contains: "saveEmbeddingSettings"
  key_links:
    - from: "src/app/(app)/settings/embedding-provider-form.tsx"
      to: "saveEmbeddingSettings Server Action"
      via: "form submit"
      pattern: "saveEmbeddingSettings"
    - from: "src/app/(app)/settings/reembed-all-button.tsx"
      to: "kb-embed-article queue"
      via: "reembedAllKb -> enqueueReembed per article"
      pattern: "reembedAllKb"
---

<objective>
Add an "Embedding Provider" configuration card to the Settings -> AI Features page so an admin can pick a separate embedding provider (OpenAI or Ollama), model, and credential — independent of the chat provider (Anthropic-for-chat orgs MUST configure OpenAI/Ollama here for RAG to work). Reuses the exact llm-provider-form / Test Connection / admin-gated Server Action patterns from Phase 4, plus a "Re-embed all KB articles" action that re-runs embedding for every article (used when the embedding model changes, since cross-model vectors are non-comparable — Pitfall 5).

Purpose: This is the operator-facing half of AIDA-15's "stored in pgvector for retrieval" — without a configured embedding provider, the 05-03 embed job and 05-04 retrieval no-op. Honors Decision 5 (separate embedding provider without breaking the one-active chat provider).
Output: embedding settings card + 3 admin-gated Server Actions + re-embed-all button wired into the existing AI Features page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-SYSTEM.md
@src/app/(app)/settings/page.tsx
@src/app/(app)/settings/actions.ts
@src/app/(app)/settings/llm-provider-form.tsx
@src/app/(app)/settings/llm-test-connection-button.tsx
@src/lib/rag/settings.ts
@src/lib/rag/types.ts
@src/lib/rag/embed-test-connection.ts

<interfaces>
<!-- Precedents to mirror exactly + 05-02/05-03 outputs to consume. -->

Existing AI Features page (page.tsx) — you insert the embedding card between LlmProviderForm and AiToggle:
```typescript
// loads getLlmSettings + isProviderConfigured; renders <LlmProviderForm/> then <AiToggle/>
```

llm-provider-form.tsx — the react-hook-form + zod/v4 + shadcn Select shape to copy (including `key={provider}` on the Model Select — the 04-07 stale-options fix; replicate it on the embedding Model Select too).

Existing actions.ts Server Action contract to mirror (saveLlmSettings/testLlmConnection):
```typescript
export async function saveLlmSettings(input): Promise<{ ok: boolean }>   // requireOrgAdmin(); blank apiKey = keep existing
export async function testLlmConnection(input): Promise<{ ok: boolean; error?: string }> // stored-key fallback, 200-char error slice
```

From 05-02: `saveEmbeddingSettings(db, orgId, input)`, `getEmbeddingSettings(db)`, `isEmbeddingConfigured(db)`, `EMBEDDING_MODEL_CATALOG` (openai/ollama), `testEmbeddingConnection(config)`.
From 05-03: `enqueueReembed(orgId, articleId)` (sets PENDING + sends kb-embed-article).

DESIGN-SYSTEM: token-only colors, `text-[Npx]` sizes, card = `rounded-lg border border-border/70 p-4` (matches llm-provider-form's wrapper), uppercase-tracking label header for the card section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embedding Server Actions (save / test / re-embed all), admin-gated</name>
  <files>src/app/(app)/settings/actions.ts</files>
  <read_first>
    - src/app/(app)/settings/actions.ts (mirror saveLlmSettings/testLlmConnection EXACTLY: requireOrgAdmin() first, getScopedDb, blank-key fallback via getEmbeddingSettings, 200-char error slice, revalidatePath("/settings"))
    - src/lib/rag/settings.ts (saveEmbeddingSettings / getEmbeddingSettings signatures)
    - src/lib/rag/embed-test-connection.ts (testEmbeddingConnection config shape)
    - src/lib/rag/types.ts (EmbeddingProviderName)
    - src/lib/kb/create-article.ts (enqueueReembed signature from 05-03)
  </read_first>
  <action>
    Add to `src/app/(app)/settings/actions.ts` (keep the existing exports):
    - `interface EmbeddingSettingsInput { provider: "openai" | "ollama"; model: string; apiKey?: string; ollamaBaseUrl?: string }`.
    - `saveEmbeddingSettings(input): Promise<{ ok: boolean }>` — `await requireOrgAdmin(); const { db, orgId } = await getScopedDb();` then `await persistEmbeddingSettings(db, orgId, input)` (import 05-02's `saveEmbeddingSettings as persistEmbeddingSettings`), `revalidatePath("/settings")`, return `{ ok: true }`; catch -> `{ ok: false }`. Blank apiKey forwarded as-is (05-02 keeps existing).
    - `testEmbeddingConnection(input): Promise<{ ok: boolean; error?: string }>` — `await requireOrgAdmin(); const { db } = await getScopedDb();` resolve the key with the stored-fallback (`const apiKey = input.apiKey || (await getEmbeddingSettings(db)).apiKey`), call the 05-02 lib function `probeEmbeddingConnection({ provider: input.provider, model: input.model, apiKey, ollamaBaseUrl: input.ollamaBaseUrl ?? "" })` (import 05-02's `testEmbeddingConnection as probeEmbeddingConnection` to avoid the name clash with THIS action), return `{ ok: true }` or `{ ok: false, error: String((e as Error).message).slice(0, 200) }`.
    - `reembedAllKb(): Promise<{ ok: boolean; count: number }>` — `await requireOrgAdmin(); const { db, orgId } = await getScopedDb();` load all article ids (`db.kbArticle.findMany({ select: { id: true } })`), call `enqueueReembed(orgId, a.id)` for each, `revalidatePath("/settings")` and `revalidatePath("/kb")`, return `{ ok: true, count }`.
  </action>
  <verify>
    <automated>cd D:/Aff/proj/aida &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm exec biome check src/app/\(app\)/settings/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `actions.ts` exports `saveEmbeddingSettings`, `testEmbeddingConnection`, and `reembedAllKb`
    - All three call `requireOrgAdmin()` as their first statement
    - `testEmbeddingConnection` falls back to the stored key via `getEmbeddingSettings` and slices errors to 200 chars
    - `reembedAllKb` iterates articles and calls `enqueueReembed`
    - `pnpm exec tsc --noEmit` and the biome check exit 0
  </acceptance_criteria>
  <done>Three admin-gated embedding Server Actions exist, mirroring the Phase 4 LLM-settings security contract exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Embedding provider form + Test Connection + Re-embed-all button, wired into the AI Features page</name>
  <files>src/app/(app)/settings/embedding-provider-form.tsx, src/app/(app)/settings/embedding-test-connection-button.tsx, src/app/(app)/settings/reembed-all-button.tsx, src/app/(app)/settings/page.tsx</files>
  <read_first>
    - src/app/(app)/settings/llm-provider-form.tsx (copy its structure: react-hook-form + zod/v4 + shadcn Form/Select, the `key={provider}` on the Model Select — the 04-07 fix — and the Custom… sentinel + free-text fallback; adapt providers to just openai|ollama)
    - src/app/(app)/settings/llm-test-connection-button.tsx (the 4-state idle/testing/success/failure `aria-live="polite"` button to copy for the embedding variant)
    - src/app/(app)/settings/page.tsx (where to insert the embedding card + what it must load)
    - .planning/DESIGN-SYSTEM.md §4 (token-only colors, text-[Npx] typography, card conventions)
    - src/lib/rag/types.ts (EMBEDDING_MODEL_CATALOG values)
  </read_first>
  <action>
    Create `src/app/(app)/settings/embedding-provider-form.tsx` (`"use client"`) by adapting llm-provider-form.tsx:
    - Provider Select limited to `openai` | `ollama` (labels "OpenAI" / "Ollama"); NO anthropic option (it has no embeddings API — add a one-line muted hint: "Anthropic has no embeddings API — choose OpenAI or Ollama for retrieval.").
    - Model Select driven by `EMBEDDING_MODEL_CATALOG[provider]` + a `Custom…` sentinel + free-text fallback (same shape as chat). Keep `key={provider}` on the Model `<Select>` (04-07 stale-options fix).
    - API key field for openai only; Base URL field for ollama only (mirror chat's conditional fields). Blank-key hint: "Leave blank to keep the currently saved key. Leave blank to reuse your chat provider's key when the same provider."
    - Card header label: "Embedding Provider" (uppercase-tracking muted, matching llm-provider-form's "AI Provider" header). Wrapper `space-y-4 rounded-lg border border-border/70 p-4`.
    - On submit -> `saveEmbeddingSettings(input)` (toast success/failure like llm-provider-form).
    - Props: `initial: { provider: "" | "openai" | "ollama"; model: string; ollamaBaseUrl: string }` (never receives the decrypted key).

    Create `src/app/(app)/settings/embedding-test-connection-button.tsx` — a copy of llm-test-connection-button.tsx calling `testEmbeddingConnection` (4-state, `aria-live="polite"`).

    Create `src/app/(app)/settings/reembed-all-button.tsx` (`"use client"`) — a button that calls `reembedAllKb()`, shows a spinner while pending, and toasts `"Re-embedding {count} articles…"` on success / an error toast on failure. Muted helper text: "Run this after changing the embedding model — vectors from different models are not comparable." Disable when there are zero articles (accept an `articleCount: number` prop).

    Edit `src/app/(app)/settings/page.tsx`: also load `getEmbeddingSettings(db)`, `isEmbeddingConfigured(db)`, and `db.kbArticle.count()`. Render `<EmbeddingProviderForm initial={{ provider, model, ollamaBaseUrl }} />` between `<LlmProviderForm/>` and `<AiToggle/>`, followed by `<ReembedAllButton articleCount={...} />` (render the button under the embedding card).
  </action>
  <verify>
    <automated>cd D:/Aff/proj/aida &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm run build &amp;&amp; pnpm exec biome check src/app/\(app\)/settings</automated>
  </verify>
  <acceptance_criteria>
    - `embedding-provider-form.tsx` offers only `openai`/`ollama` providers and contains `key={provider}` on the Model Select
    - It uses design tokens only (grep: no `oklch(`/`#` hex color literals) and `text-[` sizes (no `text-lg`/`text-xl`)
    - `embedding-test-connection-button.tsx` calls `testEmbeddingConnection` and has `aria-live="polite"`
    - `reembed-all-button.tsx` calls `reembedAllKb` and disables at zero articles
    - `page.tsx` renders `<EmbeddingProviderForm` and loads `isEmbeddingConfigured`
    - `pnpm exec tsc --noEmit`, `pnpm run build`, and the biome check exit 0
  </acceptance_criteria>
  <done>Admins configure a separate embedding provider (with Test Connection) and can re-embed all KB articles after a model change, all conforming to DESIGN-SYSTEM.md.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` + `pnpm run build` clean.
- `pnpm exec biome check src/app/(app)/settings` clean.
- DESIGN-SYSTEM check: grep the new files for hardcoded `oklch(`/hex colors and Tailwind named text sizes — must find none.
</verification>

<success_criteria>
- Embedding provider (openai/ollama) + model + credential configurable independently of chat, with Test Connection.
- Re-embed-all action available and wired to the kb-embed-article queue.
- All UI token-only + text-[Npx], no anthropic embedding option.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rag-drafted-replies/05-05-SUMMARY.md`.
</output>
