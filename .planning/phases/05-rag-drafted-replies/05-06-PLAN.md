---
phase: 05-rag-drafted-replies
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - src/app/(app)/kb/page.tsx
  - src/app/(app)/kb/new/page.tsx
  - src/app/(app)/kb/[id]/page.tsx
  - src/app/(app)/kb/actions.ts
  - src/components/kb/kb-article-form.tsx
  - src/components/kb/kb-embedding-status-chip.tsx
autonomous: true
requirements: [AIDA-15]

must_haves:
  truths:
    - "An admin can create a KB article (title + Markdown body) and see it appear in the /kb list"
    - "The KB list and article view show an embedding status chip (PENDING / COMPLETED / FAILED)"
    - "An admin can edit an existing article's title/body, which re-embeds it"
    - "An empty KB shows the halo+icon-box empty state (DESIGN-SYSTEM), not a blank page"
  artifacts:
    - path: "src/app/(app)/kb/page.tsx"
      provides: "KB article list (replaces the EmptyState-only stub)"
      contains: "kbArticle"
    - path: "src/app/(app)/kb/actions.ts"
      provides: "admin-gated create/update Server Actions delegating to lib/kb"
      contains: "createKbArticleAction"
    - path: "src/components/kb/kb-embedding-status-chip.tsx"
      provides: "PENDING/COMPLETED/FAILED status chip"
      contains: "KbEmbeddingStatus"
  key_links:
    - from: "src/app/(app)/kb/actions.ts"
      to: "src/lib/kb/create-article.ts"
      via: "createKbArticle/updateKbArticle delegation"
      pattern: "createKbArticle"
    - from: "src/app/(app)/kb/[id]/page.tsx"
      to: "kb-embedding-status-chip"
      via: "renders article.embeddingStatus"
      pattern: "KbEmbeddingStatus"
---

<objective>
Turn the `/kb` sidebar stub into a working knowledge-base authoring surface: a list of articles (with per-article embedding status), an authoring form (title + Markdown body — "author or import" is satisfied by paste-Markdown authoring), and a view/edit page. All admin-gated, delegating writes to the single `createKbArticle`/`updateKbArticle` path from 05-03 (which chunks + embeds after commit). Fully DESIGN-SYSTEM compliant (token-only, `text-[Npx]`, halo+icon-box empty state, chip conventions), living under the existing "Knowledge Base" sidebar entry.

Purpose: This is the admin-facing half of AIDA-15 ("Admins can author or import knowledge-base articles"). It drives the 05-03 embed pipeline; 05-04's retrieval reads what admins write here.
Output: /kb list + /kb/new + /kb/[id] pages + kb actions + article form + status chip.

Note (AIDA-15 "import"): v1 supports authoring via a Markdown body field (paste = import). Bulk file/URL import is deferred; noted here for honest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-SYSTEM.md
@src/app/(app)/kb/page.tsx
@src/components/empty-state.tsx
@src/app/(app)/settings/sla/sla-form.tsx
@src/app/(app)/settings/actions.ts
@src/lib/kb/create-article.ts
@src/lib/authz.ts
@src/components/tickets/triage-status-chip.tsx

<interfaces>
<!-- Precedents to mirror + 05-01/05-03 outputs to consume. -->

Existing /kb stub (page.tsx) — replace its body with a real list; keep the EmptyState for the zero-article case:
```typescript
import { EmptyState } from "@/components/empty-state";
// <EmptyState icon={BookOpen} heading=... body=... />  (halo+icon-box pattern — reuse for empty KB)
```

EmptyState (empty-state.tsx) — the mandated halo+icon-box empty-state component (icon/heading/body props).

Server-action + admin-gate precedents:
```typescript
import { requireOrgAdmin } from "@/lib/authz";   // call first in every mutating admin action
import { getScopedDb } from "@/lib/session";      // const { db, orgId } = await getScopedDb();
```

From 05-03: `createKbArticle(orgId, { title, bodyMarkdown }) -> { id }`, `updateKbArticle(orgId, articleId, { title, bodyMarkdown }) -> { ok }`.
From 05-01: `KbEmbeddingStatus` enum ("PENDING"|"COMPLETED"|"FAILED"), generated as `@/generated/prisma/client`.

triage-status-chip.tsx / the Phase 2 chip vocabulary — the Badge-based token-only chip shape to mirror for kb-embedding-status-chip.

sla-form.tsx — a react-hook-form + zod/v4 + shadcn Form example for the article form (title Input + body Textarea).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KB Server Actions (admin-gated create/update) + embedding status chip</name>
  <files>src/app/(app)/kb/actions.ts, src/components/kb/kb-embedding-status-chip.tsx</files>
  <read_first>
    - src/app/(app)/settings/actions.ts (the `"use server"` + requireOrgAdmin + getScopedDb + revalidatePath return-`{ ok }` convention)
    - src/lib/kb/create-article.ts (createKbArticle/updateKbArticle signatures from 05-03)
    - src/lib/authz.ts (requireOrgAdmin)
    - src/components/tickets/triage-status-chip.tsx + src/components/tickets/priority-chip.tsx (the token-only Badge chip shape)
    - .planning/DESIGN-SYSTEM.md §4 (chip color tokens: use `--warning`/`--success`/`--destructive` token families, never hardcoded colors)
  </read_first>
  <action>
    Create `src/app/(app)/kb/actions.ts` (`"use server"`):
    - `createKbArticleAction(input: { title: string; bodyMarkdown: string }): Promise<{ ok: boolean; id?: string }>` — `await requireOrgAdmin(); const { orgId } = await getScopedDb();` validate title non-empty (return `{ ok: false }` if blank), `const { id } = await createKbArticle(orgId, input)`, `revalidatePath("/kb")`, return `{ ok: true, id }`; catch -> `{ ok: false }`.
    - `updateKbArticleAction(articleId: string, input: { title: string; bodyMarkdown: string }): Promise<{ ok: boolean }>` — `await requireOrgAdmin(); const { orgId } = await getScopedDb();` `await updateKbArticle(orgId, articleId, input)`, `revalidatePath("/kb")` + `revalidatePath(\`/kb/${articleId}\`)`, return `{ ok: true }`; catch -> `{ ok: false }`.

    Create `src/components/kb/kb-embedding-status-chip.tsx` — a presentational Badge chip driven by `status: KbEmbeddingStatus` (import the type from `@/generated/prisma/client`), token-only, mirroring triage-status-chip's visual shape:
    - PENDING -> muted "Embedding…" (`text-muted-foreground`, small).
    - COMPLETED -> success token "Indexed" (`bg-success/10 text-success border border-success/20`).
    - FAILED -> destructive token "Embedding failed" (`bg-destructive/10 text-destructive border border-destructive/20`).
    Use `text-[12px]` sizing and `size-3`/`size-3.5` icons only. No hardcoded colors.
  </action>
  <verify>
    <automated>cd D:/Aff/proj/aida &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm exec biome check src/app/\(app\)/kb/actions.ts src/components/kb</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/kb/actions.ts` exports `createKbArticleAction` and `updateKbArticleAction`, both calling `requireOrgAdmin()` first and delegating to `createKbArticle`/`updateKbArticle`
    - `src/components/kb/kb-embedding-status-chip.tsx` handles all three `KbEmbeddingStatus` values and imports the type from `@/generated/prisma/client`
    - The chip uses only token classes (grep: no `oklch(` / hex color literals) and `text-[` sizes
    - `pnpm exec tsc --noEmit` and the biome check exit 0
  </acceptance_criteria>
  <done>Admin-gated KB write actions delegate to the single lib/kb path; a token-only status chip renders all three embedding states.</done>
</task>

<task type="auto">
  <name>Task 2: KB list, authoring form, and view/edit pages</name>
  <files>src/app/(app)/kb/page.tsx, src/app/(app)/kb/new/page.tsx, src/app/(app)/kb/[id]/page.tsx, src/components/kb/kb-article-form.tsx</files>
  <read_first>
    - src/app/(app)/kb/page.tsx (the current stub to replace; note the EmptyState usage)
    - src/components/empty-state.tsx (the halo+icon-box empty state to reuse when zero articles)
    - src/app/(app)/settings/sla/sla-form.tsx (react-hook-form + zod/v4 + shadcn Form/Input/Textarea shape for the article form)
    - src/app/(app)/contacts/page.tsx (a list Server Component precedent — layout + `text-[Npx]` typography + link rows)
    - .planning/DESIGN-SYSTEM.md §4 (token-only, text-[Npx], card/empty-state conventions)
    - src/app/(app)/kb/actions.ts + src/components/kb/kb-embedding-status-chip.tsx (from Task 1)
  </read_first>
  <action>
    Create `src/components/kb/kb-article-form.tsx` (`"use client"`) — react-hook-form + zod/v4 mirroring sla-form.tsx:
    - Fields: `title` (Input, required, 3–120 chars) and `bodyMarkdown` (Textarea, required, min-height ~240px, monospace-ish is fine but keep token typography).
    - Props: `mode: "create" | "edit"`, optional `articleId`, optional `initial: { title: string; bodyMarkdown: string }`.
    - On submit: create mode -> `createKbArticleAction(values)` then `router.push(\`/kb/${result.id}\`)` on success; edit mode -> `updateKbArticleAction(articleId, values)` then `router.refresh()`. Toast success/failure. Show a "Saving will (re)embed this article in the background" muted hint near the submit button.

    Replace `src/app/(app)/kb/page.tsx` with a `force-dynamic` Server Component list:
    - `const { db } = await getScopedDb(); const articles = await db.kbArticle.findMany({ orderBy: { updatedAt: "desc" }, select: { id, title, slug, embeddingStatus, updatedAt } });`
    - Header row: an `<h1 class="text-[18px] font-semibold">Knowledge Base</h1>` + a "New article" link (`Link href="/kb/new"`, primary button styling).
    - If `articles.length === 0`, render `<EmptyState icon={BookOpen} heading="No articles yet" body="Create knowledge base articles so AIDA can draft grounded, cited replies." />` (keeps the halo+icon-box pattern).
    - Otherwise a list of link rows to `/kb/[id]` showing title + `<KbEmbeddingStatusChip status={...} />` + relative updated time (reuse `formatRelativeTime`). Token-only, `text-[Npx]`.

    Create `src/app/(app)/kb/new/page.tsx` (`force-dynamic`) — a header ("New article") + `<KbArticleForm mode="create" />`. (No page-level admin guard needed — the Server Actions are admin-gated, matching the SLA/Tags/Custom-Fields precedent where authorization is action-gated, not page-gated.)

    Create `src/app/(app)/kb/[id]/page.tsx` (`force-dynamic`) — loads the article via `db.kbArticle.findFirst({ where: { id } })`; if missing, an EmptyState "Article not found". Otherwise render the title, the `<KbEmbeddingStatusChip status={article.embeddingStatus} />`, and `<KbArticleForm mode="edit" articleId={article.id} initial={{ title, bodyMarkdown: article.bodyMarkdown }} />`. (Editing the raw Markdown is the v1 view/edit surface; a rendered preview is optional and out of scope.)
  </action>
  <verify>
    <automated>cd D:/Aff/proj/aida &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm run build &amp;&amp; pnpm exec biome check src/app/\(app\)/kb src/components/kb</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/kb/page.tsx` queries `db.kbArticle.findMany` and renders `<EmptyState` when there are zero articles
    - `src/app/(app)/kb/new/page.tsx` renders `<KbArticleForm mode="create"`
    - `src/app/(app)/kb/[id]/page.tsx` renders `<KbArticleForm mode="edit"` + `<KbEmbeddingStatusChip`
    - `kb-article-form.tsx` calls `createKbArticleAction` (create) and `updateKbArticleAction` (edit)
    - All new files use only token classes and `text-[Npx]` sizes (grep: no `oklch(`/hex/`text-lg`/`text-xl`)
    - `pnpm exec tsc --noEmit`, `pnpm run build`, and the biome check exit 0
  </acceptance_criteria>
  <done>Admins can create, list, view, and edit KB articles from the sidebar's Knowledge Base entry; every save drives the embed pipeline and shows live status; the empty case uses the mandated halo+icon-box state.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` + `pnpm run build` clean.
- `pnpm exec biome check src/app/(app)/kb src/components/kb` clean.
- DESIGN-SYSTEM check: grep new files for `oklch(`/hex colors and Tailwind named text sizes — none found; EmptyState reused for the empty KB.
</verification>

<success_criteria>
- Working /kb list + /kb/new + /kb/[id] authoring surface under the existing sidebar entry.
- Create/edit delegates to the single lib/kb path (chunk+embed after commit) with a live embedding status chip.
- Fully token-only, text-[Npx], halo+icon-box empty state.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rag-drafted-replies/05-06-SUMMARY.md`.
</output>
