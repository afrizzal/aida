# Deferred Items — Phase 05 (rag-drafted-replies)

Out-of-scope discoveries logged during plan execution. Not fixed per SCOPE BOUNDARY rule (pre-existing, not caused by the current plan's changes).

## From 05-05 (Settings embedding provider card + Test Connection + re-embed-all)

- **CRLF vs LF formatter mismatch (repo-wide, environment artifact) — same root cause documented at 02-05/02-08.** `pnpm exec biome check src/app/(app)/settings` (the plan's own directory-wide verify command) flags every pre-existing, untouched file in the directory (`ai-toggle.tsx`, `llm-provider-form.tsx`, `llm-test-connection-button.tsx`, `settings-nav.tsx`, `layout.tsx`, `email/*`, `sla/*`, `tags/*`, `custom-fields/*`) with an identical CRLF-vs-LF diff. Root cause: `core.autocrlf=true` checks files out with CRLF on this Windows machine while the repo stores LF; this plan's own worktree required a from-scratch `pnpm install` + checkout, reproducing the same condition on every file in the tree, not just this plan's diff. Confirmed pre-existing (not introduced by 05-05) by running `biome check` against a single untouched sibling file (`ai-toggle.tsx`) in isolation — same failure. Not fixed here per SCOPE BOUNDARY (mass-rewriting the whole repo's line endings is destructive and out of scope for a single plan). This plan's own 4 touched files (`actions.ts`, `embedding-provider-form.tsx`, `embedding-test-connection-button.tsx`, `reembed-all-button.tsx`, `page.tsx`) were verified individually with `biome check` scoped to just those paths — all clean (mechanical `biome check --write` applied only to the files this plan edited, matching the 04-07 precedent). Revisit at a repo-hygiene pass (`.gitattributes` with `* text=auto eol=lf`), same as the 02-05/02-08 entries.
