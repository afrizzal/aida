---
phase: 03-email-channel
plan: 03
subsystem: email-parsing
tags: [rehype, mailparser, html-to-text, sanitization, threading, tdd]

# Dependency graph
requires:
  - phase: 03-email-channel (03-01)
    provides: "Message.emailMessageId/emailInReplyTo/emailReferences/deliveryStatus fields, imapflow/mailparser/nodemailer/html-to-text/rehype-parse dependencies"
provides:
  - "sanitizeEmailHtml() in src/lib/markdown/render.ts — inbound HTML sanitization with remote-image stripping"
  - "extractEmailBody() — html+text body reduction with html-to-text fallback"
  - "thread-match helpers — extractSubjectTicketNumber, collectCandidateMessageIds, findTicketIdByEmailMessageIds, findTicketByNumber"
  - "isAutoGenerated() — RFC 3834 / Precedence / bounce / List-Id auto-reply predicate"
affects: [03-04-inbound-poll-job, 03-05-outbound-send]

# Tech tracking
tech-stack:
  added: ["@types/html-to-text (devDependency, missing bundled types)"]
  patterns:
    - "Second rehype pipeline (rehype-parse based) alongside the existing remark-based renderMarkdown() pipeline, both reusing the SAME schema/rehypeSafeLinks — one sanitization authority, two entry points"
    - "Remote-image stripping via a rehype plugin that clears img.properties.src for http(s) URLs BEFORE the sanitize pass, so the src-less node gets dropped structurally rather than filtered by string matching"
    - "Loosely-typed db param (structural interface, not the full scopedDb type) for bundling-context-agnostic helpers consumed by both worker (esbuild) and app (webpack) code"

key-files:
  created:
    - src/lib/channels/email/parse-body.ts
    - src/lib/channels/email/thread-match.ts
    - src/lib/channels/email/auto-generated.ts
    - tests/unit/sanitize-email-html.test.ts
    - tests/unit/email-parse-body.test.ts
    - tests/unit/email-thread-match.test.ts
  modified:
    - src/lib/markdown/render.ts
    - package.json
    - pnpm-lock.yaml

key-decisions:
  - "sanitizeEmailHtml() lives in the SAME module as renderMarkdown() (src/lib/markdown/render.ts) — one sanitization authority, per STATE.md 02-02 precedent."
  - "Remote-image stripping clears img.properties.src (not deleting the img node) so rehype-sanitize's own schema does the final drop — no ad hoc DOM-removal logic duplicating what the sanitizer already does."
  - "isAutoGenerated's List-Id detection uses BOTH parsed.headers.get('list')?.id and a raw headerLines fallback (defensive dual check per 03-RESEARCH.md Open Question 1 — mailparser's exact List-Id sub-shape wasn't docs-confirmed)."

patterns-established:
  - "Email HTML sanitization pipeline: rehype-parse (fragment) -> rehypeStripRemoteImages -> rehypeSafeLinks -> rehypeSanitize(schema) -> rehypeStringify."
  - "extractEmailBody() is a pure reducer only — no sanitization, no attachment I/O; the plan-04 ingest orchestrator composes it with cid-rewriting + sanitizeEmailHtml() once attachment ids exist."
  - "Thread-match candidate ordering: inReplyTo first, then references array/string, falsy-filtered, deduped preserving order — mirrors the exact order mailparser exposes them."

requirements-completed: []  # AIDA-09 spans the whole phase; not marked complete until inbound+outbound flow lands (per 03-01's precedent).

duration: 15min
completed: 2026-07-06
---

# Phase 3 Plan 3: Inbound Email Parsing Primitives Summary

**TDD-built pure parsing helpers for the inbound email pipeline: HTML sanitization with tracking-pixel stripping, body reduction with html-to-text fallback, and RFC-3834-aware thread-matching/auto-reply detection.**

## Performance

- **Duration:** 15 min (task work only; ~10 additional min of one-time worktree/dependency setup — see Issues Encountered)
- **Started:** 2026-07-06T10:21:00+07:00
- **Completed:** 2026-07-06T10:34:31+07:00
- **Tasks:** 3
- **Files modified:** 9 (6 created, 3 modified)

## Accomplishments
- `sanitizeEmailHtml()` added to the ONE sanitization module (`src/lib/markdown/render.ts`), reusing the existing `schema`/`rehypeSafeLinks`, with a new `rehypeStripRemoteImages` plugin that strips `http(s)://` `<img>` src so tracking pixels never fire (D-18) while relative/same-origin src (cid-rewritten by plan 04) survives.
- `extractEmailBody()` reduces a parsed email to `{ rawHtml, text }`, falling back to `html-to-text`'s `convert()` when no `text/plain` part exists.
- `thread-match.ts` + `auto-generated.ts`: header-Message-ID candidate collection, `[#N]` subject-token extraction, DB threading lookups (loosely-typed `db` param), and a full RFC 3834/Precedence/bounce/List-Id auto-reply predicate.
- All three modules verified `@/`-free (worker-bundleable via esbuild, per D-01's pluggable-channel architecture).

## Task Commits

Each task was committed atomically (TDD RED -> GREEN per task; no REFACTOR step needed — implementations matched the plan's action text directly):

1. **Task 1: sanitizeEmailHtml() + remote-image stripping**
   - `7ae02fd` test(03-03): add failing test for sanitizeEmailHtml
   - `adad2c3` feat(03-03): implement sanitizeEmailHtml with remote-image stripping
2. **Task 2: extractEmailBody()**
   - `f025dc9` test(03-03): add failing test for extractEmailBody
   - `47ae5be` feat(03-03): implement extractEmailBody with html-to-text fallback (includes `@types/html-to-text` deviation)
3. **Task 3: thread-match helpers + isAutoGenerated()**
   - `9c8968c` test(03-03): add failing test for thread-match + isAutoGenerated
   - `f991885` feat(03-03): implement thread-match helpers + isAutoGenerated predicate

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `src/lib/markdown/render.ts` - added `sanitizeEmailHtml()` + `rehypeStripRemoteImages` plugin alongside the existing `renderMarkdown()`
- `src/lib/channels/email/parse-body.ts` - `extractEmailBody(parsed)` html/text reduction
- `src/lib/channels/email/thread-match.ts` - `extractSubjectTicketNumber`, `collectCandidateMessageIds`, `findTicketIdByEmailMessageIds`, `findTicketByNumber`
- `src/lib/channels/email/auto-generated.ts` - `isAutoGenerated(parsed)` predicate
- `tests/unit/sanitize-email-html.test.ts` - 5 assertions (remote-strip x2, same-origin-survive, script/handler-strip, safe-link-stamp)
- `tests/unit/email-parse-body.test.ts` - 3 assertions (html+text, html-only fallback, text-only)
- `tests/unit/email-thread-match.test.ts` - 9 assertions (subject token x2, candidate collection, isAutoGenerated x6)
- `package.json` / `pnpm-lock.yaml` - added `@types/html-to-text` devDependency

## Decisions Made
- Kept the two sanitize pipelines (`renderMarkdown`'s remark-based one, `sanitizeEmailHtml`'s rehype-parse-based one) as separate `unified()` processors but verified only ONE `schema` const exists (no forked/duplicated allowlist) — satisfies the plan's "grep count of `defaultSchema` stays effectively single-source" acceptance criterion.
- `rehypeStripRemoteImages` mutates `node.properties.src` to `undefined` rather than removing the node outright, letting `rehype-sanitize`'s existing schema handle the final drop of a now-attributeless `<img>` — avoids duplicating removal logic the sanitizer already owns.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added missing `@types/html-to-text` devDependency**
- **Found during:** Task 2 (extractEmailBody implementation)
- **Issue:** `html-to-text@10.0.0` ships no bundled `.d.ts` and no `types` field in its `package.json`; `tsc --noEmit` failed with `TS7016: Could not find a declaration file for module 'html-to-text'`. This blocked the plan's own acceptance criterion ("`pnpm test` exits 0" is satisfied, but the plan's overall `<verification>` block also requires `pnpm exec tsc --noEmit` clean).
- **Fix:** `pnpm add -D @types/html-to-text` (resolved to `9.0.4`, the latest available — DefinitelyTyped version numbering doesn't track `html-to-text`'s major 1:1, consistent with the `@types/nodemailer`/`@types/mailparser` precedent already noted in 03-RESEARCH.md).
- **Files modified:** `package.json`, `pnpm-lock.yaml`
- **Verification:** `pnpm exec tsc --noEmit` clean after the install; `pnpm test` still 27/27 (later 36/36) green.
- **Committed in:** `47ae5be` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking dependency gap)
**Impact on plan:** Necessary to satisfy the plan's own `tsc --noEmit` clean requirement; no scope creep — no new behavior added, only a type-declaration gap closed.

## Issues Encountered
- **Stale worktree.** This worktree (`agent-a895e631535e9a099`) was branched before Phase 3 planning and Wave 1 execution landed on `master` — `.planning/phases/03-email-channel/` and `src/lib/markdown/render.ts`'s baseline didn't exist here. Fast-forward merged `master` into the worktree branch first (`git merge master --ff-only`, no divergent commits existed so this was a clean fast-forward) to pick up 03-01/03-02's work (Message schema fields, imapflow/mailparser/nodemailer/html-to-text/rehype-parse deps) before starting this plan's tasks.
- **No `node_modules`/`.env` in the worktree.** Ran `pnpm install`, `cp .env.example .env`, `pnpm prisma generate` once before any task work — standard fresh-worktree bootstrap per STATE.md's documented 02-02 precedent.
- Both were one-time setup costs, not plan-execution issues; no code changes resulted from them beyond what's already tracked above.

## User Setup Required

None - no external service configuration required. (This plan is pure/unit-testable helpers; no IMAP/SMTP wiring — that lands in 03-04/03-05.)

## Next Phase Readiness
- Plan 04 (inbound ingest orchestrator) can now compose: `extractEmailBody()` -> save inline-`cid` attachments -> rewrite `cid:` refs to `/api/attachments/{id}` -> `sanitizeEmailHtml(rawHtml)` -> `Message.bodyHtml`; `renderMarkdown(text)` for the plain-text-only path -> `Message.bodyMarkdown`.
- Plan 04 can wire `collectCandidateMessageIds` + `findTicketIdByEmailMessageIds` (header match) -> `extractSubjectTicketNumber` + `findTicketByNumber` (subject-token fallback) -> `createTicket()` (miss) exactly per D-08/D-09/D-10, gating ticket-creation/auto-reopen on `!isAutoGenerated(parsed)` per D-13.
- No blockers. Plan 05 (outbound send, running in the same Wave 2) shares no files with this plan (confirmed no merge conflicts expected).

---
*Phase: 03-email-channel*
*Completed: 2026-07-06*

## Self-Check: PASSED

All 7 created files confirmed present on disk; all 6 task commits (`7ae02fd`, `adad2c3`, `f025dc9`, `47ae5be`, `9c8968c`, `f991885`) confirmed present in `git log`.
