---
phase: 07-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - src/proxy.ts
  - src/middleware.ts
  - tests/unit/proxy.test.ts
  - tests/unit/middleware.test.ts
  - src/components/tickets/sla-due-chip.tsx
  - src/lib/worker/index.ts
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/02-core-ticketing/deferred-items.md
autonomous: true
requirements: [AIDA-23]
must_haves:
  truths:
    - "`git status --porcelain` is empty and `pnpm lint` reports zero line-ending diffs on a fresh checkout"
    - "The Next 16 `middleware.ts` deprecation warning no longer appears in `pnpm run build` output"
    - "SlaDueChip's tooltip timestamp is identical on server and client (no hydration mismatch on a non-en-US OS)"
    - "`gsd-tools requirements mark-complete AIDA-22` mutates .planning/REQUIREMENTS.md instead of returning not_found"
  artifacts:
    - path: ".gitattributes"
      provides: "repo-wide LF normalization contract"
      contains: "* text=auto eol=lf"
    - path: "src/proxy.ts"
      provides: "Next 16 proxy convention replacing src/middleware.ts"
      contains: "export function proxy"
    - path: ".planning/REQUIREMENTS.md"
      provides: "checkbox + traceability-table format gsd-tools can mutate"
      contains: "- [x] **AIDA-01**"
  key_links:
    - from: "src/proxy.ts"
      to: "better-auth/cookies getSessionCookie"
      via: "unchanged auth gate logic carried over verbatim from middleware.ts"
      pattern: "getSessionCookie"
    - from: "tests/unit/proxy.test.ts"
      to: "src/proxy.ts"
      via: "import { proxy } from \"@/proxy\""
      pattern: "from \"@/proxy\""
---

<objective>
Execute the long-deferred repo-hygiene pass (D-14) as the FIRST plan of Phase 7, so every later plan in this phase lands on a clean, LF-normalized tree.

Purpose: the CRLF-vs-LF drift has been re-logged in six separate plan retrospectives (02-05, 02-08, 05-05 deferred-items) and makes `biome check` unusable as a gate. Because the fix is a repo-wide `git add --renormalize .` that touches every tracked text file, it CANNOT run in parallel with any other plan — this plan owns Wave 1 alone and every other Phase 7 plan depends on it.
Output: `.gitattributes` + a normalized tree, the Next 16 `middleware.ts` -> `proxy.ts` rename, the SlaDueChip locale hydration fix, the worker import-order lint fix, and a restructured REQUIREMENTS.md that `gsd-tools requirements mark-complete` can actually mutate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-launch-readiness/07-CONTEXT.md
@.planning/phases/02-core-ticketing/deferred-items.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Concrete current state the executor must respect. Do not re-derive by exploring. -->

`src/middleware.ts` today exports exactly two things:
```ts
export function middleware(request: NextRequest) { ... }
export const config = { matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"] };
```
`PUBLIC_PREFIXES = ["/login","/setup","/api/auth","/api/health","/request","/status","/api/public"]`.
`next.config.ts` ALREADY uses the Next 16 key `experimental.proxyClientMaxBodySize` — no next.config change is needed for this rename.

`tests/unit/middleware.test.ts` imports `import { middleware } from "@/middleware";` and calls `middleware(request)` in 4 tests.

`src/components/tickets/sla-due-chip.tsx` line 15: `const fullTimestamp = new Date(dueAt).toLocaleString();`

`src/lib/worker/index.ts` lines 1-2:
```ts
import type { Job } from "pg-boss";
import { PgBoss } from "pg-boss";
```
biome's `assist/source/organizeImports` wants the type import AFTER the value import from the same module.

ALREADY CLOSED (verify only, do NOT redo): the 02-07 chip/SLA-literal dedup. `settings/sla/page.tsx` imports `DEFAULT_SLA_TARGETS` from `@/lib/tickets/sla`, `settings/sla/sla-form.tsx` imports `PriorityChip`, `settings/tags/tag-manager.tsx` imports `TagChip`. The STATE.md "Open Todos" entry for it is stale.

`gsd-tools requirements mark-complete` matches ONLY these two shapes (from bin/lib/milestone.cjs):
1. `- [ ] **AIDA-01**` -> flips to `- [x] **AIDA-01**`
2. `| AIDA-01 | Phase 2 | Pending |` -> flips the third cell to `Complete`
The current file uses plain `- **AIDA-01** \`MVP\` — ...` bullets, which match neither.

RTK shell-hook note: if `pnpm exec <tool>` or `pnpm lint` is intercepted/mangled in your session, call the binary directly, e.g. `node node_modules/@biomejs/biome/bin/biome check .` and `node node_modules/typescript/bin/tsc --noEmit`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .gitattributes + one-time repo-wide LF renormalization</name>
  <files>.gitattributes</files>
  <read_first>
    - .planning/phases/02-core-ticketing/deferred-items.md (the 02-05 and 02-08 CRLF entries — the exact problem statement and the "revisit at a repo-hygiene pass" instruction this task discharges)
    - .gitignore (so the renormalization does not surprise you with untracked build output)
  </read_first>
  <action>
    Create `.gitattributes` at the repo root with EXACTLY this content (the `*.sh` rule matters — Plan 07-04 ships `scripts/backup.sh`/`scripts/restore.sh` that must never get CRLF or they break inside the Alpine/Debian containers):

    ```
    # Normalize every text file to LF in the repo AND in the working tree.
    # Fixes the CRLF drift caused by core.autocrlf=true on Windows checkouts
    # (logged 6x since 02-05); overrides autocrlf without touching git config.
    * text=auto eol=lf

    # Shell scripts MUST be LF — CRLF breaks the shebang inside Linux containers.
    *.sh text eol=lf
    Dockerfile text eol=lf
    Caddyfile text eol=lf

    # Lockfiles: LF, and never diffed in review noise.
    pnpm-lock.yaml text eol=lf -diff

    # Binary assets — never normalize, never diff.
    *.png binary
    *.jpg binary
    *.jpeg binary
    *.gif binary
    *.webp binary
    *.ico binary
    *.woff binary
    *.woff2 binary
    *.pdf binary
    ```

    Then perform the one-time renormalization, in this exact order:
    1. `git add .gitattributes && git commit -m "chore(07-01): add .gitattributes enforcing LF line endings"`
    2. `git add --renormalize .`
    3. `git commit -m "chore(07-01): normalize line endings to LF repo-wide"` (if step 2 staged nothing, say so in the SUMMARY and continue — the tree was already clean)
    4. Refresh the working tree so it matches the new attributes: `git rm --cached -r . -q && git reset --hard`
    5. Confirm `git status --porcelain` prints nothing.

    Do NOT change local git config (`core.autocrlf`) — `eol=lf` in `.gitattributes` overrides it for checkout, which is the whole point.
  </action>
  <verify>
    <automated>git status --porcelain && node node_modules/@biomejs/biome/bin/biome check . && node node_modules/typescript/bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `.gitattributes` exists at the repo root and its first non-comment line is `* text=auto eol=lf`
    - `.gitattributes` contains `*.sh text eol=lf`
    - `git status --porcelain` produces EMPTY output after step 5
    - `node node_modules/@biomejs/biome/bin/biome check .` exits 0 with zero formatting diffs (previously it flagged effectively every file for line endings)
    - `node node_modules/typescript/bin/tsc --noEmit` exits 0
    - `git log --oneline -2` shows the two chore commits from steps 1 and 3
  </acceptance_criteria>
  <done>The whole tree is LF-normalized, `biome check .` is clean, and every later Phase 7 plan starts from a diff-noise-free baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Next 16 proxy rename + SlaDueChip locale fix + worker import order</name>
  <files>src/proxy.ts, src/middleware.ts, tests/unit/proxy.test.ts, tests/unit/middleware.test.ts, src/components/tickets/sla-due-chip.tsx, src/lib/worker/index.ts</files>
  <read_first>
    - src/middleware.ts (the full 29-line file you are renaming — its logic is carried over VERBATIM, only the function name changes)
    - tests/unit/middleware.test.ts (4 tests importing `{ middleware } from "@/middleware"` — the import and every call site change)
    - src/components/tickets/sla-due-chip.tsx (line 15 is the only line that changes)
    - src/lib/worker/index.ts (lines 1-2 only)
  </read_first>
  <behavior>
    - The renamed proxy still returns `NextResponse.next()` for every path in PUBLIC_PREFIXES without a session cookie
    - It still returns a 401 JSON body `{ error: "unauthorized" }` for an unauthenticated `/api/*` request
    - It still redirects an unauthenticated `(app)` route to `/login`
    - `SlaDueChip`'s `title` string is byte-identical whether produced under an `en-US` or an `id-ID` process locale
  </behavior>
  <action>
    Three independent fixes; do all three, then run the gates once.

    **(a) `src/middleware.ts` -> `src/proxy.ts` (Next 16 convention).**
    Use `git mv src/middleware.ts src/proxy.ts`, then change ONLY the exported function name:
    ```ts
    export function proxy(request: NextRequest) {
    ```
    Everything else (the `getSessionCookie` import, `PUBLIC_PREFIXES`, the 401-JSON branch, the `/login` redirect, and `export const config = { matcher: [...] }`) stays byte-identical. Do not run the codemod; the file is 29 lines and a manual rename is lower-risk.
    Then `git mv tests/unit/middleware.test.ts tests/unit/proxy.test.ts` and inside it change:
    - `import { middleware } from "@/middleware";` -> `import { proxy } from "@/proxy";`
    - all 4 call sites `middleware(request)` -> `proxy(request)`
    - the describe title `"middleware — AIDA-10 auth gate"` -> `"proxy — AIDA-10 auth gate"`
    Also update the file's header comment ("Verifies edge middleware enforces...") to say "edge proxy". Do NOT touch `next.config.ts` — it already uses `experimental.proxyClientMaxBodySize`.

    **(b) SlaDueChip locale hydration fix.**
    In `src/components/tickets/sla-due-chip.tsx`, replace line 15:
    ```ts
    const fullTimestamp = new Date(dueAt).toLocaleString();
    ```
    with an explicit, locale-pinned format so the server (Node ICU default en-US) and the client (OS locale, observed id-ID) produce the same string:
    ```ts
    // Explicit locale + options: a bare toLocaleString() renders en-US on the server and the
    // OS locale on the client, which produced a React hydration mismatch on non-en-US machines.
    const fullTimestamp = new Date(dueAt).toLocaleString("en-US", {
      dateStyle: "medium",
      timeStyle: "short",
    });
    ```

    **(c) Worker import order.**
    In `src/lib/worker/index.ts`, swap lines 1-2 so the value import precedes the type import from the same module:
    ```ts
    import { PgBoss } from "pg-boss";
    import type { Job } from "pg-boss";
    ```
    If biome's organizeImports still complains after the swap, run `node node_modules/@biomejs/biome/bin/biome check --write src/lib/worker/index.ts` and accept its ordering.
  </action>
  <verify>
    <automated>node node_modules/typescript/bin/tsc --noEmit && node node_modules/vitest/vitest.mjs run tests/unit && node node_modules/@biomejs/biome/bin/biome check . && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/middleware.ts` NO LONGER EXISTS; `src/proxy.ts` exists and contains `export function proxy(request: NextRequest)`
    - `src/proxy.ts` still contains `getSessionCookie`, `"/api/public"`, `{ error: "unauthorized" }`, and `export const config = { matcher:`
    - `tests/unit/middleware.test.ts` NO LONGER EXISTS; `tests/unit/proxy.test.ts` exists and contains `from "@/proxy"` and zero occurrences of the identifier `middleware(`
    - `grep -c "toLocaleString(\"en-US\"" src/components/tickets/sla-due-chip.tsx` returns 1 and the file contains `dateStyle: "medium"` and `timeStyle: "short"`
    - `src/lib/worker/index.ts` line 1 is `import { PgBoss } from "pg-boss";`
    - Unit suite passes (all previously-green tests, including the 4 renamed proxy tests)
    - `pnpm run build` succeeds and its output contains NO line matching `middleware.*deprecat` (case-insensitive)
    - `biome check .` and `tsc --noEmit` both exit 0
  </acceptance_criteria>
  <done>The app uses the Next 16 `proxy.ts` convention with unchanged auth behaviour, SlaDueChip no longer hydration-mismatches on non-en-US machines, and the worker entrypoint is lint-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure REQUIREMENTS.md for gsd-tools + close stale STATE.md todos</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/02-core-ticketing/deferred-items.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (all 24 requirement statements — the prose text of each is preserved verbatim, only the bullet prefix changes)
    - .planning/ROADMAP.md "Requirement Coverage" table (the authoritative requirement -> phase mapping you copy into the new traceability table)
    - .planning/phases/02-core-ticketing/deferred-items.md (the two `mark-complete ... not_found` entries this task discharges)
    - .planning/STATE.md "Open Todos" section (the stale entries you are closing)
  </read_first>
  <action>
    **(a) Rewrite `.planning/REQUIREMENTS.md` bullets into checkbox form.** Keep every heading and every requirement's prose EXACTLY as written today; change only the bullet prefix from `- **AIDA-NN** \`MVP\` — ...` to `- [x] **AIDA-NN** \`MVP\` — ...` (complete) or `- [ ] **AIDA-NN** \`MVP\` — ...` (not yet complete).

    Mark these as `[x]` (delivered and validated in Phases 1-6):
    AIDA-01, AIDA-02, AIDA-03, AIDA-04, AIDA-05, AIDA-06, AIDA-07, AIDA-08, AIDA-09, AIDA-10, AIDA-11, AIDA-13, AIDA-14, AIDA-15, AIDA-16, AIDA-17, AIDA-19, AIDA-20, AIDA-21

    Mark these as `[ ]` (this phase closes them):
    AIDA-12, AIDA-22, AIDA-23, AIDA-24

    Mark AIDA-18 as `[ ]` and keep its `\`Stretch\`` tag (backlog, intentionally unshipped).

    **(b) Replace the prose "## Traceability" section** with a machine-parsable table whose third column is exactly `Pending` or `Complete` (the tool's regex requires that literal word). Use the phase mapping from ROADMAP.md's coverage table:

    ```markdown
    ## Traceability

    | Requirement | Phase | Status |
    |---|---|---|
    | AIDA-01 | Phase 2 | Complete |
    | AIDA-02 | Phase 2 | Complete |
    | AIDA-03 | Phase 2 | Complete |
    | AIDA-04 | Phase 2 | Complete |
    | AIDA-05 | Phase 2 | Complete |
    | AIDA-06 | Phase 2 | Complete |
    | AIDA-07 | Phase 2 | Complete |
    | AIDA-08 | Phase 2 | Complete |
    | AIDA-09 | Phase 3 | Complete |
    | AIDA-10 | Phase 1 | Complete |
    | AIDA-11 | Phase 1 | Complete |
    | AIDA-12 | Phase 2,4,7 | Pending |
    | AIDA-13 | Phase 4 | Complete |
    | AIDA-14 | Phase 4 | Complete |
    | AIDA-15 | Phase 5 | Complete |
    | AIDA-16 | Phase 5 | Complete |
    | AIDA-17 | Phase 6 | Complete |
    | AIDA-18 | Backlog | Pending |
    | AIDA-19 | Phase 4 | Complete |
    | AIDA-20 | Phase 4 | Complete |
    | AIDA-21 | Phase 1 | Complete |
    | AIDA-22 | Phase 7 | Pending |
    | AIDA-23 | Phase 7 | Pending |
    | AIDA-24 | Phase 7 | Pending |

    All MVP requirements (AIDA-01 … AIDA-17, AIDA-19 … AIDA-24) are mapped to phases in `ROADMAP.md`; AIDA-18 is backlog. No MVP requirement is left unmapped.
    ```

    **(c) Prove the tool now works.** Run:
    `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" requirements mark-complete AIDA-21`
    AIDA-21 is already `[x]`/`Complete`, so the tool must report it as already-complete (NOT `not_found`). Record the exact JSON output in the SUMMARY. If it reports `not_found`, your formatting is wrong — fix it before continuing.

    **(d) Close the stale bookkeeping.** In `.planning/STATE.md` "### Open Todos":
    - Strike through (`~~...~~`) and annotate the "Consolidation follow-up: dedup 02-07's inline SLA/chip literals" entry with `— DONE (verified 07-01: settings/sla/page.tsx imports DEFAULT_SLA_TARGETS, sla-form.tsx imports PriorityChip, tag-manager.tsx imports TagChip)`.
    - Strike through and annotate the "Rename `src/middleware.ts` → `proxy.ts`" entry with `— DONE in 07-01`.
    - Strike through and annotate the "SlaDueChip `title` hydration mismatch" entry with `— DONE in 07-01 (explicit en-US locale + dateStyle/timeStyle)`.
    In `.planning/phases/02-core-ticketing/deferred-items.md`, append a line under each of the CRLF and `mark-complete not_found` entries: `**RESOLVED in 07-01** (.gitattributes + renormalization / REQUIREMENTS.md checkbox+table restructure).` Do not delete the original entries — the historical record stays.

    Leave the "Disk hygiene: stale agent worktree dirs" todo untouched (user preference: never bulk-delete `.claude/worktrees`).
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" requirements mark-complete AIDA-21 && grep -c "^- \[x\] \*\*AIDA-" .planning/REQUIREMENTS.md && grep -c "| Complete |" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*AIDA-" .planning/REQUIREMENTS.md` returns exactly 19
    - `grep -c "^- \[ \] \*\*AIDA-" .planning/REQUIREMENTS.md` returns exactly 5 (AIDA-12, 18, 22, 23, 24)
    - `.planning/REQUIREMENTS.md` contains a table row matching `| AIDA-22 | Phase 7 | Pending |`
    - `grep -c "| Complete |" .planning/REQUIREMENTS.md` returns exactly 19
    - `gsd-tools requirements mark-complete AIDA-21` output does NOT contain the string `not_found`
    - `.planning/STATE.md` contains `— DONE in 07-01` at least twice
    - `.planning/phases/02-core-ticketing/deferred-items.md` contains `**RESOLVED in 07-01**` at least twice
  </acceptance_criteria>
  <done>REQUIREMENTS.md is in a format `gsd-tools requirements mark-complete` can mutate, its statuses match reality, and the three stale hygiene todos are formally closed.</done>
</task>

</tasks>

<verification>
- `git status --porcelain` is empty; `biome check .` and `tsc --noEmit` both exit 0.
- Unit suite green (including the renamed `tests/unit/proxy.test.ts`).
- `pnpm run build` succeeds with no `middleware` deprecation warning.
- `gsd-tools requirements mark-complete AIDA-21` no longer returns `not_found`.
</verification>

<success_criteria>
The repository is line-ending-clean, on the Next 16 `proxy.ts` convention, free of the SlaDueChip hydration warning and the worker lint violation, and its REQUIREMENTS.md is machine-updatable — giving every remaining Phase 7 plan a noise-free baseline to diff against.
</success_criteria>

<output>
After completion, create `.planning/phases/07-launch-readiness/07-01-SUMMARY.md` recording: the exact `.gitattributes` content shipped, how many files the renormalization commit touched, the proxy rename (old/new paths), the SlaDueChip fix, and the verbatim `gsd-tools requirements mark-complete AIDA-21` output proving the format fix.
</output>
