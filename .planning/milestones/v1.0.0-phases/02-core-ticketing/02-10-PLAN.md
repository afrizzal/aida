---
phase: 02-core-ticketing
plan: 10
type: execute
wave: 3
depends_on: ["02-06"]
files_modified:
  - src/app/(app)/contacts/page.tsx
  - src/app/(app)/contacts/contact-search.tsx
  - src/app/(app)/contacts/[id]/page.tsx
  - src/app/(app)/contacts/[id]/notes-form.tsx
  - src/app/(app)/contacts/[id]/actions.ts
  - src/components/sidebar.tsx
  - src/components/top-bar.tsx
autonomous: true
requirements: [AIDA-03]
must_haves:
  truths:
    - "A searchable Contacts list is reachable from the sidebar and shows name/email/company/ticket-count"
    - "Opening a contact shows their full ticket history (AIDA-03)"
    - "An agent can edit a contact's free-form Notes and they persist (autosave on blur)"
  artifacts:
    - path: "src/app/(app)/contacts/page.tsx"
      provides: "Contacts list (searchable) Server Component"
      contains: "getScopedDb"
    - path: "src/app/(app)/contacts/[id]/page.tsx"
      provides: "Contact detail with ticket history"
      contains: "getScopedDb"
    - path: "src/app/(app)/contacts/[id]/actions.ts"
      provides: "saveContactNotes server action"
      exports: ["saveContactNotes"]
  key_links:
    - from: "src/components/sidebar.tsx"
      to: "/contacts"
      via: "nav item added"
      pattern: "/contacts"
    - from: "contacts/[id]/page.tsx"
      to: "contact.tickets"
      via: "scopedDb include ticket history"
      pattern: "tickets"
---

<objective>
Add the Contacts area (AIDA-03): a searchable Contacts list, a dedicated contact detail page showing that contact's full ticket history, and an editable free-form Notes field with autosave. Wire "Contacts" into the sidebar and top-bar navigation.

Purpose: AIDA-03 requires every ticket link to a contact and a contact view that shows their ticket history. Contacts are auto-created during intake (plan 03) — this surfaces them to agents.
Output: contacts list + detail + notes action + nav integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-SYSTEM.md
@.planning/phases/02-core-ticketing/02-UI-SPEC.md
@src/components/sidebar.tsx
@src/components/top-bar.tsx
@src/lib/session.ts
@src/components/empty-state.tsx

<interfaces>
Sidebar (src/components/sidebar.tsx): `navItems` array of `{ href, label, icon }` rendered as `<Link>`; icons from lucide-react. Add `{ href: "/contacts", label: "Contacts", icon: Users }` (import Users). TopBar (src/components/top-bar.tsx): `pageTitles` record — add `"/contacts": "Contacts"`.
getScopedDb: `const { db } = await getScopedDb();`. Contact schema: { id, name?, email, phone?, company?, notes?, tickets Ticket[] }.
Chips (plan 06): `StatusChip`, `AssigneeAvatar` from `@/components/tickets/*`.
Contact search: reuse Postgres `contains` (case-insensitive) on name/email/company via `db.contact.findMany({ where: { OR: [{ name: { contains: q, mode: "insensitive" } }, { email: { contains: q, mode: "insensitive" } }, { company: { contains: q, mode: "insensitive" } }] } })`.
Ticket-count per contact: `_count: { select: { tickets: true } }` in the findMany select/include.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contacts list + search + nav integration</name>
  <files>src/components/sidebar.tsx, src/components/top-bar.tsx, src/app/(app)/contacts/page.tsx, src/app/(app)/contacts/contact-search.tsx</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Screen Contract §3 — /contacts list rows + empty state copy)
    - src/components/sidebar.tsx (navItems shape) and src/components/top-bar.tsx (pageTitles)
    - src/lib/session.ts (getScopedDb)
  </read_first>
  <action>
Edit `src/components/sidebar.tsx`: import `Users` from lucide-react and add `{ href: "/contacts", label: "Contacts", icon: Users }` to `navItems` (place after Tickets, before Knowledge Base). Edit `src/components/top-bar.tsx`: add `"/contacts": "Contacts"` to `pageTitles`.
Create `src/app/(app)/contacts/page.tsx` (async Server Component): `const { db } = await getScopedDb();` read `q` searchParam; `db.contact.findMany({ where: q ? { OR: [name/email/company contains insensitive] } : {}, include: { _count: { select: { tickets: true } } }, orderBy: { updatedAt: "desc" }, take: 100 })`. Render a `<ContactSearch/>` input above a flat `divide-y divide-border` list. Each row (a `<Link href={\`/contacts/${id}\`}>`): `AssigneeAvatar`-style initials avatar (`bg-primary/10 text-primary`) + name (`text-[14px] font-medium`) + email (`text-[12px] text-muted-foreground`) + company (`text-[12px] text-muted-foreground hidden md:block`) + `Badge variant="secondary"` ticket-count (e.g. "3 tickets") + last-activity timestamp right-aligned. Empty state: `EmptyState` with `Users` icon, heading `No contacts yet`, body `Contacts are created automatically when someone submits a ticket. They'll show up here once your first request comes in.`
Create `src/app/(app)/contacts/contact-search.tsx` (Client): `Input` with `Search` prefix, placeholder `Search contacts…`, debounced → `router.replace(?q=)`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sidebar.tsx` navItems contains `href: "/contacts"` with the `Users` icon
    - `src/components/top-bar.tsx` pageTitles contains `"/contacts": "Contacts"`
    - `contacts/page.tsx` uses `getScopedDb`, filters on name/email/company with `mode: "insensitive"`, and includes `_count` of tickets
    - Empty-state copy is exactly `No contacts yet` heading
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Contacts is a sidebar destination; the list is searchable and shows per-contact ticket counts.</done>
</task>

<task type="auto">
  <name>Task 2: Contact detail + ticket history + Notes autosave</name>
  <files>src/app/(app)/contacts/[id]/page.tsx, src/app/(app)/contacts/[id]/notes-form.tsx, src/app/(app)/contacts/[id]/actions.ts</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Screen Contract §3 — /contacts/[id] header card, ticket history compact rows, Notes)
    - src/app/(app)/settings/actions.ts (scopedDb server-action + revalidatePath idiom)
    - src/components/tickets/status-chip.tsx (plan 06)
  </read_first>
  <action>
Create `src/app/(app)/contacts/[id]/page.tsx` (async Server Component): `const { db } = await getScopedDb(); const { id } = await params;` `db.contact.findFirst({ where: { id }, include: { tickets: { orderBy: { createdAt: "desc" }, select: { id, number, subject, status, createdAt } } } })`. If not found → EmptyState. Header `Card p-6`: large initials `Avatar` + name (`text-[18px] font-semibold`) + email/phone/company as a compact `dl` (`text-[13px]`) + `<NotesForm contactId defaultValue={contact.notes}/>`. Below: `Tickets from {name}` heading (`text-[14px] font-medium text-muted-foreground`) then a compact ticket-history list — each a `<Link href={\`/tickets/${t.id}\`}>` row: `#{number}` + subject (truncate) + `<StatusChip status={t.status}/>` + relative timestamp (1-line, `divide-y`). If zero tickets: inline text `No tickets from this contact yet.` (`text-[13px] text-muted-foreground py-4`).
Create `src/app/(app)/contacts/[id]/notes-form.tsx` (Client): a `Textarea` labeled `Notes` that calls `saveContactNotes(contactId, value)` on blur (autosave); show a subtle "Saved" affordance via `sonner` or inline text.
Create `src/app/(app)/contacts/[id]/actions.ts` ("use server"): `saveContactNotes(contactId, notes)` → `const { db } = await getScopedDb(); await db.contact.update({ where: { id: contactId }, data: { notes } }); revalidatePath(\`/contacts/${contactId}\`);`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `contacts/[id]/page.tsx` includes `tickets` ordered by `createdAt desc` and renders `StatusChip` per history row
    - `contacts/[id]/actions.ts` exports `saveContactNotes` using `getScopedDb` + `revalidatePath`
    - Zero-ticket contact shows inline copy `No tickets from this contact yet.`
    - `pnpm exec tsc --noEmit` exits 0 and `pnpm run build` completes
  </acceptance_criteria>
  <done>Opening a contact shows their full ticket history and an editable, autosaving Notes field — satisfying AIDA-03.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` clean; `pnpm run build` succeeds.
- Sidebar + top-bar expose Contacts; list searchable; detail shows ticket history + autosaving notes.
</verification>

<success_criteria>
Every ticket's contact is browsable: agents can search contacts, open a contact to see all their tickets, and maintain free-form notes — AIDA-03 fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ticketing/02-10-SUMMARY.md`.
</output>
