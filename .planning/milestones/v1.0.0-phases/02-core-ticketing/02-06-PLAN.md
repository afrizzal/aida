---
phase: 02-core-ticketing
plan: 06
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/components/tickets/status-chip.tsx
  - src/components/tickets/priority-chip.tsx
  - src/components/tickets/sla-due-chip.tsx
  - src/components/tickets/tag-chip.tsx
  - src/components/tickets/attachment-chip.tsx
  - src/components/tickets/assignee-avatar.tsx
  - src/lib/tickets/format-duration.ts
autonomous: true
requirements: [AIDA-01, AIDA-06]
must_haves:
  truths:
    - "Each ticket status/priority/SLA state renders as a token-mapped chip matching the UI-SPEC exactly"
    - "The SLA due chip shows On-track / At-risk / Overdue with the correct icon and coarse relative duration"
    - "Internal-note and warning surfaces use amber (--warning); overdue/urgent use --destructive; resolved uses --success"
  artifacts:
    - path: "src/components/tickets/status-chip.tsx"
      provides: "StatusChip for 5 statuses"
      exports: ["StatusChip"]
    - path: "src/components/tickets/sla-due-chip.tsx"
      provides: "SlaDueChip 3-state (on-track/at-risk/overdue)"
      exports: ["SlaDueChip"]
    - path: "src/components/tickets/priority-chip.tsx"
      provides: "PriorityChip 4-level"
      exports: ["PriorityChip"]
  key_links:
    - from: "chips"
      to: "design tokens"
      via: "token-only classes (bg-warning/10, bg-success/10, bg-destructive/10, bg-primary)"
      pattern: "bg-warning/10|bg-success/10"
---

<objective>
Build the reusable presentational chip + avatar components exactly per the UI-SPEC "Reusable Chip Components" contract: `StatusChip`, `PriorityChip`, `SlaDueChip`, `TagChip`, `AttachmentChip`, `AssigneeAvatar`, plus a coarse relative-duration formatter for SLA labels. Pure token-only styling — no hardcoded colors.

Purpose: These chips are the visual vocabulary of the inbox, reading pane, contacts, and public status page. Centralizing them (built once against the design tokens from plan 02) guarantees the amber/emerald/destructive semantics are consistent everywhere and satisfies the DESIGN-SYSTEM token-only rule.
Output: 6 components + format-duration helper, all typed and design-token-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-SYSTEM.md
@.planning/phases/02-core-ticketing/02-UI-SPEC.md
@src/components/ui/badge.tsx
@src/components/ui/avatar.tsx
@src/lib/utils.ts

<interfaces>
Badge (src/components/ui/badge.tsx): `<Badge variant="secondary|outline|warning|success|destructive|default" className=...>` — h-5 px-2 py-0.5, `[&>svg]:size-3!`. `warning`/`success` variants added in plan 02.
Avatar (src/components/ui/avatar.tsx): `<Avatar className="size-6"><AvatarFallback className="bg-primary/10 text-primary text-[12px] font-medium">AB</AvatarFallback></Avatar>` — sidebar fallback pattern.
Lucide icons available: Clock, AlertTriangle, CircleAlert, Lock, Paperclip, UserPlus, X.
cn() from @/lib/utils for class merging. Enum string types: import `TicketStatus`, `TicketPriority` from `@/generated/prisma/client` (or accept lowercased string unions — prefer the generated enum types).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusChip + PriorityChip + format-duration + SlaDueChip</name>
  <files>src/lib/tickets/format-duration.ts, src/components/tickets/status-chip.tsx, src/components/tickets/priority-chip.tsx, src/components/tickets/sla-due-chip.tsx</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (Reusable Chip Components — StatusChip/PriorityChip/SlaDueChip exact class tables)
    - src/components/ui/badge.tsx (variants + sizing)
  </read_first>
  <action>
Create `src/lib/tickets/format-duration.ts`: `export function formatDueDuration(dueAt: Date | string): string` returning coarse relative labels — compute ms delta from now; if < 60m → `${m}m`, if < 24h → `${h}h`, else `${d}d`. Used for the on-track "Due in {duration}" label.

Create `src/components/tickets/status-chip.tsx` — `StatusChip({ status }: { status: TicketStatus })`. Map (UI-SPEC exact):
- NEW → `bg-primary text-primary-foreground`, label "New"
- OPEN → `bg-primary/10 text-primary border border-primary/20`, "Open"
- PENDING → `bg-warning/10 text-warning border border-warning/20`, "Pending"
- RESOLVED → `bg-success/10 text-success border border-success/20`, "Resolved"
- CLOSED → `bg-muted text-muted-foreground border border-border`, "Closed"
Render a `<Badge className={cn(mapClasses)}>` with no icon, `h-5 px-2 py-0.5 text-[12px]`.

Create `src/components/tickets/priority-chip.tsx` — `PriorityChip({ priority }: { priority: TicketPriority })`. Map:
- LOW → `border border-border text-muted-foreground`, "Low"
- NORMAL → `border border-border text-foreground`, "Normal"
- HIGH → `bg-warning/10 text-warning border border-warning/20`, "High"
- URGENT → `bg-destructive/10 text-destructive border border-destructive/20`, "Urgent"

Create `src/components/tickets/sla-due-chip.tsx` — `SlaDueChip({ dueAt, isAtRisk, isBreached }: { dueAt: Date|string; isAtRisk: boolean; isBreached: boolean })`. Precedence: breached → overdue; else atRisk → at-risk; else on-track.
- On track → `bg-muted text-muted-foreground border border-border`, `<Clock className="size-3"/>` + `Due in {formatDueDuration(dueAt)}`
- At risk → `bg-warning/10 text-warning border border-warning/20`, `<AlertTriangle className="size-3"/>` + "At risk"
- Overdue → `bg-destructive/10 text-destructive border border-destructive/20`, `<CircleAlert className="size-3"/>` + "Overdue"
Add native `title={new Date(dueAt).toLocaleString()}` for full timestamp on hover.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `status-chip.tsx` maps all 5 statuses; contains `bg-success/10 text-success` (RESOLVED) and `bg-primary text-primary-foreground` (NEW)
    - `priority-chip.tsx` contains `bg-destructive/10 text-destructive` (URGENT) and `bg-warning/10 text-warning` (HIGH)
    - `sla-due-chip.tsx` contains `Clock`, `AlertTriangle`, `CircleAlert` and the label `Due in`
    - No file in this task contains a raw hex or `oklch(` literal (token-only)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Status/priority/SLA chips render every state with exact UI-SPEC token classes; duration labels are coarse.</done>
</task>

<task type="auto">
  <name>Task 2: TagChip + AttachmentChip + AssigneeAvatar</name>
  <files>src/components/tickets/tag-chip.tsx, src/components/tickets/attachment-chip.tsx, src/components/tickets/assignee-avatar.tsx</files>
  <read_first>
    - .planning/phases/02-core-ticketing/02-UI-SPEC.md (TagChip, AttachmentChip, Assignee Avatar sections)
    - src/components/ui/avatar.tsx and src/components/sidebar.tsx (avatar fallback pattern)
  </read_first>
  <action>
Create `src/components/tickets/tag-chip.tsx` — `TagChip({ label, onRemove }: { label: string; onRemove?: () => void })`: `<Badge variant="secondary" className="rounded-full">` with the label; if `onRemove` provided, render an inline `<button aria-label={\`Remove tag ${label}\`}>` with `<X className="size-3"/>`. Also export `TagOverflowChip({ count }: { count: number })` as `<Badge variant="outline">+{count}</Badge>`.
Create `src/components/tickets/attachment-chip.tsx` — `AttachmentChip({ filename, sizeLabel, href, onRemove }: {...})`: `bg-muted border border-border rounded-md px-2 py-1 text-[12px] flex items-center gap-1.5` with `<Paperclip className="size-3"/>`, truncated filename + `({sizeLabel})`. If `href`, wrap as an anchor opening a new tab; if `onRemove`, show an `X` remove button (unsent state). Include `export function formatBytes(n: number): string` (KB/MB) for the size label.
Create `src/components/tickets/assignee-avatar.tsx` — `AssigneeAvatar({ name, size = "sm" }: { name?: string | null; size?: "sm" | "default" })`. If `name`: `<Avatar className={size==="sm"?"size-6":"size-8"}><AvatarFallback className="bg-primary/10 text-primary text-[12px] font-medium">{initials}</AvatarFallback></Avatar>` (initials helper: first letters of up to 2 words, uppercase). If unassigned (no name): dashed placeholder `size-6 rounded-full border border-dashed border-border` containing `<UserPlus className="size-3 text-muted-foreground"/>`, `title="Unassigned"`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tag-chip.tsx` exports `TagChip` and renders `aria-label="Remove tag ` when removable; exports `TagOverflowChip`
    - `attachment-chip.tsx` exports `AttachmentChip` and `formatBytes`; contains `Paperclip`
    - `assignee-avatar.tsx` renders the dashed `border-dashed` `UserPlus` placeholder when `name` is empty and `title="Unassigned"`
    - No hardcoded hex/oklch literals
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Tag, attachment, and assignee-avatar chips (incl. unassigned placeholder) render per UI-SPEC, token-only, with correct aria-labels.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` clean.
- All 6 components + format-duration exist; every color class is a design token (no hex/oklch literals).
- SlaDueChip precedence: breached > at-risk > on-track.
</verification>

<success_criteria>
A complete, token-only chip vocabulary exists for every ticket state and is ready to be composed into the inbox, reading pane, contacts, and public pages — satisfying the DESIGN-SYSTEM token-only rule.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ticketing/02-06-SUMMARY.md`.
</output>
