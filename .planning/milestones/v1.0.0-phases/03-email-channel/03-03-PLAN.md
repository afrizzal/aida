---
phase: 03-email-channel
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/markdown/render.ts
  - src/lib/channels/email/parse-body.ts
  - src/lib/channels/email/thread-match.ts
  - src/lib/channels/email/auto-generated.ts
  - tests/unit/sanitize-email-html.test.ts
  - tests/unit/email-parse-body.test.ts
  - tests/unit/email-thread-match.test.ts
autonomous: true
requirements: [AIDA-09]
must_haves:
  truths:
    - "Inbound email HTML is sanitized through the SAME module as renderMarkdown() — one sanitization authority, no second dangerouslySetInnerHTML pass (D-16)."
    - "Remote images (http/https img src) are stripped from inbound HTML so tracking pixels never fire when an agent opens a ticket (D-18)."
    - "An inbound email's body is reduced to a sanitized HTML part and a plain-text/markdown part (html-to-text fallback when only HTML exists) (D-17)."
    - "Threading candidates are derived: header Message-IDs (In-Reply-To/References) first, then a [#N] subject-token fallback (D-08/D-09)."
    - "Auto-generated mail (RFC 3834 / Precedence / bounce / List-Id) is detected as a pure predicate over parsed headers (D-12)."
  artifacts:
    - path: "src/lib/markdown/render.ts"
      provides: "sanitizeEmailHtml() alongside renderMarkdown(), reusing the shared sanitize schema"
      contains: "export function sanitizeEmailHtml"
    - path: "src/lib/channels/email/parse-body.ts"
      provides: "extractEmailBody(parsed) → { rawHtml, text }"
      contains: "extractEmailBody"
    - path: "src/lib/channels/email/thread-match.ts"
      provides: "collectCandidateMessageIds, extractSubjectTicketNumber, findTicketIdByEmailMessageIds, findTicketByNumber"
      contains: "extractSubjectTicketNumber"
    - path: "src/lib/channels/email/auto-generated.ts"
      provides: "isAutoGenerated(parsed) predicate"
      contains: "isAutoGenerated"
  key_links:
    - from: "sanitizeEmailHtml"
      to: "rehype-sanitize shared schema + remote-image strip"
      via: "unified rehype-parse → strip-remote-images → rehype-sanitize → stringify"
      pattern: "rehype-parse"
    - from: "extractEmailBody"
      to: "html-to-text convert()"
      via: "plain-text fallback when no text/plain part"
      pattern: "html-to-text"
---

<objective>
Build the pure, unit-testable parsing primitives the inbound pipeline (plan 04) composes:
`sanitizeEmailHtml()` (added to the ONE sanitization module, with remote-image stripping),
`extractEmailBody()` (HTML + plain-text/markdown reduction with html-to-text fallback),
`thread-match` helpers (header + `[#N]` subject-token candidates), and `isAutoGenerated()`
(auto-reply/bounce detection).

Purpose: Isolate every RFC/sanitization decision into small tested functions so the plan-04 ingest
orchestrator is thin wiring, not a place where sanitization or threading logic is re-derived. These
are all pure (or db-param) helpers with clear input→output, executed TDD-first.
Output: Four modules + three unit-test files.

Runs in Wave 2 in parallel with 03-05 (outbound) — no shared files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-email-channel/03-CONTEXT.md
@.planning/phases/03-email-channel/03-RESEARCH.md

# The single sanitization authority this plan extends (do NOT create a second pipeline):
@src/lib/markdown/render.ts
# Existing tests/unit shape:
@tests/unit/markdown-render.test.ts

<interfaces>
<!-- mailparser's ParsedMail (from @types/mailparser, installed in 03-01) is the input type for
     parse-body / thread-match / auto-generated. Key fields the executor uses: -->
```ts
// from "mailparser"
interface ParsedMail {
  subject?: string;
  messageId?: string;           // bracketed, e.g. "<abc@host>"
  inReplyTo?: string;           // bracketed
  references?: string[] | string;
  html?: string | false;
  text?: string;
  textAsHtml?: string;
  from?: { value: { address?: string; name?: string }[] };
  headers: Map<string, unknown>;      // lowercased keys
  headerLines: { key: string; line: string }[];
  attachments: { cid?: string; content: Buffer; filename?: string; contentType: string; related?: boolean }[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sanitizeEmailHtml() + remote-image stripping (TDD)</name>
  <files>src/lib/markdown/render.ts, tests/unit/sanitize-email-html.test.ts</files>
  <read_first>
    - src/lib/markdown/render.ts (the existing `schema`, `rehypeSafeLinks` plugin, and `renderMarkdown` — REUSE the same schema; add the new export in THIS file, never a second module — STATE.md 02-02 "one sanitization authority")
    - .planning/phases/03-email-channel/03-RESEARCH.md (D-16/D-18; Anti-Patterns "second sanitize pass" / "trusting mailparser html for tracking-pixel safety")
    - .planning/phases/03-email-channel/03-CONTEXT.md (D-18 strip remote images; D-19 cid images are rewritten to same-origin BEFORE this sanitize runs, so relative/same-origin img src must survive)
  </read_first>
  <behavior>
    - sanitizeEmailHtml('<img src="http://tracker.example/pixel.gif">') → output contains NO `http://` and NO `<img` with that remote src (remote image stripped).
    - sanitizeEmailHtml('<img src="https://x/y.png">') → https remote image also stripped.
    - sanitizeEmailHtml('<img src="/api/attachments/abc123">') → the relative same-origin img src is PRESERVED (cid images are pre-rewritten to this shape by plan 04).
    - sanitizeEmailHtml('<script>alert(1)</script><b onclick="x">hi</b>') → no `<script>`, no `onclick` (schema strips them).
    - sanitizeEmailHtml('<a href="https://ok">t</a>') → link retains `rel="nofollow noopener noreferrer"` and `target="_blank"` (reuses rehypeSafeLinks behavior).
  </behavior>
  <action>
    In `src/lib/markdown/render.ts`, add (do not remove/relocate the existing `renderMarkdown`):
    - `import rehypeParse from "rehype-parse";` (installed in 03-01).
    - A new unified plugin `rehypeStripRemoteImages()` (mirrors the existing `rehypeSafeLinks` shape using
      `visit(tree, "element", ...)` from `unist-util-visit`): for every `img` element whose
      `properties.src` is a string starting with `http://` or `https://` (case-insensitive), delete the
      node's `src` (set to undefined) so the sanitize pass drops the now-src-less image — this guarantees
      remote tracking pixels never load (D-18). Leave relative/`/api/...` src untouched.
    - `export function sanitizeEmailHtml(html: string): string` built from a NEW processor that parses raw
      HTML (not markdown): `unified().use(rehypeParse, { fragment: true }).use(rehypeStripRemoteImages)
      .use(rehypeSafeLinks).use(rehypeSanitize, schema).use(rehypeStringify)` and returns
      `String(processor.processSync(html))`. REUSE the existing `schema` const and `rehypeSafeLinks` /
      `rehypeStringify` already imported in this file — do not fork the schema.

    Note (design-system scope): this HTML is agent-facing (rendered inside the app), so token/schema rules
    apply here. The SEPARATE outbound email wrapper (plan 05 `wrapEmailSafeHtml`) is NOT governed by this —
    it uses inline literal styles for third-party mail clients (03-UI-SPEC scope note).

    Write `tests/unit/sanitize-email-html.test.ts` covering all five behaviors above.
  </action>
  <verify>
    <automated>grep -q 'export function sanitizeEmailHtml' src/lib/markdown/render.ts && grep -q 'rehype-parse' src/lib/markdown/render.ts && grep -q 'rehypeStripRemoteImages' src/lib/markdown/render.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/markdown/render.ts` exports `sanitizeEmailHtml` and still exports `renderMarkdown`
    - `src/lib/markdown/render.ts` imports `rehype-parse` and defines a remote-image-stripping plugin (grep `rehypeStripRemoteImages`)
    - The existing `schema` const is reused (no second `defaultSchema` spread is added — grep count of `defaultSchema` stays at 1)
    - `tests/unit/sanitize-email-html.test.ts` asserts a `http://` image is stripped and a `/api/attachments/` image survives
    - `pnpm test` exits 0
  </acceptance_criteria>
  <done>Inbound HTML sanitizes through the one shared module, remote images are stripped, same-origin (cid-rewritten) images survive, and scripts/handlers are removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extractEmailBody() — HTML + plain-text/markdown reduction (TDD)</name>
  <files>src/lib/channels/email/parse-body.ts, tests/unit/email-parse-body.test.ts</files>
  <read_first>
    - .planning/phases/03-email-channel/03-CONTEXT.md (D-17: HTML → sanitizeEmailHtml → bodyHtml; text/plain OR html-to-text fallback → bodyMarkdown used for FTS + reply quoting; plain-text-only email uses the existing renderMarkdown path)
    - .planning/phases/03-email-channel/03-RESEARCH.md (html-to-text `convert()` usage; Don't-Hand-Roll "HTML→plain-text")
    - src/lib/markdown/render.ts (renderMarkdown — the plain-text-only path)
  </read_first>
  <behavior>
    - extractEmailBody({ html: '<p>Hi <b>there</b></p>', text: 'Hi there' }) → { rawHtml: '<p>Hi <b>there</b></p>', text: 'Hi there' } (uses provided text/plain verbatim).
    - extractEmailBody({ html: '<p>Only HTML here</p>', text: undefined }) → { rawHtml: '<p>Only HTML here</p>', text: 'Only HTML here' } (text derived via html-to-text; tags stripped, non-empty).
    - extractEmailBody({ html: false, text: 'plain body' }) → { rawHtml: null, text: 'plain body' }.
  </behavior>
  <action>
    Create `src/lib/channels/email/parse-body.ts`. Import types + lib with BARE specifiers (safe for both
    esbuild + webpack): `import type { ParsedMail } from "mailparser";` and
    `import { convert } from "html-to-text";`.

    `export function extractEmailBody(parsed: Pick<ParsedMail, "html" | "text">): { rawHtml: string | null; text: string }`:
    - `const rawHtml = typeof parsed.html === "string" ? parsed.html : null;`
    - `const text = parsed.text?.trim() ? parsed.text : (rawHtml ? convert(rawHtml, { wordwrap: false }) : "");`
    - return `{ rawHtml, text }`.

    Do NOT sanitize here and do NOT save attachments here — the plan-04 ingest orchestrator does cid
    rewriting + `sanitizeEmailHtml()` on `rawHtml` (it needs the saved attachment ids first), and calls
    `renderMarkdown(text)` for the plain-text-only path. This function only reduces the two body parts.
  </action>
  <verify>
    <automated>grep -q 'export function extractEmailBody' src/lib/channels/email/parse-body.ts && grep -q 'html-to-text' src/lib/channels/email/parse-body.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/channels/email/parse-body.ts` exports `extractEmailBody` and imports `convert` from `html-to-text`
    - `tests/unit/email-parse-body.test.ts` covers the html+text, html-only (fallback), and text-only cases
    - The html-only case asserts the returned `text` is non-empty and contains no `<` tag characters
    - `pnpm test` exits 0
  </acceptance_criteria>
  <done>Any inbound email reduces to a rawHtml part (or null) and a usable plain-text/markdown part with an html-to-text fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: thread-match helpers + isAutoGenerated() (TDD)</name>
  <files>src/lib/channels/email/thread-match.ts, src/lib/channels/email/auto-generated.ts, tests/unit/email-thread-match.test.ts</files>
  <read_first>
    - .planning/phases/03-email-channel/03-RESEARCH.md (Pattern 3 threading match; Pattern 6 isAutoGenerated code — copy the predicate verbatim; Pitfall 1 bracket-consistency; "Threading lookup query shape")
    - .planning/phases/03-email-channel/03-CONTEXT.md (D-08 header match; D-09 [#N] subject token; D-12 auto-generated detection set; D-13 auto mail excluded from creation/reopen)
    - src/lib/scoped-db.ts (the scopedDb client the db-querying helpers receive as a param)
  </read_first>
  <behavior>
    - extractSubjectTicketNumber("Re: Login broken [#42]") === 42; extractSubjectTicketNumber("no token here") === null.
    - collectCandidateMessageIds({ inReplyTo: "<a@h>", references: ["<a@h>", "<b@h>"] }) === ["<a@h>", "<b@h>"] (falsy filtered, deduped, order preserved).
    - isAutoGenerated: header "auto-submitted: auto-replied" → true; "precedence: bulk" → true; return-path "<>" → true; content-type value "multipart/report" → true; a List-Id header present → true; an ordinary human reply (auto-submitted absent/"no", no precedence) → false.
  </behavior>
  <action>
    Create `src/lib/channels/email/auto-generated.ts`:
    - `import type { ParsedMail } from "mailparser";`
    - `export function isAutoGenerated(parsed: ParsedMail): boolean` — implement EXACTLY 03-RESEARCH.md
      Pattern 6: check `auto-submitted !== "no"`, presence of `x-auto-response-suppress`, `precedence` in
      `["bulk","junk","auto_reply","list"]`, `return-path` trimmed === `"<>"`, content-type value ===
      `"multipart/report"`, and List-Id via BOTH `parsed.headers.get("list")?.id` AND a
      `parsed.headerLines.some(h => h.key === "list-id")` fallback (Open Question 1 — defensive dual check).

    Create `src/lib/channels/email/thread-match.ts`:
    - `import type { ParsedMail } from "mailparser";`
    - `export function extractSubjectTicketNumber(subject: string | undefined): number | null` — regex
      `/\[#(\d+)\]/`, return `Number(match[1])` or null.
    - `export function collectCandidateMessageIds(parsed: Pick<ParsedMail,"inReplyTo"|"references">): string[]`
      — `[parsed.inReplyTo, ...(Array.isArray(parsed.references) ? parsed.references : parsed.references ? [parsed.references] : [])]`,
      filter falsy, dedupe preserving order.
    - `export async function findTicketIdByEmailMessageIds(db, candidateIds: string[]): Promise<string | null>`
      — if empty return null; `const m = await db.message.findFirst({ where: { emailMessageId: { in: candidateIds } }, select: { ticketId: true } });` return `m?.ticketId ?? null` (scopedDb auto-injects organizationId).
    - `export async function findTicketByNumber(db, number: number): Promise<{ id: string; status: string; contactId: string } | null>`
      — `db.ticket.findFirst({ where: { number }, select: { id: true, status: true, contactId: true } })`.
    Type the `db` param loosely (`db: { message: { findFirst: (a: unknown) => Promise<{ ticketId: string } | null> }; ticket: { findFirst: (a: unknown) => Promise<{ id: string; status: string; contactId: string } | null> } }`) to stay bundling-context-agnostic. Use no `@/` imports (worker-bundleable).

    Write `tests/unit/email-thread-match.test.ts` covering the three pure functions (isAutoGenerated,
    extractSubjectTicketNumber, collectCandidateMessageIds). The db-querying helpers are exercised by the
    plan-04 integration test.
  </action>
  <verify>
    <automated>grep -q 'export function isAutoGenerated' src/lib/channels/email/auto-generated.ts && grep -q 'extractSubjectTicketNumber' src/lib/channels/email/thread-match.ts && grep -q '\[#' src/lib/channels/email/thread-match.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/channels/email/auto-generated.ts` exports `isAutoGenerated` and checks `auto-submitted`, `precedence`, return-path `<>`, `multipart/report`, and List-Id (dual check)
    - `src/lib/channels/email/thread-match.ts` exports `extractSubjectTicketNumber`, `collectCandidateMessageIds`, `findTicketIdByEmailMessageIds`, `findTicketByNumber`
    - Neither file imports via `@/` (grep finds no `@/` — both are worker-bundleable)
    - `tests/unit/email-thread-match.test.ts` asserts an auto-replied header → true, a normal reply → false, and `[#42]` → 42
    - `pnpm test` exits 0
  </acceptance_criteria>
  <done>Threading candidates and auto-generated detection are pure, tested functions ready for the ingest orchestrator.</done>
</task>

</tasks>

<verification>
- `pnpm test` passes (sanitize-email-html, email-parse-body, email-thread-match unit suites green).
- `render.ts` exports `sanitizeEmailHtml`; a `http://` image is stripped and a `/api/attachments/` image survives.
- The three channels/email helper files import no `@/` paths (worker-bundleable) and `pnpm exec tsc --noEmit` is clean.
</verification>

<success_criteria>
Every RFC/sanitization decision for inbound mail lives in a small tested function: HTML is sanitized
through the one authority with remote images stripped; bodies reduce to html + text; threading
candidates and auto-generated detection are ready to wire.
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-channel/03-03-SUMMARY.md`. Record the exact
sanitizeEmailHtml pipeline order (parse → strip-remote-images → safe-links → sanitize → stringify),
that cid images must be pre-rewritten to `/api/attachments/{id}` by the ingest step to survive
sanitization, and the thread-match candidate ordering.
</output>
