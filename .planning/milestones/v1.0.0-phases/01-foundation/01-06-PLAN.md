---
phase: 01-foundation
plan: 6
type: execute
wave: 4
depends_on: [2, 3, 5]
files_modified:
  - src/app/(app)/layout.tsx
  - src/components/sidebar.tsx
  - src/components/top-bar.tsx
  - src/components/user-menu.tsx
  - src/components/theme-toggle.tsx
  - src/components/empty-state.tsx
  - src/app/(app)/tickets/page.tsx
  - src/app/(app)/kb/page.tsx
  - src/app/(app)/settings/page.tsx
  - src/app/(app)/settings/ai-toggle.tsx
  - src/app/(app)/settings/actions.ts
autonomous: true
requirements: [AIDA-10]
must_haves:
  truths:
    - "Visiting any (app) route while unauthenticated is redirected to /login by the server-side layout guard (defense in depth with middleware)"
    - "The app shell renders a 240px sidebar (Tickets, Knowledge Base, Settings), a 56px top bar (page title, theme toggle, user menu), with the active nav item highlighted"
    - "The theme toggle switches light/dark via next-themes and persists"
    - "The user menu shows the signed-in user and a working Sign out action"
    - "The Settings page shows an Enable AI switch (default off) that persists aiEnabled to the org-scoped Setting via scopedDb"
    - "Tickets and Knowledge Base render screenshot-worthy empty states"
  artifacts:
    - path: "src/app/(app)/layout.tsx"
      provides: "Auth-gated persistent shell (sidebar + top bar), active-org resolution"
      contains: "requireSession"
    - path: "src/components/sidebar.tsx"
      provides: "240px nav: Tickets/KB/Settings with active-state highlighting + user area"
      contains: "Knowledge Base"
    - path: "src/components/top-bar.tsx"
      provides: "56px top bar: page title + theme toggle + user menu"
      min_lines: 15
    - path: "src/app/(app)/settings/actions.ts"
      provides: "Server Action persisting aiEnabled via scopedDb (org-scoped Setting)"
      contains: "use server"
    - path: "src/components/empty-state.tsx"
      provides: "Reusable centered empty-state (icon + heading + body)"
      exports: ["EmptyState"]
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/lib/session.ts"
      via: "requireSession() server-side guard + active-org resolution"
      pattern: "requireSession"
    - from: "src/app/(app)/settings/actions.ts"
      to: "org-scoped Setting"
      via: "getScopedDb().db.setting.upsert key aiEnabled"
      pattern: "aiEnabled"
    - from: "src/components/theme-toggle.tsx"
      to: "next-themes"
      via: "useTheme().setTheme"
      pattern: "setTheme"
    - from: "src/components/user-menu.tsx"
      to: "Better Auth signOut"
      via: "authClient.signOut()"
      pattern: "signOut"
---

<objective>
Build the authenticated application shell — the "full shell, empty rooms" deliverable (D-22, D-24, D-25): a server-guarded `(app)` layout with a persistent 240px sidebar (Tickets / Knowledge Base / Settings) and 56px top bar (page title, theme toggle, user menu), stub pages with screenshot-worthy empty states, and a Settings page whose AI toggle is the only AI artifact in Phase 1 (D-18) — persisted to the org-scoped `Setting` via `scopedDb`.

Purpose: Deliver the screenshot-ready product surface for the README hero (repo health = star driver) and enforce auth server-side in the layout (AIDA-10, defense-in-depth with middleware), without over-building UI that Phase 2 reshapes.
Output: A navigable, themeable, signed-in app shell with three stub destinations and a working AI toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-RESEARCH.md

<critical_decisions>
- D-18 (LOCKED): zero LLM code. The ONLY AI artifact is the aiEnabled toggle (default false) stored in the org-scoped Setting. No lib/llm.
- D-22/D-24/D-25 (LOCKED): auth-gated (app) route group with persistent sidebar + top bar; nav stubs Tickets / Knowledge Base / Settings -> empty pages; "full shell, empty rooms"; dark mode already wired in Plan 01.
- D-15 (LOCKED): the Settings AI toggle MUST persist through scopedDb (org-scoped) — use getScopedDb() from Plan 03. Never write the org-scoped Setting with the bare client.
- SECURITY.md: the (app) layout does the AUTHORITATIVE server-side session check (middleware is the optimistic edge gate). Both exist (defense in depth).
- UI-SPEC is the visual contract — follow it exactly (tokens, spacing, copy, two font weights only, icons, aria-labels).
</critical_decisions>

<ui_spec_quickref>
<!-- From 01-UI-SPEC.md — implement EXACTLY. -->
- Sidebar: 240px fixed, bg-muted, "AIDA" wordmark (18px/600, 16px padding) at top. Nav items 36px tall, icon h-4 w-4 gap-2 before label, body 14px. Active = bg-primary text-primary-foreground; hover = hover:bg-accent hover:text-accent-foreground. User area at bottom (avatar + name + truncated email, 16px padding).
- Nav icons (Lucide): Inbox (Tickets), BookOpen (Knowledge Base), Settings2 (Settings).
- Top bar: 56px fixed, bg-background, 1px bottom border. Left = page title (18px/600). Right = theme toggle icon button (Sun/Moon 20px, tooltip "Switch to light mode"/"Switch to dark mode") + user dropdown.
- User menu trigger: <Button variant="ghost" aria-label="Open user menu"> wrapping <Avatar>. Dropdown item "Sign out".
- Stub empty state: centered, Lucide icon h-12 w-12 above heading (18px/600) above body (14px/400 muted).
- Copy: Tickets "Your inbox is empty" / "New tickets from customers will appear here. Set up an email channel or embed a web form to start receiving requests." | KB "No articles yet" / "Create knowledge base articles to help your team answer customer questions consistently." | Settings AI section "AI Features", toggle label "Enable AI", description "Allow AIDA to triage tickets and draft replies. Configure your AI provider in a future step."
- Two font weights only: font-normal (400) and font-semibold (600). NO font-medium/font-bold.
</ui_spec_quickref>
</context>

<interfaces>
<!-- CONSUMED -->
import { requireSession, getCurrentSession, getScopedDb } from "@/lib/session";  // Plan 03
import { authClient } from "@/lib/auth-client";   // Plan 02 — authClient.signOut()
// shadcn components (Plan 01): button, avatar, dropdown-menu, separator, switch, tooltip, badge, card
// Lucide icons: Inbox, BookOpen, Settings2, Sun, Moon, Loader2
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Auth-gated (app) layout + sidebar + top bar (theme toggle + user menu)</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (App Shell — Sidebar, App Shell — Top Bar, Implementation Notes 5,6,8,9,10)
    - src/lib/session.ts (requireSession, getCurrentSession — Plan 03)
    - src/lib/auth-client.ts (authClient.signOut — Plan 02)
    - src/components/theme-provider.tsx (Plan 01) and src/components/ui (dropdown-menu, avatar, button, tooltip, separator)
  </read_first>
  <action>
    1. Create src/app/(app)/layout.tsx (Server Component — authoritative guard + active-org resolution):
       - const session = await requireSession();  // redirects to /login if unauthenticated (AIDA-10 server-side)
       - If session.session.activeOrganizationId is null, resolve and set it (call auth.api.setActiveOrganization for the user's single org) so getScopedDb works downstream; if still none, this is an unexpected state — render a minimal "No workspace" message rather than crashing.
       - Layout markup: a flex row, min-h-screen. Left: <Sidebar user={...} />. Right: a flex column with <TopBar /> on top and a <main className="flex-1 overflow-auto p-6">{children}</main>.

    2. Create src/components/sidebar.tsx (Client Component for active-state via usePathname):
       - 240px fixed width, bg-muted, full height, flex column.
       - Top: "AIDA" wordmark, text-[18px] font-semibold, p-4.
       - Nav list: items [{href:"/tickets",label:"Tickets",icon:Inbox},{href:"/kb",label:"Knowledge Base",icon:BookOpen},{href:"/settings",label:"Settings",icon:Settings2}]. Each is a Link, h-9 (36px), px-3, rounded, flex items-center gap-2, text-[14px]. Active (pathname starts with href) = bg-primary text-primary-foreground; otherwise hover:bg-accent hover:text-accent-foreground. Icon h-4 w-4.
       - Bottom user area (mt-auto, p-4): Avatar + name (14px) + email (12px muted, truncate). Accept user as a prop from the layout.

    3. Create src/components/top-bar.tsx (Client Component): h-14 (56px), bg-background, border-b, flex items-center justify-between px-6. Left: page title (18px font-semibold) — derive from usePathname (Tickets / Knowledge Base / Settings). Right: <ThemeToggle /> then <UserMenu user={...} />.

    4. Create src/components/theme-toggle.tsx ("use client"): a ghost icon Button using next-themes useTheme(); show Sun in dark mode / Moon in light mode (20px); wrap in a Tooltip with content "Switch to light mode" (when dark) / "Switch to dark mode" (when light); onClick setTheme(theme === "dark" ? "light" : "dark"). Guard against hydration mismatch (only render the icon after mount).

    5. Create src/components/user-menu.tsx ("use client"): a DropdownMenu whose trigger is <Button variant="ghost" aria-label="Open user menu"> wrapping <Avatar> (initials fallback). Menu shows the user's name/email (disabled header item) and a "Sign out" item calling authClient.signOut() then router.push("/login").
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm exec biome check "src/app/(app)/layout.tsx" src/components/sidebar.tsx src/components/top-bar.tsx src/components/user-menu.tsx src/components/theme-toggle.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/layout.tsx contains requireSession() (server-side guard) and resolves activeOrganizationId
    - src/components/sidebar.tsx contains "Tickets", "Knowledge Base", "Settings", the Lucide icons Inbox/BookOpen/Settings2, w-60 (or 240px width), bg-muted, and bg-primary for the active item
    - src/components/top-bar.tsx contains h-14 (56px) and border-b and renders ThemeToggle + UserMenu
    - src/components/theme-toggle.tsx contains setTheme and the tooltip copy "Switch to light mode" / "Switch to dark mode"
    - src/components/user-menu.tsx contains aria-label="Open user menu", "Sign out", and authClient.signOut()
    - No font-medium or font-bold class anywhere in these files (grep confirms two-weight rule)
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The persistent, auth-gated shell renders sidebar + top bar with active-state nav, working theme toggle, and a user menu with sign out — matching the UI-SPEC contract.</done>
</task>

<task type="auto">
  <name>Task 2: Stub pages — Tickets + Knowledge Base empty states (shared EmptyState)</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Implementation Note 7 empty state + Copywriting Contract)
    - src/app/(app)/layout.tsx (from Task 1 — these pages render inside <main>)
  </read_first>
  <action>
    1. Create src/components/empty-state.tsx exporting EmptyState({ icon: LucideIcon, heading: string, body: string }): a centered column (flex flex-col items-center justify-center text-center, min-h-[60vh], gap-3). Icon rendered h-12 w-12 text-muted-foreground; heading text-[18px] font-semibold; body text-[14px] text-muted-foreground max-w-md.

    2. Create src/app/(app)/tickets/page.tsx (Server Component): render <EmptyState icon={Inbox} heading="Your inbox is empty" body="New tickets from customers will appear here. Set up an email channel or embed a web form to start receiving requests." />.

    3. Create src/app/(app)/kb/page.tsx (Server Component): render <EmptyState icon={BookOpen} heading="No articles yet" body="Create knowledge base articles to help your team answer customer questions consistently." />.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && grep -q "Your inbox is empty" "src/app/(app)/tickets/page.tsx" && grep -q "No articles yet" "src/app/(app)/kb/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/empty-state.tsx exports EmptyState and contains h-12 w-12 and text-muted-foreground
    - src/app/(app)/tickets/page.tsx contains the EXACT copy "Your inbox is empty" and the full body string
    - src/app/(app)/kb/page.tsx contains the EXACT copy "No articles yet" and the full body string
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Tickets and Knowledge Base render screenshot-worthy empty states from a shared, reusable component with verbatim UI-SPEC copy.</done>
</task>

<task type="auto">
  <name>Task 3: Settings page + AI toggle persisted to org-scoped Setting via scopedDb</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Settings — AI Toggle interaction contract + Copywriting Contract)
    - src/lib/session.ts (getScopedDb — Plan 03)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-18)
    - src/components/ui/switch.tsx (Plan 01)
  </read_first>
  <action>
    1. Create src/app/(app)/settings/actions.ts ("use server") exporting setAiEnabled(enabled: boolean):
       - const { db } = await getScopedDb();  // org-scoped client bound to the active org
       - await db.setting.upsert({ where: { organizationId_key: { organizationId: <from getScopedDb>, key: "aiEnabled" } }, update: { value: String(enabled) }, create: { key: "aiEnabled", value: String(enabled) } });
         (Because scopedDb injects organizationId on create/where for domain models, confirm the exact upsert where-shape the generated client expects for the @@unique([organizationId, key]) — getScopedDb returns orgId for building the compound where if needed.)
       - return { ok: true } or revalidate the settings path.

    2. Create src/app/(app)/settings/page.tsx (Server Component):
       - const { db } = await getScopedDb();
       - const setting = await db.setting.findFirst({ where: { key: "aiEnabled" } });
       - const aiEnabled = setting?.value === "true";  // default false (D-18)
       - Render a section with heading "AI Features" (18px font-semibold) and <AiToggle defaultEnabled={aiEnabled} />.

    3. Create src/app/(app)/settings/ai-toggle.tsx ("use client"): a row with a shadcn Switch (default from prop) + label "Enable AI" (to the right) + description below (12px muted) "Allow AIDA to triage tickets and draft replies. Configure your AI provider in a future step." onCheckedChange calls the setAiEnabled Server Action (optimistic UI; revert + sonner error toast on failure). No confirmation dialog (non-destructive).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm exec biome check "src/app/(app)/settings" && grep -q "aiEnabled" "src/app/(app)/settings/actions.ts" && grep -q "getScopedDb" "src/app/(app)/settings/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/settings/actions.ts contains "use server", getScopedDb, and writes Setting key "aiEnabled"
    - src/app/(app)/settings/page.tsx reads aiEnabled via getScopedDb and defaults to false
    - src/app/(app)/settings/ai-toggle.tsx contains "use client", a Switch, label "Enable AI", and the exact description copy
    - The org-scoped Setting is written via scopedDb (NOT the bare prisma client) — grep shows no @/lib/db import in settings/actions.ts
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Settings persists the aiEnabled toggle (default off) to the org-scoped Setting via scopedDb — the only AI artifact in Phase 1 (D-18), tenant-isolated by construction.</done>
</task>

</tasks>

<verification>
- pnpm exec tsc --noEmit and pnpm exec biome check pass across all (app) files + components.
- Layout uses requireSession (server-side AIDA-10 guard) and resolves the active org.
- Sidebar/top bar match UI-SPEC (dimensions, tokens, icons, copy, aria-label, two font weights).
- AI toggle reads/writes the org-scoped Setting through scopedDb only.
- (Live rendering + theme switch + sign out are visually confirmed under docker compose in Plan 08.)
</verification>

<success_criteria>
A screenshot-worthy, auth-gated app shell (sidebar + top bar + dark mode + user menu) with Tickets/KB empty states and a working, tenant-scoped AI toggle — "full shell, empty rooms" delivered, with server-side auth enforced in the layout (AIDA-10).
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-06-SUMMARY.md documenting: the active-org resolution approach used in the layout, the exact scopedDb upsert where-shape for the aiEnabled Setting (compound unique), the component file map for Phase 2 (which stubs it will replace), and confirmation the two-font-weight rule holds.
</output>
