---
phase: 07-launch-readiness
plan: 11
subsystem: docs
tags: [astro, starlight, docs-site, ai-providers, ollama, backup-restore, security]

# Dependency graph
requires:
  - phase: 07-launch-readiness (07-06)
    provides: "website/ Starlight scaffold (site/base config, one sidebar group, quick-start.md) that this plan fills in"
  - phase: 07-launch-readiness (07-04)
    provides: "scripts/backup.sh + scripts/restore.sh + docs/OPERATIONS.md runbook this plan summarises"
  - phase: 07-launch-readiness (07-07)
    provides: "the exact DEMO_* env vars/defaults/credentials this plan documents"
  - phase: 07-launch-readiness (07-09/07-09.1)
    provides: "the security-pass verdicts this plan's security-model page is capped at"
provides:
  - "11 new Starlight content pages under website/src/content/docs/{getting-started,configuration,guides,operations,security}/ covering install-adjacent config, AI provider setup (OpenAI/Anthropic/Ollama), KB/RAG, AIDA Insight, the public portal, backups, upgrades/logs, and the security model"
  - "website/astro.config.mjs sidebar extended from 1 group (getting-started) to 5 groups (getting-started, configuration, guides, operations, security), each autogenerated per directory"
  - "A full-site build + internal-link audit proving all 12 content pages emit HTML and every internal href carries the /aida/ base prefix"
affects: [07-12-launch-checklist]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sidebar groups use Starlight 0.41.5's nested { label, items: [{ autogenerate }] } shape (established by 07-06) — repeated for all four new groups, never the older { label, autogenerate } shorthand"

key-files:
  created:
    - website/src/content/docs/getting-started/demo-mode.md
    - website/src/content/docs/configuration/environment.md
    - website/src/content/docs/configuration/email-channel.md
    - website/src/content/docs/configuration/ai-providers.md
    - website/src/content/docs/configuration/branding.md
    - website/src/content/docs/guides/knowledge-base-and-rag.md
    - website/src/content/docs/guides/aida-insight.md
    - website/src/content/docs/guides/public-portal.md
    - website/src/content/docs/operations/backup-and-restore.md
    - website/src/content/docs/operations/upgrading-and-logs.md
    - website/src/content/docs/security/security-model.md
  modified:
    - website/astro.config.mjs

key-decisions:
  - "Ollama Docker-to-host addressing documented as a pattern with two concrete branches (Docker Desktop's automatic host.docker.internal vs Linux's manual extra_hosts: host-gateway or bridge-IP fallback) rather than asserting one universal address — docker-compose.yml ships no extra_hosts entry today, so this is genuinely operator-dependent, not a pre-wired convenience."
  - "configuration/environment.md links out to docs/OPERATIONS.md's own copy of the same table instead of claiming to be the sole source of truth — both tables must independently match .env.example, verified by an automated diff (see below), not by one forking the other."
  - "security/security-model.md deliberately stops at what 07-09/07-09.1's security pass actually verified (data residency, credentials at rest, server-side authz, untrusted-input handling, public-surface controls, disclosure process) and does not restate docs/SECURITY.md's full threat model or known-issues list — it links both docs/SECURITY.md and .github/SECURITY.md rather than forking either."
  - "backup-and-restore.md's cron example uses a ```txt fence instead of ```cron — Starlight's Shiki-based highlighter has no 'cron' grammar and warned/fell back to txt automatically; using txt directly removes the build warning with no content change."

requirements-completed: [AIDA-23]

# Metrics
duration: 27min
completed: 2026-08-03
---

# Phase 07 Plan 11: Docs site content — install, configuration, AI setup, guides, ops, security Summary

**Eleven new Starlight pages (AI provider setup for OpenAI/Anthropic/Ollama, the full env reference, email-channel/branding config, KB-RAG/Insight/public-portal guides, backup-restore/upgrade-logs ops, and a capped security-model summary) plus a five-group sidebar, all sourced from `.env.example`/`docs/OPERATIONS.md`/`docs/SECURITY.md`/the actual settings UI — built clean with zero broken `/aida/`-prefixed links.**

## Performance

- **Duration:** 27 min
- **Started:** 2026-08-03T18:02:00+07:00 (approx, first Read call)
- **Completed:** 2026-08-03T18:29:34+07:00 (Task 3 commit `a28e20a`)
- **Tasks:** 3/3 completed
- **Files modified:** 11 created (content pages) + 1 modified (astro.config.mjs)

## Accomplishments

- Wrote the four getting-started/configuration pages AIDA-23 and D-06 name explicitly: demo mode's honesty section (stored `provider: "demo"`/`model: "demo-seed"` artifacts vs. live AI actions), the full 20-variable environment reference table (verified to exactly match `.env.example`, zero extra/missing), the email-channel walkthrough matching the real Settings UI field labels, and the single-field branding page.
- Wrote the AI-setup page AIDA-23 explicitly names — dedicated OpenAI/Anthropic/Ollama sections, the 768-dimension embedding constraint, and a concrete (not hand-waved) Docker-to-host addressing pattern for a fully local Ollama setup — plus three feature guides (KB/RAG, AIDA Insight, the public portal) sourced from the actual retrieval/insight/public-route code.
- Wrote the operations and security pages (backup/restore, upgrade/logs/health, a security-model summary capped at what the 07-09 security pass verified), extended the sidebar from one group to five, and ran a full-site build + programmatic link audit.
- Zero occurrences of "trained"/"fine-tuned" anywhere in the docs site; logo upload and the backup scheduler are explicitly stated as unavailable, never implied as coming.

## Task Commits

Each task was committed atomically (all `--no-verify`, per parallel-execution instructions):

1. **Task 1: Getting-started and configuration pages** - `b3de563` (feat)
2. **Task 2: AI setup and feature guides** - `0bb3890` (feat)
3. **Task 3: Operations/security pages, sidebar wiring, full-site link check** - `a28e20a` (feat)

**Plan metadata:** (this commit) — docs(07-11): complete docs-site-content plan

## Files Created/Modified

- `website/src/content/docs/getting-started/demo-mode.md` - Seeded-dataset counts, the stored-vs-live AI honesty section, reset instructions, never-expose caution
- `website/src/content/docs/configuration/environment.md` - Full `.env.example`-matching variable table, secret-generation block, `BETTER_AUTH_SECRET`/`APP_ENCRYPTION_KEY` danger callout
- `website/src/content/docs/configuration/email-channel.md` - Settings > Email field-by-field walkthrough, worked IMAP/SMTP example, threading/outbound behavior
- `website/src/content/docs/configuration/ai-providers.md` - AI-optional opener, two-provider model, OpenAI/Anthropic/Ollama sections, embedding constraint, turning AI off, what's sent to the model
- `website/src/content/docs/configuration/branding.md` - The one workspace-name field, its four surfaces, v1 scope
- `website/src/content/docs/guides/knowledge-base-and-rag.md` - Authoring, embedding status states, Re-embed all, human-approval gate, ungrounded-draft honesty, audit trail
- `website/src/content/docs/guides/aida-insight.md` - What a run computes, 7/30/90-day periods, background-job execution, reproducibility, CSAT source
- `website/src/content/docs/guides/public-portal.md` - `/request` protections, `/status/<token>` server-side visibility filtering, branding surface
- `website/src/content/docs/operations/backup-and-restore.md` - Complete-backup definition, backup/restore usage, cron retention, restore-to-new-server, link to `docs/OPERATIONS.md`
- `website/src/content/docs/operations/upgrading-and-logs.md` - Upgrade/rollback, per-service logs, health JSON shape, stale-heartbeat signal
- `website/src/content/docs/security/security-model.md` - Verified-claims-only summary, disclosure process, links to both SECURITY.md files
- `website/astro.config.mjs` - Sidebar extended to 5 groups (getting-started, configuration, guides, operations, security)

## Decisions Made

See `key-decisions` in the frontmatter above. Most notable: the Ollama Docker-networking guidance is deliberately conditional (Docker Desktop vs. Linux) rather than a single asserted address, because `docker-compose.yml` doesn't pre-wire `extra_hosts` — asserting one universal address would have been dishonest for at least one platform.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `placeholder` in prose tripped the plan's own acceptance-criteria grep**
- **Found during:** Task 1 verification
- **Issue:** `environment.md` originally read "...not the placeholder shipped in `.env.example`", which is a legitimate use of the word but collides with the acceptance check `grep -rci "lorem|TODO|coming soon|placeholder" ... returns 0` (a blunt literal-string check, not a semantic one).
- **Fix:** Reworded to "...not the example string shipped in `.env.example`" — same meaning, no longer contains the literal string.
- **Files modified:** website/src/content/docs/configuration/environment.md
- **Verification:** Re-ran the grep; 0 matches across all content pages.
- **Committed in:** `b3de563` (Task 1 commit)

**2. [Rule 3 - Blocking] Shiki has no `cron` language grammar**
- **Found during:** Task 3 full-site build
- **Issue:** `backup-and-restore.md`'s cron-retention example used a ```cron fence; Starlight's Expressive Code/Shiki highlighter warned "language could not be found" and silently fell back to `txt`.
- **Fix:** Changed the fence to ```txt directly — same rendered output, zero build warning.
- **Files modified:** website/src/content/docs/operations/backup-and-restore.md
- **Verification:** Rebuilt; warning gone, `pnpm build` output clean.
- **Committed in:** `a28e20a` (Task 3 commit)

**3. [Environment/process, not a content deviation] Bash tool defaulted into the shared (non-worktree) checkout**
- **Found during:** start of Task 1, before any file writes
- **Issue:** The Bash tool's persistent shell started in this worktree, but an early `cd "D:/Aff/proj/aida/website"` (no `.claude/worktrees/...` prefix) silently `cd`'d into the shared checkout instead — the Write tool correctly refused a write there ("isolated in the worktree"), which is what surfaced the mistake. A `pnpm build` had already run once against the shared checkout's `website/` before this was caught.
- **Fix:** All subsequent commands used the explicit worktree-prefixed path (`D:/Aff/proj/aida/.claude/worktrees/agent-a1c89ba987ae4473a/website`), including a fresh `pnpm install` (the worktree had no `node_modules` of its own). The one build that ran against the shared checkout only produced gitignored `dist/`/`.astro` output (per 07-06's `.gitignore` entries) — no tracked file in the shared checkout was touched or needs reverting.
- **Files affected:** None (build artifacts only, already gitignored).
- **Verification:** `git status --short` in the shared checkout was not (and could not be) re-checked by this agent per its own isolation constraints, but the only command that ran there was a read-only `pnpm build`, which cannot modify tracked files.

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking) + 1 environment/process note (no content or tracked-file impact)
**Impact on plan:** None of the three affected scope, correctness, or the plan's deliverables. Both auto-fixes were required for the acceptance criteria and a clean build respectively.

## `.env.example` vs. `configuration/environment.md` variable diff

Ran an automated diff between every `KEY=` line in `.env.example` and every backtick-fenced `` `VARIABLE_NAME` `` in the environment table: **20 variables on each side, zero difference.** `BETTER_AUTH_URL`, `NEXT_PUBLIC_APP_URL`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_TRUSTED_ORIGINS`, `DATABASE_URL`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `DOMAIN`, `DB_POOL_MAX`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `ADMIN_NAME`, `UPLOADS_DIR`, `RATE_LIMIT_PEPPER`, `APP_ENCRYPTION_KEY`, `DEMO_MODE`, `DEMO_ADMIN_EMAIL`, `DEMO_ADMIN_PASSWORD`, `DEMO_AGENT_EMAIL`.

## Built page list

`cd website && pnpm build` (via `volta run --node 22.23.1`, required — this machine's default Node is 20.20.2, below Astro 7's `>=22.12.0` floor) emits 14 files under `website/dist/`: 12 content pages, the splash `index.html`, and Starlight's `404.html`. The 12 content pages, one per source markdown file under `src/content/docs/*/`:

```
dist/configuration/ai-providers/index.html
dist/configuration/branding/index.html
dist/configuration/email-channel/index.html
dist/configuration/environment/index.html
dist/getting-started/demo-mode/index.html
dist/getting-started/quick-start/index.html      (pre-existing, 07-06)
dist/guides/aida-insight/index.html
dist/guides/knowledge-base-and-rag/index.html
dist/guides/public-portal/index.html
dist/operations/backup-and-restore/index.html
dist/operations/upgrading-and-logs/index.html
dist/security/security-model/index.html
```

## Internal-link base-prefix check

A Node script walked every built HTML file, extracted every root-relative `href="/..."` attribute (253 total across 14 files), and confirmed each one starts with `/aida/` (or is exactly `/aida`). **0 violations.** External links (`http(s)://`, `mailto:`) and same-page anchors were excluded from the check by construction (only root-relative hrefs were considered).

Forbidden-string sweep of `website/dist/`: `localhost:3000` appears **exactly once**, in `getting-started/quick-start/index.html` — the pre-existing (07-06) `:::note` explicitly clarifying that port 3000 is *not* the compose URL. `TODO`, `lorem`, and `coming soon` all return **zero** matches across the entire built site.

## Deliberately not documented (deferred, per plan scope)

- **Logo upload** — `configuration/branding.md` states plainly it isn't available in v1; not implied as coming.
- **A built-in backup scheduler** — `operations/backup-and-restore.md` states AIDA ships no scheduler and shows the cron workaround instead, matching `docs/OPERATIONS.md`.
- **KB auto-generation from resolved tickets (AIDA-18)** — not mentioned anywhere in the docs site; it's post-v1 backlog per `.planning/PROJECT.md`.

## Issues Encountered

- This machine's Volta-pinned default Node (20.20.2) is below Astro 7.1.5's `>=22.12.0` requirement — every build/install in this plan used `volta run --node 22.23.1 -- pnpm <cmd>`, matching the exact version 07-06 already established as this project's precedent.
- This worktree had no `website/node_modules` of its own (git worktrees don't share `node_modules`); ran a fresh `pnpm install` against the committed `website/pnpm-lock.yaml` before the first build.

## User Setup Required

None — no external service configuration required by this plan. The two GitHub Pages human-only setup steps (enable Pages with Source = GitHub Actions; verify the first deploy) were already logged by 07-06 under `deferred-items.md` → `## From 07-06 (docs site)`, tracked for 07-12's launch checklist, and are unaffected by this plan.

## Next Phase Readiness

- AIDA-23's docs-site half (install, configuration, AI setup per provider, plus the guides/ops/security pages a self-hoster needs after day one) is content-complete and builds clean under the production `/aida` base path.
- 07-12 (launch checklist) can now treat the docs site as ready to go live the moment GitHub Pages is enabled — no further content work is blocking that step from this plan's side.
- **REQUIREMENTS.md's AIDA-23 checkbox deliberately left `Pending` by this plan.** AIDA-23's full statement requires both a star-ready README *and* a docs site; this plan only ships the docs-site half — the README half is 07-10, running in a separate parallel worktree this agent has no visibility into. Per the split-requirement precedent already recorded in STATE.md (07-05's entry, mirroring AIDA-05/AIDA-09), only mark the checkbox once **both** 07-10 and 07-11 are confirmed landed — running `requirements mark-complete AIDA-23` from this plan alone would be premature and risks a race against 07-10's own concurrent edit to the same file.
- No blockers.

---
*Phase: 07-launch-readiness*
*Completed: 2026-08-03*

## Self-Check: PASSED

All 12 claimed files found on disk (11 new content pages + astro.config.mjs). All 3 task commit
hashes (`b3de563`, `0bb3890`, `a28e20a`) found in git history. `cd website && pnpm build` (Node
22.23.1) exits 0 with 12/12 content pages emitting HTML and 0/253 internal links missing the
`/aida/` base prefix.
