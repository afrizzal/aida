---
phase: 07-launch-readiness
plan: 12
type: execute
wave: 6
depends_on: ["07-08", "07-09", "07-10", "07-11"]
files_modified:
  - LAUNCH.md
  - CHANGELOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: false
requirements: [AIDA-12, AIDA-22, AIDA-23, AIDA-24]
must_haves:
  truths:
    - "Every quality gate the project defines passes on the merged phase: typecheck, lint, unit, integration, e2e, production build and a docker compose cold start"
    - "A maintainer has a single ordered checklist of the human-only steps required to launch"
    - "v1.0.0 release notes exist and describe only what actually shipped"
    - "AIDA-12, AIDA-22, AIDA-23 and AIDA-24 are marked complete and the roadmap reflects a finished v1"
  artifacts:
    - path: "LAUNCH.md"
      provides: "the ordered human-only launch checklist (repo settings, Pages, tag, release, outreach)"
      contains: "## Repository settings"
    - path: "CHANGELOG.md"
      provides: "v1.0.0 release notes derived from the seven phases"
      contains: "## v1.0.0"
    - path: ".planning/REQUIREMENTS.md"
      provides: "all 23 MVP requirements checked off"
      contains: "- [x] **AIDA-24**"
  key_links:
    - from: "LAUNCH.md"
      to: ".github/workflows/docs.yml"
      via: "the checklist's Pages-enablement step that the workflow depends on"
      pattern: "Pages"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "phase 7 checkbox + requirement statuses flipped together"
      pattern: "Phase 7"
---

<objective>
Close Phase 7 and the v1 milestone: run every quality gate against the fully merged phase, write the release notes, produce the maintainer's launch checklist, and update the planning records so they tell the truth.

Purpose: D-15 defines "launch" in this phase as **repo state, not the outreach act**. Everything a machine can do to make the repository launch-ready must be done and verified here; everything only a human can do (repo settings, enabling Pages, pushing a tag, publishing a release, posting anywhere) must be written down in one ordered list rather than lived in someone's head.
Output: `LAUNCH.md`, `CHANGELOG.md`, updated `REQUIREMENTS.md` / `ROADMAP.md` / `PROJECT.md` / `STATE.md`, a full gate run, and a human sign-off checkpoint.

**Tagging note:** this plan does NOT create or push the `v1.0.0` tag. Plans execute in worktrees, so a tag created here would point at the wrong commit. The exact tag and release commands go into `LAUNCH.md` for the maintainer to run on `master` after the phase merges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-launch-readiness/07-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/DESIGN-SYSTEM.md
@.planning/LOOP-ENGINEERING.md

<interfaces>
<!-- Everything this plan needs to state accurately. -->

**The seven phases of v1** (from ROADMAP.md): 1 Foundation, 2 Core Ticketing, 3 Email Channel, 4 AI Foundation, 5 RAG & Drafted Replies, 6 AIDA Insight, 7 Launch Readiness.

**Requirement status after this phase:** AIDA-01 … AIDA-17 and AIDA-19 … AIDA-24 all complete (23 MVP requirements). AIDA-18 (KB auto-generation from resolved tickets) stays backlog. Plan 07-01 restructured `REQUIREMENTS.md` into `- [ ] **AIDA-NN**` checkboxes plus a `| Requirement | Phase | Status |` table whose third column is literally `Pending` or `Complete` — the format `gsd-tools requirements mark-complete` can mutate.

**The four requirements this phase closes:** AIDA-12 (branding settings, Plan 07-03), AIDA-22 (seed dataset + demo mode, Plans 07-02/07-07), AIDA-23 (README + docs site, Plans 07-06/07-10/07-11), AIDA-24 (backup/restore + ops docs + security pass, Plans 07-04/07-09).

**Quality gates this project defines** (CLAUDE.md + LOOP-ENGINEERING.md): `tsc --noEmit` clean, tests for logic, dogfood through the real UI, and a per-phase hard stop condition checkable by a command. Concretely: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:integration` (needs Node 22: `volta run --node 22.23.1`), `pnpm test:e2e` (also Node 22), `pnpm build`, and a real `docker compose up` cold start.

**DESIGN-SYSTEM.md §9 checklist** (must be answered before a UI phase is marked complete): new tokens in `globals.css` not hardcoded; empty states use the halo + icon-box pattern; sidebar uses `sidebar-*` tokens; top bar sticky + backdrop-blur; auth pages do not self-wrap; typography uses explicit `text-[Npx]`; dark mode tested; `tsc --noEmit` clean. Phase 7's only UI surface is the Settings > Branding tab (07-03) plus the branded sidebar/public-page changes; Plan 07-08's light+dark captures are the dark-mode evidence.

**Deferred items to carry forward honestly:** logo upload (07-03), automated backup scheduler sidecar (D-08), hosted public demo instance (D-04), KB auto-generation / AIDA-18, i18n docs. `.planning/phases/07-launch-readiness/deferred-items.md` accumulates them.

**Known STATE.md tooling quirk:** `gsd-tools state update-progress` and `add-decision` silently no-op on this file (the `Progress:` regex matches the YAML frontmatter's lowercase `progress:` key first, and the heading here is `### Key Decisions`). Hand-edit `STATE.md` directly; do not trust those two commands' reported success.

RTK shell-hook note: if `pnpm exec <tool>` is intercepted, call binaries directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full quality-gate run against the merged phase</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - CLAUDE.md (the quality gates this project holds itself to)
    - .planning/LOOP-ENGINEERING.md (the phase loop and what counts as a hard stop condition)
    - .planning/DESIGN-SYSTEM.md §9 (the eight checklist questions you must answer)
    - .planning/phases/07-launch-readiness/07-09-SUMMARY.md (the security pass verdicts, so you do not re-litigate them)
  </read_first>
  <action>
    Run every gate, in this order, and record the VERBATIM result of each (pass counts, failure output, duration) — this evidence is the phase's hard stop condition:

    1. `node node_modules/@biomejs/biome/bin/biome check .`
    2. `node node_modules/typescript/bin/tsc --noEmit`
    3. `node node_modules/vitest/vitest.mjs run tests/unit`
    4. `volta run --node 22.23.1 pnpm test:integration`
    5. `volta run --node 22.23.1 pnpm test:e2e`
    6. `pnpm build`
    7. `cd website && pnpm build && cd ..`
    8. A real cold start: `docker compose build && docker compose up -d`, wait for `docker compose ps` to show `app` healthy, `curl -s http://localhost/api/health` and assert `"status":"ok"` with a non-null `worker.lastRunAt`, then `docker compose down -v`.

    For any failure: fix it if it is a genuine regression introduced by Phase 7, or record it as a pre-existing/environmental issue with evidence for why it is not this phase's doing. Do NOT weaken a test or a gate to make it pass. If a gate cannot run in your environment (for example no Docker), say so explicitly and route it to the Task 4 checkpoint rather than silently skipping it.

    Then answer the **DESIGN-SYSTEM.md §9 checklist** for Phase 7's UI surface (the Branding tab, the branded sidebar, the branded public pages), one line per question, citing evidence — for the dark-mode question cite the specific `-dark` screenshots from Plan 07-08 and the human's approval.

    Append a `### Phase 7 gate run (<date>)` block to `.planning/STATE.md`'s Open Todos section summarising the results in three or four lines, and record the full detail in this plan's SUMMARY.
  </action>
  <verify>
    <automated>node node_modules/@biomejs/biome/bin/biome check . && node node_modules/typescript/bin/tsc --noEmit && node node_modules/vitest/vitest.mjs run tests/unit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the verbatim result of all eight gates, each marked PASS / FAIL / NOT-RUN with a reason
    - Gates 1, 2, 3 and 6 all PASS (these run without Docker or Node-version gymnastics and have no excuse)
    - Any FAIL is accompanied by either a fix (with the commit) or documented evidence that it is pre-existing and out of Phase 7's scope
    - The SUMMARY contains all eight DESIGN-SYSTEM §9 answers, each with cited evidence rather than an assertion
    - `.planning/STATE.md` contains a `### Phase 7 gate run` block with the date
    - No test file, lint rule, or tsconfig setting was relaxed to make a gate pass (`git diff` on `biome.json`, `tsconfig.json`, `vitest*.config.ts` and `playwright.config.ts` shows no weakening)
  </acceptance_criteria>
  <done>Every quality gate the project defines has been run against the fully merged phase, with verbatim evidence and no gate weakened to obtain a pass.</done>
</task>

<task type="auto">
  <name>Task 2: v1.0.0 release notes and the launch checklist</name>
  <files>CHANGELOG.md, LAUNCH.md</files>
  <read_first>
    - .planning/ROADMAP.md (the seven phases and their success criteria — the source for the release notes' feature list)
    - .planning/REQUIREMENTS.md (the 23 shipped MVP requirements)
    - .planning/phases/07-launch-readiness/deferred-items.md (what is honestly NOT in v1, and the one-time human steps logged by Plans 07-06 and 07-05)
    - README.md (the shipped positioning — the release notes must not exceed its claims)
    - .planning/phases/07-launch-readiness/07-06-SUMMARY.md (the GitHub Pages enablement step that only a human can do)
    - .planning/phases/07-launch-readiness/07-05-SUMMARY.md (any placeholder contact address left for launch)
  </read_first>
  <action>
    **(a) `CHANGELOG.md`** — start it with a `## v1.0.0 — <date>` section (Keep a Changelog style headings: Added / Changed / Fixed / Known limitations). Content rules:
    - *Added*: group by capability area, derived from the seven phases — ticketing core, contacts, tags/custom fields, SLA, attachments; public web intake + tokenised status page + CSAT; email channel (inbound threading, outbound SMTP); model-agnostic LLM layer (OpenAI / Anthropic / Ollama) with encrypted keys and a full off switch; auto-triage; knowledge base with pgvector embeddings; citation-backed drafted replies behind a human-approval gate; append-only AI audit log; AIDA Insight; settings (branding, SLA, tags, custom fields, email, AI); one-command self-host with Caddy; demo mode + seed dataset; backup/restore scripts; docs site; CI.
    - *Known limitations*: be direct — single workspace in the UI (the data model is multi-tenant), no logo upload, no built-in backup scheduler, no hosted demo, KB auto-generation not implemented, integration tests run nightly rather than per-PR.
    - No metrics, no "trained/fine-tuned", nothing not in the repository.
    Add a note at the top that this file starts at v1.0.0 and that earlier development history lives in `.planning/`.

    **(b) `LAUNCH.md`** (repo root) — the maintainer's ordered, human-only checklist. Every item must be a concrete action with the exact command or the exact GitHub screen, and nothing a machine already did. Sections:

    ```markdown
    # Launch checklist — v1.0.0
    ## Before you tag
    ## Repository settings
    ## Tag and release
    ## After the release
    ## Outreach (your call, not automated)
    ```

    - *Before you tag*: confirm the phase is merged to `master`; re-run `pnpm lint && pnpm typecheck && pnpm test && pnpm build`; skim `README.md` as a stranger would; confirm the demo credentials in `.env.example` are the ones the README documents; resolve any placeholder contact address left in `CODE_OF_CONDUCT.md` / `.github/SECURITY.md`.
    - *Repository settings* (each with the exact GitHub path): make the repository public; set the **About** description (draft one, in one sentence, matching the README pitch); set **topics** (draft a concrete list: `helpdesk`, `customer-support`, `open-source`, `self-hosted`, `ai`, `llm`, `rag`, `nextjs`, `typescript`, `postgresql`, `pgvector`, `ollama`, `zendesk-alternative`); upload a **social preview** image (Settings > General > Social preview — suggest reusing a screenshot from `docs/assets/`); enable **Discussions** (the issue-template `config.yml` already links to it, so leaving it off produces a dead link); **Settings > Pages > Source = GitHub Actions** (required or the docs workflow deploys nothing); enable **private vulnerability reporting** (Settings > Code security) so the `.github/SECURITY.md` advisory link works.
    - *Tag and release*: the exact commands —
      ```bash
      git checkout master && git pull
      git tag -a v1.0.0 -m "AIDA v1.0.0 — open-source, AI-native, self-hostable helpdesk"
      git push origin v1.0.0
      gh release create v1.0.0 --title "AIDA v1.0.0" --notes-file CHANGELOG.md --verify-tag
      ```
      with a note that the release body should lead with the hero animation and the quick start.
    - *After the release*: verify the CI badge is green on the public README; verify `https://afrizzal.github.io/aida` renders WITH styling (an unstyled page means the Pages URL and `base: '/aida'` disagree); verify the quick start on a clean machine or VM; add the release badge to the README (Plan 07-10 left an HTML comment marking where).
    - *Outreach*: explicitly framed as the maintainer's judgement call and out of scope for automation. List candidate venues without prescribing timing, and one hard rule: every post repeats the README's honest claims and never invents a metric or a comparison the README does not make.

    Cross-check `LAUNCH.md` against `.planning/phases/07-launch-readiness/deferred-items.md` and fold in every human-only item logged there (notably the Pages enablement and the styled-render verification from Plan 07-06).
  </action>
  <verify>
    <automated>test -f LAUNCH.md && test -f CHANGELOG.md && grep -q "## v1.0.0" CHANGELOG.md && grep -q "## Repository settings" LAUNCH.md && grep -q "Source = GitHub Actions" LAUNCH.md && grep -q "git tag -a v1.0.0" LAUNCH.md && ! grep -Eqi "trained|fine-tuned" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` has a `## v1.0.0` section with Added / Known limitations subsections
    - Its *Known limitations* names at least: single workspace in the UI, no logo upload, no backup scheduler, no hosted demo, no KB auto-generation
    - `grep -Eci "trained|fine-tuned" CHANGELOG.md` returns 0 and `grep -Ec "[0-9]+%|[0-9]+x " CHANGELOG.md` returns 0
    - `LAUNCH.md` contains all five required `##` sections
    - It contains the literal `Settings > Pages` GitHub path with `Source = GitHub Actions`, the topics list, the social-preview step, the Discussions step and the private-vulnerability-reporting step
    - It contains the four verbatim tag/release commands including `git push origin v1.0.0`
    - Every human-only item from `.planning/phases/07-launch-readiness/deferred-items.md` appears somewhere in `LAUNCH.md`
    - No item in `LAUNCH.md` describes work a plan in this phase already completed
  </acceptance_criteria>
  <done>The v1.0.0 release notes describe exactly what shipped (and honestly what did not), and the maintainer has one ordered list of the human-only steps between here and a public launch.</done>
</task>

<task type="auto">
  <name>Task 3: Close out the planning records</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the checkbox + table format Plan 07-01 created — you are flipping four requirements)
    - .planning/ROADMAP.md (the Phase 7 entry as it stands NOW: its Requirements line, its `**Plans:** 12 plans across 6 waves` line, the twelve `- [ ] 07-NN-PLAN.md` entries written at planning time, and the requirement-coverage table)
    - .planning/PROJECT.md (its Validated / Active requirement lists as they stand NOW — verify before editing; some entries 04-VERIFICATION.md once flagged may already have been corrected)
    - .planning/STATE.md (Current Position, progress frontmatter, Key Decisions, Open Todos — all need updating, and the two gsd-tools state commands silently no-op on this file)
    - all .planning/phases/07-launch-readiness/07-*-SUMMARY.md (the source of truth for what each plan actually delivered)
  </read_first>
  <action>
    **(a) `.planning/REQUIREMENTS.md`** — mark the four requirements this phase closed:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" requirements mark-complete AIDA-12 AIDA-22 AIDA-23 AIDA-24
    ```
    Then verify by hand that all four flipped in BOTH the checkbox list and the traceability table (`- [x] **AIDA-12**` … and `| AIDA-12 | Phase 2,4,7 | Complete |`). If the tool misses either representation, hand-edit it and record that in the SUMMARY. After this, exactly 23 requirements are `[x]`/`Complete` and only AIDA-18 remains `[ ]`/`Pending` as backlog.

    **(b) `.planning/ROADMAP.md`** — every step below is idempotent: READ the current line first, and edit only if it does not already read as specified. Report "already correct" rather than claiming a fix you did not make.
    - Ensure the Phase 7 entry in the Phases list is `- [x]` and ends with `(completed <date>)`, in the same style as Phases 1-6. (Planning left it `- [ ]`.)
    - Ensure Phase 7's Plans line reads `**Plans:** 12/12 plans complete`. (Planning wrote `**Plans:** 12 plans across 6 waves` — replace it.)
    - Ensure all twelve `07-NN-PLAN.md` entries are ticked `- [x]` with a `(Wave N)` suffix matching the FINAL wave layout: 07-01 Wave 1; 07-02/03/04/05/06 Wave 2; 07-07/08 Wave 3; 07-09 Wave 4; 07-10/11 Wave 5; 07-12 Wave 6. The list itself already exists from planning — tick the boxes and correct any wave suffix that drifted, do not rewrite the descriptions.
    - Ensure Phase 7's `**Requirements:**` line includes AIDA-12. It should ALREADY read `AIDA-12 (branding remainder), AIDA-22, AIDA-23, AIDA-24` — verify and leave it alone if so.
    - Append a dated "Last updated" line at the bottom summarising Phase 7's execution, mirroring the existing entries' style.

    **(c) `.planning/PROJECT.md`** — idempotent: read its Validated / Active lists FIRST, then move only what is genuinely still misfiled. Target end state: AIDA-12, AIDA-13, AIDA-20, AIDA-22, AIDA-23 and AIDA-24 all appear under Validated and none of them under Active. Some of these (notably AIDA-13 and AIDA-20, which 04-VERIFICATION.md flagged in Phase 4) may already have been corrected — if so, record "already correct" rather than reporting a fix. Also update any "branding/channels/AI config land in Phases 4/7" note to past tense if it is still future-tense.

    **(d) `.planning/STATE.md`** — hand-edit (the `state update-progress` / `add-decision` commands no-op on this file):
    - Frontmatter: `status`, `last_updated`, `last_activity`, and `progress` (`total_plans` and `completed_plans` become 59, `percent: 100`, `completed_phases: 7`).
    - The body `Progress:` bar line — hand-edit it; the tool's regex matches the frontmatter's lowercase `progress:` key first and silently fails.
    - **Current Position**: Phase 7 complete, v1 milestone complete, next action = the human steps in `LAUNCH.md`.
    - **Key Decisions**: append the durable Phase 7 decisions, one bullet each, in the established `- (07-NN) …` style. At minimum: the `provider: "demo"` / `model: "demo-seed"` honesty convention for all seeded AI artifacts; the seed's refuse-on-non-empty guard and WHY a destructive reset is impossible (the append-only AuditEvent trigger blocks DELETE); demo mode being strictly `DEMO_MODE === "true"` with documented credentials; `src/lib/branding/settings.ts` being worker-bundleable and therefore relative-import-only; `website/` being a separate workspace excluded from tsconfig/biome/dockerignore; `.gitattributes` `* text=auto eol=lf` finally closing the six-times-recurring CRLF drift; `src/middleware.ts` now being `src/proxy.ts`.
    - **Open Todos**: close everything Phase 7 resolved; carry forward only what genuinely remains (the `LAUNCH.md` human steps, the deferred items, any known issue the security pass accepted).
    - **Session Continuity**: a Phase 7 entry per plan, in the established format.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*AIDA-" .planning/REQUIREMENTS.md && grep -q "| AIDA-24 | Phase 7 | Complete |" .planning/REQUIREMENTS.md && grep -q "Phase 7: Launch Readiness" .planning/ROADMAP.md && grep -q "12/12 plans complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*AIDA-" .planning/REQUIREMENTS.md` returns 23 and `grep -c "^- \[ \] \*\*AIDA-" .planning/REQUIREMENTS.md` returns 1 (AIDA-18 only)
    - The traceability table shows `Complete` for AIDA-12, AIDA-22, AIDA-23 and AIDA-24
    - `.planning/ROADMAP.md`'s Phase 7 line is `- [x]` with a completion date, its Plans line reads `12/12 plans complete`, all twelve plan entries are `- [x]` with wave suffixes matching the final layout (1 / 2×5 / 3×2 / 4 / 5×2 / 6), and its Requirements line includes `AIDA-12`
    - The SUMMARY distinguishes, for step (b) and step (c), which lines were actually edited and which were already correct
    - `.planning/PROJECT.md` lists AIDA-12, AIDA-13, AIDA-20, AIDA-22, AIDA-23 and AIDA-24 as Validated, with none of them still under Active
    - `.planning/STATE.md` frontmatter shows `completed_phases: 7` and `percent: 100`, and the BODY `Progress:` line matches the frontmatter (hand-verified, not tool-reported)
    - `.planning/STATE.md` Key Decisions contains at least seven new `(07-NN)` entries including the `demo-seed` honesty convention and the `.gitattributes` closure
    - No Open Todo remains that Phase 7 actually resolved
  </acceptance_criteria>
  <done>The planning records match reality: 23 of 23 MVP requirements complete, the roadmap shows a finished v1, and STATE.md carries forward only what genuinely remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: v1 launch sign-off</name>
  <files>(verification only — no source files modified)</files>
  <read_first>
    - README.md (the artefact the human is judging first)
    - LAUNCH.md and CHANGELOG.md (your Task 2 output)
    - .planning/phases/07-launch-readiness/07-SECURITY-PASS.md (the Findings and Known issues sections the human must accept)
    - .planning/phases/07-launch-readiness/07-12-SUMMARY.md draft / your Task 1 gate results (the evidence you present alongside the request)
  </read_first>
  <action>
    This is a human-verification checkpoint — do NOT auto-complete it. Present: the eight-gate result matrix from Task 1, the DESIGN-SYSTEM §9 answers, the requirement close-out (23/23 with AIDA-18 backlog), and the security pass's accepted known issues. Then present the numbered review steps below and PAUSE for the user's reply. On "approved", record the sign-off verbatim and mark Phase 7 and the v1 milestone closed. On blockers, record each verbatim with its file and defect so it can be routed into `/gsd:plan-phase 7 --gaps` — do not patch blockers inside this plan.
  </action>
  <what-built>
    Phase 7 in full: the demo dataset and demo mode (AIDA-22), the Branding settings tab closing AIDA-12, backup/restore scripts with a proven round-trip and an ops runbook plus the written security pass (AIDA-24), the rewritten README with an honest comparison table and a hero animation, and the Starlight docs site covering install/configuration/AI setup (AIDA-23) — alongside CI, community files, and the long-deferred repo-hygiene pass. All eight quality gates have been run (Task 1), the v1.0.0 release notes and the maintainer's launch checklist are written (Task 2), and the planning records are closed out (Task 3).
  </what-built>
  <how-to-verify>
    1. **Read the README as a stranger.** Open it on GitHub (or a markdown preview). Does the hero animation play? Do the badges render? Would you star this? Is any claim one you would not defend in a comment thread?
    2. **Run the quick start on a clean machine or VM**, following only what the README says — no prior knowledge. If you get stuck, that is a defect in the README, not in you. Then repeat with `DEMO_MODE=true` and confirm you land in a populated helpdesk.
    3. **Read `07-SECURITY-PASS.md`'s Findings and Known issues sections.** Are you comfortable shipping with each accepted item? Anything you are not comfortable with becomes a blocker.
    4. **Skim the docs site** (`cd website && pnpm dev`, or the built output). Are install, configuration and AI setup genuinely followable? Any dead link?
    5. **Read `LAUNCH.md` end to end.** Is every remaining step something only you can do, and is anything missing that you would otherwise forget?
    6. **Confirm the honest-claims rule holds** across README, CHANGELOG, docs site and the comparison table: no invented metrics, no "trained/fine-tuned", no competitor claim you cannot support.
  </how-to-verify>
  <resume-signal>Reply "approved" to close Phase 7 and the v1 milestone, or list specific blockers (file + what is wrong). Blockers become a gap-closure plan rather than being patched silently.</resume-signal>
  <verify>
    <automated>MANUAL — human-verify checkpoint. Automated proxy already passed in Task 1: biome check, tsc --noEmit, the unit/integration/e2e suites, both production builds, and a docker compose cold start returning /api/health status ok.</automated>
  </verify>
  <acceptance_criteria>
    - The user was presented with the eight-gate matrix, the §9 answers, the 23/23 requirement close-out and the security pass's accepted known issues BEFORE being asked to approve
    - The user's reply is recorded verbatim in the SUMMARY
    - If the user raised blockers, each is recorded with its file and defect and routed to a gap-closure plan — none is patched inside this plan
    - On approval, `.planning/STATE.md` records the Phase 7 sign-off with the date
  </acceptance_criteria>
  <done>A human has exercised the launch review and either signed off on Phase 7 and the v1 milestone or produced a precise blocker list; the outcome is recorded in the SUMMARY and STATE.md.</done>
</task>

</tasks>

<verification>
- All eight quality gates recorded with verbatim evidence; none weakened.
- `CHANGELOG.md` and `LAUNCH.md` exist, are honest, and contain no step a plan already completed.
- 23/23 MVP requirements marked complete; ROADMAP Phase 7 closed with all twelve plans listed; PROJECT.md and STATE.md reconciled.
- DESIGN-SYSTEM.md §9 answered with cited evidence for Phase 7's UI surface.
- Human sign-off recorded.
</verification>

<success_criteria>
Phase 7 and the v1 milestone are closed with evidence: every gate green, every requirement accounted for, the repository in a state a stranger can clone, run and evaluate — and a single ordered checklist covering the only steps left, which are the maintainer's alone.
</success_criteria>

<output>
After completion, create `.planning/phases/07-launch-readiness/07-12-SUMMARY.md` recording: the eight-gate result matrix, the DESIGN-SYSTEM §9 answers, the requirement close-out (23/23 with AIDA-18 backlog), the release-notes and launch-checklist contents in brief, the human's sign-off text, and any blocker they raised.
</output>
